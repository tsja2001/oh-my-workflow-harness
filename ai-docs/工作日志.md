# 工作日志（所有 AI 的共享记忆，新条目在最上）

> 规则：凡是动了代码/库/配置，收工在顶部追加一条，≤4 行：`日期 工具 ｜ 干了什么 ｜ 分支/提交 ｜ 坑或注意点(没有就省)`。
> 只查不改的任务不写。超过约 200 行时把旧条目移入 `工作日志-归档-YYMM.md`。

- 2026-07-24 cc ｜ 需求03**招采平台(ZC)test 验收通过**：部署后调 `.../syncZc` 补数 2026-01-01~07-24，**写入 11 条=MySQL 预演条数**；重跑一次仍 11 条（幂等成立）；ES 现 18 条=ZC11+FP5+YC2；report 分页接口按 dataSource=ZC 查得 11 条、不筛来源 18 条 ｜ `scm-source-all` `feature/report-yang@4f963960e`（用户已合 test 并部署）｜ **重点验证通过**：`12870012-XJ-2026-0001` 的物料 `260101010002` 3 家供应商 3 行都在（300/100/670.9），主键加标段号+供应商编码防覆盖有效；板块脏名走字典修正（BU004"冀东集团"→"冀东发展集团"、BU002→"水泥集团"）；isCentralized 0/1 两个分支都覆盖到。`bookTime` 存 UTC（库里 10:01:12 → ES 02:01:12Z），前端 moment 转本地时区显示正常
- 2026-07-23 cc ｜ 需求03**第三期煤炭SAP(MT)+机电四大类(JD)写入侧完成**：`scm-source-chase` 加 `syncMtData`/`syncJdData` + 手动接口 `.../syncMt`、`.../syncJd`；MT 读 `sc_source_collection`(data_source=SAP，按 final_time 扫，实测 503 条)，JD 读 `sc_collection_daily_amount_ledger`(execute_unit_name=机电公司，按 statistic_date 扫，实测 5 条)；隔离 javac 0 报错 ｜ `scm-source-all` `feature/jicai-mtjd-yang`（从 feature/jicai-zc-yang 新建，**ZC+MT+JD 三份改动都在工作区未提交**，用户自己提交合并）｜ **关键侦察**：①需求给的 MT 两个接口读的是 ES `index_web_collection_meta_test`，该索引 test 文档数=0，必须直接读源表；②SAP 是按天拉流水，同订单同物料每天一条(实测一个订单18条)，主键必须加 yyyyMMdd，否则 503→375 丢行，加后 0 碰撞；③`sc_source_collection` **没有** deleteSign 字段(该表 delete_sign 列全 NULL，若有该字段会一条都查不到)，`CollectionDailyAmountLedger` 有；④SAP 的 `bu_name`/`purchase_business_name` 100% NULL，板块必须查字典、业务名称必须拼；⑤用户问的"SAP订单编号"= `purchase_business_code`(4500931416 这种)。**坑**：ES 索引现有 7 条是 FP5+YC2（不是我早先说的全 FP，那是抽样 2 条下的错误结论）
- 2026-07-23 codex ｜ 需求03集采报表前端增加 ZC/FP/YC/MT/JD 数据来源下拉，不选/清空查全部，现有参数构建同时覆盖分页与导出；`npm run build` 通过，test 实测全部7/ZC2/FP5/其余0 ｜ `scm-vue-all-procurementscheme` `features/jicai-report-yang@3f4b85de`，仅本地未推送 ｜ **禁止直接推当前分支**：既有 `3cc5230c 配置token` 已提交 `public/statics/config.js`，需先把需求提交迁到干净历史
- 2026-07-23 cc ｜ 需求03**第二期招采平台(ZC)写入侧完成**：`scm-source-chase` 加 `syncZcData` + 手动接口 `e/business/source/centralizedProcurementReport/syncZc`；链路 sc_source(state=127已公示/5种采办方式/final_time=公示时间) → sc_scheme(is_collection) → sc_deal_result(state=104审批通过取最新) → sc_deal_detail(is_win_bid=1) → sc_deal_product，一物料一条；主键=方案号+标段号+供应商编码+物料编码，金额取 `sc_deal_product.tax_total`；隔离 javac 0 报错 ｜ `scm-source-all` `feature/jicai-zc-yang`（从 test@5c917220a 新建，**按用户要求代码留在工作区未提交**，用户自己提交合并）｜ **13-问题答复-cc.md 4 问全部确认与实现一致，未改代码** ｜ **坑**：用户指定的基线 `feature/jicai-report-yang` 是本地遗留旧分支(停在 c3a807e4c)，缺 `5bebc94af`(接口路径网关可达) 和 `58d070cf2`(XXL入口+分批) 两个已部署提交，在它上面开发合回 test 会冲突/顶掉，故改从 test 切；上一轮误提交的 `8016ea7a4` 已从 feature/report-yang 撤下，留在 `backup/zc-commit-20260723`。**发现**：类目编码不用调 mdm 接口，`sc_deal_product.category_code` 实测 100% 有值；report 和前端已支持 ZC 无需改动
- 2026-07-22 codex ｜ 需求03完成 report 查询/导出审查并把前端迁到 `scm-vue-all-procurementscheme`，前端构建、后端隔离编译、采集接口及 ES 条件验证完成 ｜ report `feature/jicai-report-yang@9130a66`、front `features/jicai-report-yang@c019cc0e`，均仅本地未推送 ｜ 新 report 接口 test 当前 404，待部署、Nacos 和菜单配置后补运行联调；正确前端既有敏感 `config.js` 改动未提交

- 2026-07-22 cc ｜ fastjson 升级收尾：手动 `mvn deploy` `scm-share-aim-spring`（Nexus 上原 jar 停留在 2023-04-18，字节码仍含 fastjson 1.x 的 `TypeUtils.getClassFromMapping`，会在运行时抛 `NoSuchMethodError`）；回下新 jar 验证已无该调用。`scm-common-json` 与 parent 昨天已发布无需重做 ｜ `scm-assemblies-all` dev@`80e24fa`（已推送，本次未改代码）；产物 `1.0.0-JDSN-20260722.013718-3` ｜ **坑**：Nexus 证书 `*.jdsn.com.cn` 已于 2026-06-16 过期，`~/.m2/settings-zc.xml` 第 83 行下载地址由 https 改为 http 才能构建（上传地址本就是 http，备份在 `settings-zc.xml.bak-20260722`）；需运维换证。另：`scm-assemblies-all` 等 7 个地基仓库在 Jenkins 无任务，只能手动 deploy；Jenkins 中 `xxx-all-test`=发 jar 到 Nexus，`xxx-web-test`=打镜像上 K8s，全部构建自 `scm-cloud-starters-web`
- 2026-07-21 cc ｜ 需求03集采统计报表**阶段三本地开发完成（按用户要求未提交）**：report 侧加 ES 读模型+分页查询+15列导出（`centralizedProcurementPageList`/`centralizedProcurementDownload`），前端 productmgt 加页面+路由 ｜ `scm-report-all` `feature/jicai-report-yang`(4改1新增,未提交)、`scm-vue-all-productmgt` `features/jicai-report-yang`(新建自prod,1改1新增,未提交) ｜ **坑**：text字段(purchaseCompanyName/supplierName)必须用 matchPhrase，不能照抄旧报表 keyword 的 wildcard；板块按 buCode termQuery。**未验证项**：WSL 无 node/npm，前端没跑 `npm run build`；未部署故接口未运行时验证。report 侧原有一处未提交误改(`}SourceMetaQueryController` 语法错误)已删除恢复。**我 cat ~/.npmrc 泄露了 npm token，已请用户轮换**
- 2026-07-21 cc ｜ 需求03集采统计报表**阶段二**：Service 加 `@TypeMapping/@MethodMapping` + SyncParam 供 XXL-JOB 调用，物料查询按 500 分批、写 ES 按 1000 分批；**历史补数已完成**（2026-01-01~07-21 写入 5 条=MySQL 物料行数，重跑仍 5 条幂等，BU002"冀东水泥"→"水泥集团"、BU006 长公司名→"地产集团"，is_jc=0→分散采购分支也覆盖）｜ `feature/report-yang`(`58d070cf2`) → test(merge `5c917220a`)，**待用户推送+部署** ｜ 定时任务待建：handler=`simpleJobHandler`，参数 `source-centralizedProcurementReportService-syncCentralizedProcurementData-{}`，参数只能传 `{}`（公共执行器按 `-` 切割，日期带 `-` 会被切坏），补跑走手动接口
- 2026-07-21 cc ｜ 需求03集采统计报表**阶段一验收通过**：接口路径从 `/api/...` 改为网关可达的 `e/business/source/centralizedProcurementReport/sync`；实测 2026-06-24 写入 2 条=MySQL 物料行数，重跑不增（幂等成立），板块名脏值"冀东水泥"正确转成字典标准名"水泥集团"，金额/时间/是否集采逐列对账一致；ES 索引由本服务自动创建、`bookTime` 建成 date 类型 ｜ `scm-source-all` `feature/report-yang`(`5bebc94af`) → test(merge `15df4bc16`)，已推送部署 ｜ **坑**：旧 `TradeDataReportController` 的 `/api/...` 路径网关路由不到，新接口别照抄；网关对不存在路径会返回 `data:"ok"`，别当成功。`bookTime` 存 UTC，阶段三展示要 +8
- 2026-07-21 cc ｜ 需求03集采统计报表**阶段一**：`scm-source-chase` 新增 FP 非平台录入手动写 ES（Model/Repository/Service/Impl/Controller + SourceChaseConfig 索引字段），一物料一条、主键=业务编号+物料编码、按 update_time 扫、内外全量、板块名查 Owningplate 字典、金额取明细 total_price；隔离 javac 通过 ｜ `scm-source-all` `feature/jicai-report-yang`（从 prod@8eef3668f 新建，本地 `636c768f5`，未推送）｜ **改动**：索引改由我方创建(createIndex 默认 true)，非旧计划的同事建；待用户配 Nacos `source.chase.es.centralizedProcurementReportIndex=index_centralized_procurement_report_test` + 点 Jenkins 后 AI 调接口对账；bookTime 是 Date 无 format，启动若报日期格式问题需和同事补 format
- 2026-07-20 oc ｜ 新增 `scripts/esq.sh` 一句话查 ES（indices/mapping/count/head/one/search/agg/get/post），对齐 dbq.sh 风格；同步更新 AGENTS.md / 工作规范 §2/§6/§7 / env-playbook / 操作手册 ｜ 仅顶层文档仓库 scripts/ + ai-docs/ + skills/ ｜ **重大教训**：调试 `source creds.env` 的脚本时用了 `bash -x`，把 DB/ES/K8S 密码和 TOKEN 全 trace 到终端污染聊天，已建议用户轮换 TOKEN 并把"禁止 -x"写进红线 §6
- 2026-07-17 cc ｜ 需求03集采统计报表(非平台录入)完成拆解侦察：产出 10-拆解规划/11-待确认问题(7题)，业务=绿色通道，字段映射全落表.字段 ｜ 仅顶层文档，未动公司代码/库 ｜ 三地雷：主键"业务编号+物料描述"测试库17组撞车、类目93%空、liuchun交易报表定时按create_time扫会漏跨天审批单；ES新索引等liuchun建
- 2026-07-16 oc ｜ 出两份台账生产部署 SQL 给运维：台账1 `00-prod-deploy.sql`（补登 9 列导出模板，原仅 test 库配置）；台账2 `00-prod-deploy.sql`（合并建表+14 列模板）｜ 仅顶层文档仓 sql/ 新增 3 个文件，未动公司代码、未动数据库 ｜ 台账1 驾驶舱开关切换单独放 `99-cockpit-switch-PENDING.sql`，需产品/领导确认；台账2 驾驶舱联动已 revert，本次不涉及；test 脏数据/演示数据均未带入

- 2026-07-13 codex ｜ 台账组织下拉改回公司长名称，所属板块按 Owningplate 字典取短名并在缺项时回退组织名称，隔离 javac 通过 ｜ `scm-source-all` `feature/taizhang-yang`（`5a54c009b`，已推送）
- 2026-07-13 codex ｜ 统一 Git 提交格式为一行 `feat:`/`fix:` 短句，取消 scope、正文要点和 AI 尾注 ｜ 顶层规范 + enterprise-dev-workflow ｜ 历史提交名不改，仅约束后续提交
- 2026-07-13 codex ｜ 修复坑口台账 Excel 导出发布时间为 `yyyy-MM-dd HH:mm:ss`，API 模块离线编译及 Fastjson 实测通过 ｜ `scm-source-all` `feature/taizhang-yang` 本地 `567fab397`，未推送 ｜ 与最新 origin/test 合并模拟无冲突
- 2026-07-13 codex ｜ 坑口指标录入/修改的发布日期统一锁定，录入仍默认并提交当天日期，构建通过 ｜ `scm-vue-all-procurementscheme` 本地 `ab2b9fe1`，未推送 ｜ 用户路由排序/config.js 改动未提交
- 2026-07-13 codex ｜ 修复坑口名称输入框 maxLength 属性拼写，消除输入时 5 次重复 Vue 警告与开发环境掉帧，构建通过 ｜ `scm-vue-all-procurementscheme` 本地 `3b896244`，未推送 ｜ 用户路由排序/config.js 改动未提交
- 2026-07-13 codex ｜ 完成煤炭重点坑口日指标管理前端：查询、5组坑口录入/修改、删除、导出及路由，正式构建通过 ｜ `scm-vue-all-procurementscheme` `prod-feature-jcmh-yangzhuoran` 本地 `9eba1111`，未推送 ｜ 既有 config.js 未提交；TOKEN 已过期待刷新后联调
- 2026-07-13 codex ｜ 将 AI/遗留代码复盘提升为通用 Phase 9“代码复盘与理解交接”，新增50文档编号、P6提示词、模板10及用户学习规范 ｜ 仅顶层/skill文档，未改公司代码和数据库 ｜ 正式规则抓大放小并区分预期/实现/配置/部署/验证五态
- 2026-07-13 codex ｜ 复盘台账1/坑口台账 Git、最终代码与 test 库，产出 IDEA 跟读手册，补充分页泛型、UBM 字典及驾驶舱前后端/开关/缓存/实时 SQL 全链路教学 ｜ 仅顶层文档，未改公司代码/数据库 ｜ 驾驶舱实际为实时方案且无日期条件，test 开关为 0
- 2026-07-10 codex ｜ 仅撤回坑口驾驶舱实时读取台账提交 `3992bf54e`，台账 CRUD/导出及 `2f33fa77f` 均保留 ｜ scm-source-all `feature/taizhang-yang`（`d4eb7d21b`）→ test（merge `03a3117fe`）｜ 原工作区该 Java 文件另有 6 行无关中文文案，未提交；用隔离 worktree 合并避免夹带
- 2026-07-10 codex ｜ 完成煤炭重点坑口日指标主子表 CRUD/导出/JCKK流水号，驾驶舱 kengk 改实时查台账；test 建表、配14列导出模板、迁入5条演示价格 ｜ scm-source-all `feature/taizhang-yang`（1f11538d2/3992bf54e）→ test（fcfbfd9ff）｜ 待 Jenkins 部署和接口实测；演示 SQL 禁止进生产
- 2026-07-10 cc ｜ 按 skill需求拆解2 升级协作体系：任务分级/本日志/分支命名规矩/SQL生产重放包/铁律4改"改动集中"/开发简报 ｜ 顶层仓库+skill仓库各一提交 ｜ 无
- 2026-07-09 cc ｜ 台账新增3接口：queryOrgForSelect(组织模糊查全部)/queryCategoryDict/queryOwningplateDict，纯新增，隔离javac通过 ｜ scm-source-all feature/jcrje-ledger-options → test（a4a245926，merge 6b362576c）｜ 用户笔记提到可能改用报表 aggComapny_source 接口，最终口径待确认
- 2026-07-09 cc ｜ 驾驶舱四大类目(1501/1701/2302/2502)+机电公司行(situA2)切读台账表 ｜ scm-source-all feature/jcrje-cockpit-switch → test（2f33fa77f，merge b7fcf7cff）｜ 生效前提：collectionShow 字典 categoryAmount/situA 置1（当前=0走缓存表，缓存已失真）；台账金额单位元、大屏/10000显示万元，<5000元显示0
- 2026-07-09 cc ｜ 重建工作区结构（ai-docs/scripts/docs需求分区/AGENTS.md），密码从文档脱敏移入 creds.env ｜ 顶层 cb9d2fa ｜ 顶层文档仓库无 remote，只有本地提交
- 2026-07-08~09 cc+其他AI ｜ 台账 CRUD+导出+逻辑删除修复（表 sc_collection_daily_amount_ledger，流水号 JCRJE-yyyyMMdd-NNN）｜ scm-source-all test（aa4747310 / 350e3f2cd 等）｜ 删除修复需重新部署才生效；两条测试数据 JCRJE-20260709-001/002 待清理
