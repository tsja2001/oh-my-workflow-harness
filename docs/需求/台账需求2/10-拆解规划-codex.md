# 台账需求2 · 任务拆解计划文档

> 日期：2026-07-13
> 工具：codex
> 当前状态：台账后端 CRUD/导出及前端管理页面均已开发；前端在当前分支完成主体提交 `9eba1111` 和输入性能修复 `3b896244`，正式构建通过，尚未推送；驾驶舱联动仍不在本版本范围。
> 下一阶段：用户明确发起推送后再推前端分支并点 Jenkins；找张雨按页面路由配置菜单岗位，随后测试管理页面全链路。

> **本文档的定位**：整合并取代此前关于“台账需求2”的零散讨论，作为后续 AI 开发的唯一主计划。
> 第一部分写给用户看，不要求懂 Java；第二部分写给执行开发的 AI。
> 文中代码路径均已逐一核实存在；数据库事实均来自 2026-07-10 对 test 库的只读查询。

> **2026-07-10 最新范围变更（优先级最高）：** 驾驶舱交付日期调整到下一版本。本版本不把 `kengk` 接到新台账；本文后续关于“实时查询新台账、删除后驾驶舱回退”的内容仅保留为历史方案，不代表当前待交付行为。较早的 `2f33fa77f` 不撤回。

## ⭐ 执行进度

**已完成：**

0. ✅ 2026-07-13 Codex：根据全部功能提交、`origin/test` 最终代码、建表/导出 SQL 和 test 库主子数据，完成《代码复盘与 IDEA 跟读手册》（`90-临时-代码复盘与IDEA跟读-codex.md`），明确区分“台账管理已保留”和“驾驶舱联动已撤回”。
1. ✅ 逐条拆解需求原文和页面草图（证据：`docs/需求/台账需求2/台账需求2.md:3-46`）。
2. ✅ 核实驾驶舱已有前端页面和后端查询接口，不需要新建驾驶舱地址（证据见本文第 3 节）。
3. ✅ 核实 test 库当前有 5 条旧坑口数据，但它们是 `sc_collection_data` 缓存数据，尚未和新台账相连（查询结果见本文第 3 节）。
4. ✅ 找到上一版“集采日金额统计台账”的查询、导出、新增、修改、删除、详情、流水号和审计字段样板（路径见本文第 4 节）。
5. ✅ 把必须问人的问题压缩为 5 个，其余低感知事项已自行拍板并留痕。
6. ✅ 2026-07-10 cc：回收用户答复（`01台账需求追问回答.md`），二次核实代码——驾驶舱表格是循环渲染没写死 5 行（`index.vue:164`）、`queryKengk()` 仍为空、现有三个实时分支由 `collectionShow` 字典开关控制（`SourceCollectionDataServiceImpl.java:52-57`）。问题 1/2/4 已解决，问题 5 话术升级并加撞车协调（见 6.5 节）。
7. ✅ 2026-07-10 用户拍板：表结构按主表+明细表（数量可扩展），"坑口是否超过 5 个"不再需要问产品；数据库设计已按主子表定稿（第二部分第 2 节）。
8. ✅ 2026-07-10 回收最终答复：旧 5 条只作为 test 演示数据迁入新表，缺失的单位、日期、操作人使用明确标注的合理测试值；生产 SQL 包不包含这些演示数据。
9. ✅ 2026-07-10 驾驶舱后端同事确认：不读缓存表，`kengk` 直接改为实时查询新台账；共享代码可以直接修改。
10. ✅ 2026-07-10 Codex：从 `origin/prod` 建长期分支 `feature/taizhang-yang`，完成台账主体提交 `1f11538d2` 和驾驶舱实时接线提交 `3992bf54e`，均已推远端。
11. ✅ 2026-07-10 Codex：试合并确认仅含本需求 10 个 Java 文件，merge `fcfbfd9ff` 已推送 `test`。
12. ✅ 2026-07-10 Codex：test 执行 `sql/01~03`；主表 1 条、明细 5 条演示数据、导出模板 14 列均已反查；隔离 javac 生成 15 个 class，零错误。
13. ✅ 2026-07-10 Codex：按用户最新决定，仅在本地 `feature/taizhang-yang` 撤回 `3992bf54e`，新提交 `d4eb7d21b`；文件内容与 `3992bf54e^` 完全一致，隔离 javac 通过。本地 `test` 保持 `fcfbfd9ff`，备份分支 `backup/test-before-revert-3992-20260710` 仅保留本地，未进行任何远程操作。
14. ✅ 2026-07-10 cc 复核（只读+可回滚模拟，无远程/无提交）：
    - 撤回**干净彻底**：`3992bf54e` 只改 1 个共享文件 `SourceCollectionDataServiceImpl.java`（加 import、@Autowired、`queryCollectionData` 顶部 KENGK 分支、`queryKengk()` 实现共 4 处）；`d4eb7d21b` 精确移除这 4 处，与改动前逐字节一致。台账主体 `1f11538d2`（9 个全新 `CoalPit*` 文件、815 行纯增）未受影响。
    - **模拟合并验证**：`git merge --no-commit --no-ff feature/taizhang-yang` 进 test，仅带入 1 个文件的还原（`SourceCollectionDataServiceImpl.java` −8/+1），无冲突、无其他文件——已 `--abort` 撤销。
    - **残留（有意保留）**：台账服务里 `CoalPitDailyIndicatorService#queryCockpitData()` + 其实现（`CoalPitDailyIndicatorServiceImpl.java:173`）为当前无调用方的死代码，可正常编译、不影响功能；下版本做驾驶舱正好复用，暂不删除。
    - **撤回后大屏行为**：`queryKengk()` 恢复返回 null → kengk 落回 `else` 分支读 `sc_collection_data` 5 条旧缓存 → 大屏红框继续显示现有 5 个数，维持现状、不空不乱。
    - 全部 4 个相关提交作者均为 杨卓然：`2f33fa77f`(07-09 台账1驾驶舱)、`1f11538d2`/`3992bf54e`/`d4eb7d21b`(07-10 本需求)。
15. ✅ 2026-07-10 Codex：用户明确允许远程操作后，已推送 `feature/taizhang-yang` 的 `d4eb7d21b`，并通过 `--no-ff` 合入、推送远程 `test`（merge `03a3117fe`）；最终只带 1 个 Java 文件的 −8/+1 撤回。
16. ✅ 2026-07-13 Codex：在前端仓库当前分支 `prod-feature-jcmh-yangzhuoran` 新增管理页面和路由，覆盖流水号/单位/发布时间查询、5 组坑口列表、录入发布、详情回显修改、删除及 Excel 导出；`npm run build` 两次通过，本地提交 `9eba1111`，未做远程操作，既有 `public/statics/config.js` 改动未纳入提交。
17. ✅ 2026-07-13 Codex：根据用户测试反馈，将坑口名称输入框错误的 `:maxlength` 改为 Ant Design Vue 1.7.8 可识别的 `:max-length`，消除每次输入时 5 个组件重复打印 Vue 警告造成的开发环境掉帧；`npm run build` 通过，本地提交 `3b896244`，用户调整的路由顺序和 `config.js` 未纳入提交。

**用户已定（2026-07-10）：** ①台账1 的 `2f33fa77f` 不撤，属另一个已交付需求、且开关关着无害。②本次只撤坑口驾驶舱接线；用户随后已明确允许推送，feature 和 test 均已完成。

**需要用户做的：** ①需要推送时明确告诉 AI；②推送并部署前端后，找张雨按路由 `/businessManagementLedgerJC/coalPitDailyIndicator` 配“煤炭重点坑口日指标管理”菜单及执行单位岗权限；③刷新 `ai-docs/creds.env` 的 TOKEN 后做页面全链路测试。

**本阶段不做的：** 不交付台账到驾驶舱的联动，不撤回 `2f33fa77f`，不删 `queryCockpitData()` 死代码，不擅自改菜单岗位权限，不把 test 演示数据带进生产，**未获用户明确许可不做任何远程操作**。

---

# 第一部分：写给你看的

## 1. 这个需求到底是做什么

给招采平台加一本“煤价日报账本”：执行单位每天录入最多 5 个坑口/港口的含税煤价，提交就算发布；账本可以查、改、删、导出。领导驾驶舱右侧那张“煤炭重点坑口日指标”表继续用原地址，但以后应该自动显示这本账里最新一版的 5 个价格。

这不是“每天 24 点自动抄数”的定时任务。需求原文是执行单位在 24 点前完成录入，系统要做的是提交后立刻发布并让驾驶舱能读到，截止时间本身是对人的要求（`台账需求2.md:5`）。

## 2. 产品需求逐条翻译

| 产品原话 | 大白话翻译 | 谁来做 |
|---|---|---|
| 增加“煤炭重点坑口日指标管理”，支持录入、导出、删除、修改（`:3`） | 后端新增一套台账接口和新业务表；前端页面等同事代码 | 后端 + 前端 |
| 执行单位岗有菜单权限（`:4`） | 找权限同事把菜单分给指定岗位，不需要业务后端硬编码权限 | 配置；用户联系同事 |
| 每日 24 点前完成前一日价格（`:5`） | 人在截止时间前录；需求没有“系统到点自动执行”的动作 | 无需定时开发；产品需确认“价格属于哪一天” |
| 发布后自动更新驾驶舱（`:5-6`） | 台账提交成功后，原驾驶舱接口下一次查询就拿到最新有效数据 | 后端；驾驶舱前端地址不改 |
| 按最新发布日显示当日价格（`:6`） | 后端必须定清楚“最新”的排序、同日多条听谁的、删除后回退规则 | 后端；✅ 已定：发布日期新→同日发布时间新→删除回退上一条 |
| 新增后直接发布（`:7`） | 不做草稿和审批状态；保存成功即对驾驶舱生效 | 后端 |
| 可修改单位和坑口 1~4（`:8`） | 修改接口要更新业务字段，同时刷新发布时间、操作人 | 后端；✅ 已拍板 1~5 均可改（原文只写到 4 判定为笔误） |
| 删除本行（`:9`） | 页面看不见，但数据库保留记录，方便审计和回退 | 后端（逻辑删除） |
| 修改后更新发布时间和操作人（`:10`） | 每次修改由服务器记录当前时间和当前登录人，不能信前端自报 | 后端 |
| 查询条件：流水号/执行单位/发布时间（`:14`） | 流水号模糊查、单位按编码精确查、发布时间按起止时间查 | 后端 + 前端 |
| 列表固定出现坑口 1~5（`:18-22`） | 一条台账记录在页面上横向展示最多 5 组“名称+价格” | 后端 + 前端；✅ 已定主表+明细表，页面当前按 5 组 |
| 执行单位用组织机构控件，发布日期默认提交日期（`:27-28`） | 前端复用组织选择器；后端保存单位编码+名称，发布日期由服务器给默认值 | 前端 + 后端 |
| 价格单位为元/吨（`:32-38`） | 后端保存含税单价数字，前端继续显示“元/吨” | 后端 + 前端 |
| 修改弹窗样式未定（`:42-46`） | 不阻塞后端；新增和修改复用同一套字段即可 | 前端后续决定 |

## 3. 现有驾驶舱到底有没有接口

结论：**接口已经有，页面也已经在调用；但它现在只读 5 条人工/历史缓存，和本次要建的新台账没有连接。**

证据链如下：

1. 现有驾驶舱页面右侧表格字段就是“坑口名称 / 今日含税单价 / 单位”，逐行渲染 `planData`（`02zhaocai-front/scm-vue-all-cockpit/src/views/procurementCatalogMgt/index.vue:149-170`）。需求截图 `驾驶舱页面截图.png` 与该区域一致。
2. 页面已经调用 `GET /e/business/source/source/cockpit_collection_data_kengk`，并把 `dataDesc` 映射为坑口名、`dataValue` 映射为价格、单位固定为“元/吨”（同文件 `:492-507`）。
3. 后端统一入口真实存在：`SourceQueryController.java:555-558` 接收 `typeCode` 并调用 `queryCollectionData(typeCode)`。
4. 类型枚举已预留 `KENGK("kengk", "煤炭重点坑口日指标")`（`CollectionDataTypeEnum.java:3-10`）。
5. 当前服务只有中心头部、类目金额和情况分析走实时查询；其他类型直接按 `data_type` 读取 `sc_collection_data`（`SourceCollectionDataServiceImpl.java:43-62`）。专门的 `queryKengk()` 仍返回 `null`（同文件 `:81-83`）。
6. 2026-07-10 查询 `scm_source_test.sc_collection_data WHERE data_type='kengk'` 返回 5 行：`60001~60005`，价格分别为 `575/677/650/660/730`，名称与截图完全一致。`SHOW CREATE TABLE` 同时确认该表只有通用 `data_desc/data_value` 和扩展字段，没有发布日期、发布时间或操作人，无法自己判断“哪一天最新”。
7. 2026-07-10 查询 `scm_ubm_test` 的 `collectionShow` 字典，只存在 `centerHeader/categoryAmount/situA` 三个开关，没有 `kengk` 项；所以 `kengk` 当前确定走第 5 点所述的缓存分支。
   **2026-07-10 cc 补充开关实际值**：`centerHeader=1`（开，大屏头部数字实时算）；`categoryAmount=0`、`situA=0`（关，2026-01-29 有人改过）。而台账1 的实时汇总 SQL 早已写好——`SourceCollectionMapper.java:112-143` 里，大屏"钢材/润滑剂/电线电缆/轴承及备件"和情况分析"机电公司"直接 SUM 台账1 的 `sc_collection_daily_amount_ledger`（该表已有 10 条真实数据）。开关没开 → 大屏类目金额/情况分析仍显示手工缓存数。**即台账1 驾驶舱接线处于"代码已开发、未启用"状态**；这只是侦察事实，最终答复已明确 `kengk` 不沿用开关模式。
   另查明缓存表 `sc_collection_data` 全部家底（2026-07-10）：centerHeader 7 条、doRate 4 条（对应台账2 的执行分析各率）、categoryAmount 7 条、situA 3 条、kengk 5 条、mt/gc 各 12 条（对应台账4 的 12 个月价格趋势）——整块大屏最初就是靠这张表的手工数撑起来的。
8. 运行态 curl 还差一步：`scripts/api.sh` 检测到本地 TOKEN 为空，故本阶段没有声称“test 接口已经实际请求成功”。

因此，产品说的“自动更新”不需要再造一个驾驶舱 API，也不需要定时任务；只需把现有 `kengk` 分支改为实时查询新台账的最新有效记录。同事已确认不再读取缓存表；test 用旧 5 条价格生成一条演示台账，避免部署后大屏空白。

## 4. 系统里现成的样板

以下文件均在 2026-07-10 用 `test -f` 核实存在：

| 这次要做的 | 已存在的样板 | 参考什么 |
|---|---|---|
| 台账分页查询和统一导出 | `01zhaocai-end/scm-source-all/scm-source-source/src/main/java/com/pcitc/scm/source/source/controller/CollectionDailyAmountLedgerController.java:37-48` | `DefaultPageBean`、日期区间转换、`@ExportRequestTransfer` |
| 新增/修改/删除/详情接口 | 同文件 `:51-75` | 5 个接口的路径和返回包装风格 |
| 查询条件、分页、排序 | `.../service/impl/CollectionDailyAmountLedgerServiceImpl.java:63-100` | MyBatis-Plus 条件拼装、分页结果转 DTO |
| 直接发布和操作人 | 同文件 `:102-112,179-186` | 新增生成流水、服务器写发布时间和当前登录人 |
| 防止修改字段越权 | 同文件 `:114-129` | 流水号、创建信息、删除标记不能被前端覆盖 |
| 逻辑删除和详情 | 同文件 `:131-147` | `deleteById` 配合 `@TableLogic`，已删数据不回显 |
| 组织字段和日期 JSON 格式 | `.../api/dto/CollectionDailyAmountLedgerDTO.java:26-50,70-114` | 单位 ID/编码/名称双存、时间格式、区间查询字段 |
| 通用审计字段 | `.../model/CollectionDailyAmountLedger.java:81-120` | 发布人、创建人、更新人、逻辑删除注解 |
| `JCKK-日期-三位流水` | `.../seq/CollectionDailyLedgerCodeSequence.java:11-69` | 将 `JCRJE` 换为 `JCKK`，按服务器当天重置三位流水 |
| 驾驶舱统一返回结构 | `.../model/SourceCollectionData.java:18-59` | 继续返回 `dataDesc=坑口名`、`dataValue=价格`，不改前端协议 |
| 驾驶舱查询入口 | `.../service/impl/SourceCollectionDataServiceImpl.java:43-83` | 在既有 `KENGK` 专属分支实现实时查询，不再读取旧缓存 |
| 导出模板配置 | test 库 `scm_simple_template` / `scm_simple_template_sub` | 已有 `collection-daily-ledger-export` 模板共 9 列，可照同样配置新模板 |

## 5. 清楚的部分与必须暂停的部分

**现在已经清楚、答案回来后可直接开发：**

- 后端落在 `scm-source-all` 的 `scm-source-source-api` 和 `scm-source-source`；不另起微服务。
- 新增台账表、DTO、Model、Mapper、Service、Controller 和 `JCKK` 流水号类。
- 接口覆盖分页/导出、新增、修改、删除、详情；新增成功即发布。
- 查询仍使用流水号、执行单位、发布时间；驾驶舱继续使用原 GET 地址。
- 菜单、岗位权限和新管理页面不写入本次后端代码，由用户等前端提交后找张雨配置。
- “24 点前录入”不是定时任务；本次不开发 XXL Job。

**有模糊点，确认前禁止写最终表结构和驾驶舱选择逻辑（2026-07-10 cc 更新，多数已解决，详见第 6 和 6.5 节）：**

- ~~独立价格日期~~ 已解决：不加，发布日期（表单日期控件、默认当天、可修改）就是日期轴。
- ~~驾驶舱展示哪条、删除怎么回退~~ 已解决：发布日期最新优先，同日按发布时间最后一条；删除回退上一条。
- ~~坑口数量~~ 已解决（2026-07-10 用户拍板）：主表+明细表，数量可扩展，不再需要问产品。
- ~~旧 5 条数据~~ 已解决：作为 test 演示数据迁入新台账，缺失字段使用明确标注的测试值；不进入生产重放包。
- ~~能否修改共享的驾驶舱查询服务~~ 已解决：同事确认可直接改，并明确要求 `kengk` 实时查询、不读缓存表。

## 6. 确认进展（2026-07-10 cc 回收用户答复后）

详细逐字话术见 `11-待确认问题-cc.md`（已取代 codex 版）。原 5 个问题现状：

| # | 原问题 | 现状 |
|---|---|---|
| 1 | 是否单独保存价格日期 | ✅ 已解决：不加字段。用户澄清"发布日期"本就是表单里的日期控件（默认提交当天、可修改），补录历史价格就把发布日期改成对应那天，追溯需求已覆盖，与原问题不冲突。 |
| 2 | 同日多条驾驶舱听谁的 | ✅ 用户拍板采纳推荐：发布日期最新优先，同日按发布时间最后一条；删除后回退上一条有效记录。若产品日后有异议，只改一处查询规则，返工小。 |
| 3 | 是否固定最多 5 个坑口 | ✅ 已解决：cc 核实前后端都没写死——驾驶舱表格循环渲染回几条显示几条（`index.vue:164`），后端 `queryKengk()` 为空，新录入页面还没开发，"5"只来自产品原型。2026-07-10 用户拍板：表结构直接按主表+明细表设计，数量可扩展，**不再需要问产品**。坑口 1~5 均可修改、名称自由输入已自行拍板（见第二部分拍板表）。 |
| 4 | 旧 5 条数据怎么过渡 | ✅ 最终答复：旧价格作为 test 演示数据迁入新台账，缺失字段填合理测试值；演示数据单独存档，不进生产。 |
| 5 | 驾驶舱接线和共享代码 | ✅ 后端同事确认：不读缓存表，直接实时查询新台账；共享代码可直接修改。 |

## 6.5 问题 5 的大白话解释 + 全组任务冲突面（2026-07-10 cc 新增）

**问题 5 到底在问什么：** 驾驶舱右侧那张坑口价格表的数字，现在是从一张"白板"（`sc_collection_data` 缓存表）上读的，白板上是 5 条手工写的数，没日期、没操作人。确认的改法是：大屏、接口地址、返回格式全都不动，只把后端"去白板抄数"的逻辑改成"直接去新台账里拿最新一版价格"。同事已允许修改共享文件，并明确不要再读白板；部署前通过 test 演示数据保证画面有数。

**最终接线方式（2026-07-10 答复）：** `kengk` 不照其他模块增加字典开关，也不做缓存回退。`queryCollectionData` 遇到 `KENGK` 时直接调用 `queryKengk()`，从新台账按“发布日期倒序、同日发布时间倒序”取最新有效主记录，再按 `sort_no` 返回其明细。新台账没有有效记录时返回空列表。

**全组任务表带来的冲突面分析（任务表由用户 2026-07-10 提供，存于 `01台账需求追问回答.md`）：**

| 组内编号 | 任务 | 和本需求的关系 |
|---|---|---|
| 台账1 集采日金额统计 | 已完成，正是本需求的代码样板 | 无冲突 |
| 台账2 集采日计划统计 | 要更新"驾驶舱-集采计划分析"，含云采定时同步 | ⚠️ 大概率也要改 `SourceCollectionDataServiceImpl` 或同批驾驶舱代码；菜单同在"集采业务管理台账"下 |
| 台账3 | **就是本需求**（注意：需求文件夹叫"台账需求2"，组内编号是台账3，别混淆） | — |
| 台账4 价格趋势维护 | 要更新"驾驶舱-煤炭/钢材近12个月价格趋势"，前端已在调 `cockpit_collection_data_mt`（`index.vue:509-512`，钢材图同页面） | ⚠️ 同一个控制器/服务，撞车风险同上 |

结论：驾驶舱共享文件近期可能有 3 个需求先后动它。本需求只增加 `kengk` 实时查询分支，并把共享改动单独提交；合入 test 前必须以远端最新文件模拟合并，发现同事并行改动就只解决这一处冲突。

## 7. 你需要知道的背景知识

- **驾驶舱**：领导看的大屏。现在大屏已经有插座（GET 接口），本次只需要把插座背后的电线从“手填的 5 个旧数”接到“新台账最新记录”。
- **缓存表**：像抄在白板上的副本。当前 `sc_collection_data` 就是白板，只有名称和价格，不知道这些数是哪天、谁发布的。
- **逻辑删除**：像把纸质账页盖“作废”章，页面不再显示，但记录仍在库里，方便查错和回退。
- **导出模板**：平台里配置“Excel 第几列对应哪个后端字段”的清单；只有 Java 接口还不够，test 和正式环境都要配置同一份模板。

## 8. 整件事的推进顺序

1. 从 `origin/prod` 新建长期分支 `feature/taizhang-yang`；先做数据库脚本和导出配置清单，再照样板开发。
2. 对唯一的共享改动（`SourceCollectionDataServiceImpl` 的 `KENGK` 分支）单独提交，方便回退。
3. 隔离 javac 验证全部新 Java 文件；推 feature，再按公司流程合进 test。
4. test 执行 DDL、导出配置和单独存档的演示数据，反查确认。
5. 用户一次性做：找张雨配菜单/岗位、点 Jenkins、必要时刷新 TOKEN。
6. AI 用接口按“新增→列表→详情→修改→驾驶舱→删除→驾驶舱回退→导出”全链路验证。

---

# 第二部分：给执行开发 AI 的开发计划

## 0. 总体纪律

1. 开发范围仅限煤炭重点坑口日指标台账和现有驾驶舱 `KENGK` 读取分支；其他驾驶舱类型禁止修改。
2. 新需求按公司规则从 `origin/prod` 建 feature 分支。当前工作树虽在 `test`，不能直接在 `test` 写代码。
3. 除 `SourceCollectionDataServiceImpl` 的专属 `KENGK` 分支外，优先全部新增文件。共享改动必须单独提交，并以问题 5 的书面答复为前置条件。
4. 不把组织、价格、操作人等业务值硬编码；当前用户和当前时间都从后端上下文获取。
5. 数据库、导出模板在 test 做过的每一步必须存档到本需求 `sql/` 和配置核查表，正式发版交负责人重放。

## 1. 改动落点

建议类名先按以下占位；拿到问题答案后允许统一调整命名，但不能改变职责：

| 模块 | 建议新增/修改 | 职责 |
|---|---|---|
| `scm-source-source-api` | `api/dto/CoalPitDailyIndicatorDTO.java` | 页面查询、录入、修改、导出的统一字段 |
| `scm-source-source` | `model/CoalPitDailyIndicator.java` + `model/CoalPitDailyIndicatorItem.java` | 主表/明细表实体 |
| 同上 | `repositories/CoalPitDailyIndicatorMapper.java` + `CoalPitDailyIndicatorItemMapper.java` | CRUD、明细批量读写、最新有效记录查询 |
| 同上 | `service/CoalPitDailyIndicatorService.java` | 业务接口 |
| 同上 | `service/impl/CoalPitDailyIndicatorServiceImpl.java` | 分页、发布、修改、删除、驾驶舱转换 |
| 同上 | `controller/CoalPitDailyIndicatorController.java` | 5 个台账接口 |
| 同上 | `seq/CoalPitDailyIndicatorCodeSequence.java` | `JCKK-yyyyMMdd-NNN` |
| 同上（修改） | `service/impl/SourceCollectionDataServiceImpl.java` | `typeCode=kengk` 时直接实时查询新台账；无记录返回空列表 |

## 2. 数据库设计（2026-07-10 用户拍板：主表 + 明细表，数量可扩展）

主表 `sc_coal_pit_daily_indicator`（一条台账一行）：

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `indicator_id` | `varchar(32)` | 主键，`ASSIGN_ID` |
| `indicator_no` | `varchar(32)` | `JCKK-yyyyMMdd-NNN`，唯一索引 |
| `execute_unit_id/code/name` | `varchar` | 组织控件的 ID、编码、名称 |
| ~~`price_date`~~ | — | 不需要（问题 1 已解决：发布日期即日期轴） |
| `publish_date` | `date` | 表单日期控件传入，默认当天；补录历史价格时用户可改；驾驶舱按它排序 |
| `publish_time/by/by_name` | `datetime/varchar` | 最新发布/修改时间与操作人 |
| `create_* / update_*` | 项目同款字段 | 审计信息 |
| `delete_sign` | `int default 0` | 逻辑删除 |

明细表 `sc_coal_pit_daily_indicator_item`（一个坑口一行，数量不设结构上限）：

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `item_id` | `varchar(32)` | 主键，`ASSIGN_ID` |
| `indicator_id` | `varchar(32)` | 关联主表，普通索引 |
| `pit_name` | `varchar(255)` | 坑口/港口名称 |
| `tax_price` | `decimal(20,2)` | 今日含税价格，单位元/吨 |
| `sort_no` | `int` | 显示顺序（对应页面坑口 1~5 的位置） |
| `create_* / update_* / delete_sign` | 项目同款字段 | 与主表口径一致 |

索引暂定：主表唯一 `uk_indicator_no`，普通 `idx_publish_date`、`idx_publish_time`、`idx_execute_unit_code`；明细表 `idx_indicator_id`。建表前必须再对照 test 的 `sc_collection_daily_amount_ledger` 实际 DDL；2026-07-10 已通过 `SHOW CREATE TABLE` 确认其主键、审计字段、字符集和索引风格。

页面/接口兼容说明：列表和导出仍按产品原型输出"坑口1名称~坑口5价格"平铺字段——DTO 保留 `pit1Name...pit5TaxPrice`，由明细行按 `sort_no` 组装；以后超过 5 组时只改 DTO/页面/导出配置，**不动表结构、不搬数据**。录入校验上限当前按 5 组（与页面一致，只是一处校验数字）。

## 3. 暂定接口设计

| 功能 | 方法 + 路径 | 核心规则 |
|---|---|---|
| 分页/导出 | `POST /e/business/source/coalPitDailyIndicator/queryPageList` | 流水号模糊、单位编码精确、发布时间区间；默认按发布时间倒序 |
| 新增并发布 | `POST /e/business/source/coalPitDailyIndicator/add` | 后端生成流水、发布日期/发布时间/操作人；返回流水号 |
| 修改并重新发布 | `POST /e/business/source/coalPitDailyIndicator/modify` | 必传 ID；刷新发布时间和操作人；禁止改流水号/创建信息/删除标记 |
| 删除 | `POST /e/business/source/coalPitDailyIndicator/delete` | 逻辑删除；删除当前展示项时按确认规则回退 |
| 详情 | `POST /e/business/source/coalPitDailyIndicator/detail` | 修改弹窗回显，和新增共用字段 |
| 驾驶舱 | 保持 `GET /e/business/source/source/cockpit_collection_data_kengk` | 返回 `List<SourceCollectionData>`；`dataDesc=名称`、`dataValue=价格` |

组织控件不新增专用后端接口：前端复用平台已有控件，只把选中的 `executeUnitId/executeUnitCode/executeUnitName` 传给台账接口。上一版台账曾新增又删除专用组织查询接口；`test` 最近合入的相关 feature 提交 `435d0f6b5` 正是“删除台账专用组织查询接口”，本次不要重复走弯路。

## 4. 特殊逻辑

1. **流水号**：照 `CollectionDailyLedgerCodeSequence` 写 `JCKK` 版本；日期取服务器提交当天，序号每天从 `001` 重置，沿用项目 GID 服务生成，并用数据库唯一索引兜底防重。
2. **直接发布**：不增加草稿/发布状态；insert 成功即成为候选最新记录。
3. **修改防越权**：复制 DTO 后必须把流水号、创建信息、删除标记和前端传入的发布人/发布时间置空，再由服务器重填发布信息。
4. **价格校验**：非空价格必须大于等于 0、最多两位小数；名称和价格必须成对出现；至少有 1 组有效明细。
5. **驾驶舱转换**：`typeCode=kengk` 直接按“发布日期最新优先、同日发布时间最后”取一条有效主记录，把它的有效明细按 `sort_no` 转成 `SourceCollectionData`；不查询 `sc_collection_data`，没有有效台账时返回空列表。
6. **删除影响**：逻辑删除当前记录后自然回退到下一条有效记录；全部删光后驾驶舱返回空列表。
7. **主子表读写**：新增/修改在同一事务里写主表和明细；修改采用"先逻辑删除旧明细、再整批插入新明细"的整替换方式，避免逐行 diff。列表/详情/导出按 `sort_no` 把明细组装回 DTO 的坑口 1~5 平铺字段。开发前先在仓里找一个现成的主子表功能照抄写法（如导出模板 `scm_simple_template`/`_sub` 的维护代码），找不到再按 MyBatis-Plus 常规事务写法实现。

## 5. 导出和配置交付

建议导出模板编码 `coal-pit-daily-export`（长度 21，满足 `scm_simple_template.template_code varchar(32)`）。模板字段默认如下：

`indicatorNo`、`executeUnitName`、坑口 1~5 的名称和含税价格、`publishTime`、`publishByName`。不导出页面序号和“修改/删除”操作列。

2026-07-10 已核实 test 的 `scm_simple_template` 主表和 `scm_simple_template_sub` 明细表；现有 `collection-daily-ledger-export` 模板处于启用状态并配置 9 列，可直接作为配置样板。开发阶段需产出：

- `sql/01-create-coal-pit-daily-indicator.sql`
- `sql/02-export-template-reference.sql`（只作负责人重放参考，不含任何凭据）
- `sql/03-test-demo-kengk-data.sql`（只在 test 执行：把旧 5 条价格组成一条演示台账；文件明确标注禁止生产执行）
- 配置核查表：test 页面配置后再从上述两张表反查字段、排序、状态。
- 菜单和岗位配置记录：由张雨执行，开发 AI 只记录菜单名、路径、岗位和验证账号，不擅自改 UBM 权限库。

## 6. 自测清单

1. 新增返回 `JCKK-当天-001`；同日第二条为 `002`。
2. 未传/伪造发布时间、操作人时，库里仍以服务器当前登录人和当前时间为准。
3. 流水号、单位、发布时间区间都能正确筛选；分页和导出结果口径一致。
4. 只填 1~3 个坑口也能保存，列表空位不报错；名称无价格或价格无名称时明确报错。
5. 修改后业务字段、发布时间、操作人变化，流水号和创建信息不变。
6. 删除后列表、详情、导出均不可见，数据库记录仍保留且 `delete_sign=1`。
7. 驾驶舱原 GET 地址返回最新有效记录的非空坑口，字段仍为 `dataDesc/dataValue`。
8. 删除当前驾驶舱记录后回退到上一条有效台账；删到没有有效台账时返回空列表，确认不会再读旧缓存。
9. 新增 Java 文件和共享服务修改必须按工作规范走隔离 javac；不以全量 Maven 漂移报错代替自身验证。

## 7. 自行拍板记录

| 决定 | 理由 | 猜错的返工代价 |
|---|---|---|
| 删除采用逻辑删除 | 公司默认做法，可审计、可回退 | 低；改删除调用和查询条件 |
| 价格用 `decimal(20,2)`，不在库里存“元/吨”文字 | 金额/单价两位小数是项目惯例；单位现前端固定显示 | 低；调整字段精度或返回单位 |
| 组织保存 ID/编码/名称，查询用编码、显示用名称 | 组织名称会变，编码适合查询，名称适合历史展示 | 低；增删 DTO/表字段 |
| 发布时间查询采用起止区间 | 单个时刻几乎无法命中，页面日期控件通常按范围查 | 低；调整两个查询字段 |
| 导出列等于业务列表列，排除序号和操作列 | 序号/按钮不是业务数据 | 低；只改导出模板 |
| 至少填 1 组坑口，名称和价格成对出现 | 空记录没有业务意义，半组数据无法展示 | 低；只改校验条件 |
| 不开发 24 点定时任务 | 原文要求“人在 24 点前完成”，没有系统到点动作 | 中；若产品改口才新增调度任务 |
| 修改弹窗样式不影响后端接口 | 新增和修改可共用同一 DTO/详情接口 | 低；前端布局变化无需改库 |
| 不加独立价格日期字段（2026-07-10 cc） | 用户澄清：发布日期本就是表单日期控件、默认当天、可修改，足够承担"价格属于哪天" | 低；真要加就是加一列 |
| 驾驶舱取数规则按推荐（2026-07-10 用户拍板） | 发布日期新→同日发布时间新→删除回退上一条 | 低；改一处查询排序 |
| 坑口 1~5 均可修改（2026-07-10 cc） | 原文修改说明只写到坑口 4，对照列表/录入原型都有 5 组，判定为笔误 | 极低；改字段清单 |
| 坑口名称自由输入（2026-07-10 cc） | 无字典来源，截图名称还带热值和地区 | 低；后补下拉不影响表结构 |
| 旧 5 条作为 test 演示数据迁移（2026-07-10 最终答复） | 方便部署后直接测试；缺失字段用明确标注的测试值，演示 SQL 与生产包隔离 | 低；删除演示主子记录即可 |
| kengk 直接实时查询且不读缓存（2026-07-10 后端同事确认） | 避免两份数据不一致，答复明确无需字典开关和回退 | 低；共享改动单独提交可回退 |
| 表结构用主表+明细表（2026-07-10 用户拍板） | 数量可扩展，以后超 5 个不用改表、不搬数据；代价是查询组装和事务稍复杂、与台账1 单表样板不同 | 方向性决定；页面/导出仍按 5 组输出，对外无感知 |
| 明细录入校验上限暂按 5 组（2026-07-10 cc） | 与当前页面一致；放开只改一处校验数字 | 极低 |

## 8. 待确认参数占位

- 是否新增 `priceDate`：✅ 不新增（2026-07-10 用户澄清：发布日期即表单日期控件、默认当天、可修改，即唯一日期轴）
- 驾驶舱最新记录排序和删除回退：✅ 发布日期最新优先，同日发布时间最后一条；删除回退上一条有效记录（用户拍板）
- 最大坑口数、坑口 5 修改权、名称录入方式：✅ 主表+明细表，数量可扩展（2026-07-10 用户拍板）；坑口 1~5 可改、名称自由填已拍板；录入校验上限暂按 5 组
- 旧缓存切换/历史迁移策略：✅ 不再读取旧缓存；旧 5 条仅以 test 演示数据形式写入新台账，禁止带入生产
- 后端同意的 `KENGK` 数据源方案：✅ 直接实时查询新台账，共享代码可以修改
