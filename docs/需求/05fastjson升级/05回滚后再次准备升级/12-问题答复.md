# 问题答复（用户 2026-08-10 带回）

> 对应 `11-待确认问题-cc.md`。方案已按这些答复调整，见 `10-拆解规划-cc.md` 与 `20-升级实施手册-cc.md`。

| 编号 | 问题 | 答复 | 对方案的影响 |
|---|---|---|---|
| Q1 | 先上 1.2.84 止血还是直接冲 2.x | ✅ **直接升级到 2.x 最新版（2.0.64）** | 取消阶段 0，1.2.84 只作为**回滚落点**保留在预案里 |
| Q2 | 升级期间生产漏洞裸着可接受吗 | ✅ **裸着没问题** | 不需要网络层临时防护，不设时间压力 |
| Q3 | SafeMode 要不要长期保留 | ✅ **关了**（不配） | 不加 `JAVA_TOOL_OPTIONS`，Dockerfile 保持现状不动 |
| Q4 | 生产发布入口 | ✅ **生产由别人负责部署，我们不管生产端** | 我们的交付边界 = **test（+UAT）**；生产只产出一份交接清单给对方 |
| Q5 | 分批部署需要发布窗口配合 | 未答 | 仍按分波执行；发 Nexus 前用户在群里知会一声即可 |
| Q6 | scm-cloud-report 流水线怎么配 | 未答 | 部署到这个服务时现场确认，不阻塞 |
| Q7 | scm-ifs-all 未提交的本地改动怎么办 | ✅ **舍弃本地修改，重新做** | `git checkout .` + 删除未跟踪的 `util/` 目录；B3 改用**公共层方案**（见下），不再走 IFS 本地那套 |
| Q8 | 规则引擎注入点不止 IFS，要不要一起修 | 未答 | 已改为**公共层一处收口**方案，业务模块零改动，不再需要逐处确认 |
| Q9 | 产品的测试清单 | ❌ 仍未拿到 | 阻塞"验收清单"这一项，不阻塞开发。放到 `00-需求原件/` |
| Q10 | 供应商测试账号 | 未答 | 影响 source 最关键验收点，波次 4 之前要拿到 |

## 由此产生的方案调整

1. **删除阶段 0（1.2.84 止血）** —— 直接 `1.2.83 → 2.0.64`。
2. **不配 SafeMode** —— 不动任何 Dockerfile / deploy yaml。
3. **交付边界收缩到 test / UAT** —— 生产部分只写交接清单，不执行。
4. **B3（规则脚本）改用公共层收口方案** —— 用户要求"公共依赖做全面，别影响子模块"，我重新查证后找到了唯一收口点，可在 `scm-share-rule` **一个方法里**覆盖全部 22 个规则调用点，业务模块一行不改。比原来"逐个注入点替换"的方案更干净。
5. **舍弃 scm-ifs-all 的本地未提交改动**。
