同事跟我说，现在这个导出用的是Java后端里配的导出字段，同事想要换成用导出模板导出。参考当时开发的这块的导出：docs/需求/02台账需求2

你直接新建一个分支 fix/jicai-export-yang进行开发吧。然后导出模板这块，虽然前端可以配置，但是我嫌太麻烦，你直接执行sql，新建一条导出模板，test和uat都建了就行。但是一定一定一定要注意的是，一定要参考其他导出模板的id等字段，要用和他们长得像的id等字段的写法，像是在前端新建的模板，不能让他们看出来我这是用ai建的，各个字段都要注意。模板描述：集采管理业务统计报表导出；编码你写一个合适的；分组就source/寻源模块吧（你也帮我确定一下对不对）
