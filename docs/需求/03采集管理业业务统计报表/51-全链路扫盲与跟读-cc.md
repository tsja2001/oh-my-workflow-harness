# 集采管理业务统计报表 · 全链路扫盲 + 跟读手册

> 日期：2026-07-23 ｜ 工具：cc
> 面向：Node/Koa 全栈背景、第一次接触 Java 企业微服务的接手人
> 读完的验收标准：能对同事讲清楚「数据怎么从 MySQL 走到页面上」，能说出每一段代码在哪个仓库哪个文件，
> 能独立回答「为什么 SQL 要手动执行」「为什么要 ES 不直接查 MySQL」这类问题。
>
> **本文是自包含的，从头读到尾即可，不需要跳去别的文档。**
> 覆盖：全景架构 → 非代码技术扫盲 → 写入侧跟读 → 查询侧跟读 → 前端跟读 → 自测。
>
> 另外两份 50 文档是更早阶段的产物，**可以不读**；想交叉印证时再看：
> - `50-代码复盘与学习-codex.md`：招采平台写入侧跟读（写于 ZC 完成时，当时 `ServiceImpl` 还没有 MT/JD，行号已过期）
> - `50-代码复盘与学习-cc.md`：非平台录入写入侧的文件职责入门
>
> 编号说明：本文是规范外的补充教学文档，按 AGENTS.md 应走 90 段；但它是 50「理解交接」的延续，
> 故取 51，与两份 50 并列。

---

# 第一部分：先回答你的问题

## 1.1 你的理解对不对？

你说的是：

> 先在 source 里取数据（同事写了从 order 里取数据），然后存到 es 里，然后再 report 里查数据

**主干完全正确。** 这就是这套东西的骨架，你抓住了最重要的东西。

要补三个细节，补完你的图就完整了：

**补充一：不是"两个服务"，是"五个搬运工"**

不是 source 和 order 两家。是**五个数据来源，各写各的搬运代码**，但都往同一个 ES 索引里写：

| 来源 | 编码 | 谁写的 | 在哪个服务 | 从哪张表捞 |
|---|---|---|---|---|
| 非平台录入 | FP | 你（AI 代写） | `scm-source-all` | `sc_supplier_green_channel` + 物料明细表 |
| 招采平台 | ZC | 你 | `scm-source-all` | `sc_source` → `sc_deal_*` 四张表 |
| 煤炭SAP | MT | 你 | `scm-source-all` | `sc_source_collection`（`data_source='SAP'`）|
| 机电四大类 | JD | 你 | `scm-source-all` | `sc_collection_daily_amount_ledger` |
| 云采平台 | YC | 同事 | `scm-order-all` | 订单相关表 |

你负责 4 个，同事负责 1 个。你们在**两个不同的 Git 仓库**里各写各的，互不影响，唯一的约定是「往同一个索引写、字段名和类型必须一模一样」。

**补充二：中间那步不只是"存"，是"摊平 + 翻译"**

数据从 MySQL 到 ES 不是原样搬。做了两件事：

1. **摊平**：MySQL 里是一层套一层的（一个采购方案 → 几家中标供应商 → 每家几个物料），ES 里是**一条物料一行**，全部拍平成一张大表。
2. **翻译**：把编码翻成人看得懂的名字（`BU002` → `水泥集团`），把 `1/0` 翻成业务含义（`is_jc=1` → `集团集采`）。

这是整个设计的核心。后面 3.5 节会讲为什么必须这么做。

**补充三：还有第四段，前端**

完整是四段：

```
MySQL ──①写入──→ ES ──②查询──→ report 服务 ──③HTTP──→ Vue 页面
        (source)         (report)                    (前端仓库)
```

你参与了①②③全部。

## 1.2 那个"笑话"其实不是笑话

你说你问同事「SQL 要手动执行吗，不会自动建表吗」，觉得丢人。

**这个问题问得完全合理**，而且你恰好踩在一个真实存在的不一致上：

| | 要不要手动 | 为什么 |
|---|---|---|
| **MySQL 建表** | **要手动执行 SQL** | 这个项目没装自动迁移工具 |
| **ES 建索引** | **不用，自动的** | 代码里 `@Document` 注解一写，服务启动时框架自动去 ES 建好 |

**同一个需求里，一半自动一半手动**。你会困惑太正常了。

再说 Node 那边：Prisma / Sequelize / TypeORM 都自带 migration，`npx prisma migrate deploy` 一条命令，表就建好了。你的直觉「不是应该自动吗」来自那个世界，没错。

Java 也有对应的东西（Flyway、Liquibase），**只是这个项目没用**。为什么不用，两个现实原因：

1. **生产库权限是分开的**。开发碰不到 prod 库，只有 DBA/运维能执行。自动迁移意味着"应用一启动就改生产库结构"，很多公司不接受这个风险。
2. **团队习惯**。发版时 SQL 是一个单独的交付物，交给负责人统一执行、统一回滚。

所以这个项目的规矩是：**代码你提交，SQL 你写好存档，发版时人工执行。** AGENTS.md 里"发版纪律"那条说的就是这个 —— 每条执行过的 SQL 都要存进需求文件夹的 `sql/`，因为上生产时要交给别人重放一遍。

> 记住这句话，以后不会再问错：
> **这个项目里，数据库结构靠人手动执行 SQL；ES 索引结构靠代码自动创建。**

---

# 第二部分：业务扫盲 —— 这报表到底在解决什么问题

## 2.1 一句话

**领导想知道：全集团这段时间，从哪些供应商、买了什么东西、花了多少钱。**

问题在于这些钱花在五个完全不同的系统里：

- 走招标流程的（招采平台）
- 线下谈完了补录进系统的（非平台录入）
- 电商化直接下单的（云采平台）
- 从 SAP 拉过来的煤炭采购（煤炭SAP）
- 机电公司每天报的汇总台账（机电四大类）

五个系统，五套表结构，五种叫法。领导要的是**一张表**。

这就是这个需求的全部意义：**把五种形状的数据，捏成同一个形状，放进同一个筐，让人能一起查、一起导出。**

## 2.2 "一行"到底代表什么（最容易搞错的地方）

报表的一行**不是**一笔采购业务，而是 **"某一笔业务里，某个供应商中标的某一个物料"**。

举个真实例子（test 库里的真数据）：

```
采购方案 12870012-XJ-2026-0001（一次询价采购）
  └── 中标物料"倒焰窑85矾土块料"，3 家供应商分着中标：
        ├── 江苏鱼跃医疗设备股份有限公司   300.00 元
        ├── 中国工艺集团有限公司           100.00 元
        └── 唐山明诚矿业有限公司           670.90 元
```

**这一笔业务，在报表里占 3 行。** 三行的方案编号一样、物料一样，只有供应商和金额不同。

为什么这么设计？因为报表要按供应商统计金额。合并成一行的话，另外两家供应商的钱就没地方放了。

**这个认知直接决定了主键怎么设计**，也是我这次开发发现的两个大坑的根源（后面 4.4 节）。

## 2.3 五个来源的"形状差异"有多大

这是为什么这个需求"看起来简单做起来复杂"的真正原因：

| | 招采平台 ZC | 非平台 FP | 煤炭SAP MT | 机电 JD |
|---|---|---|---|---|
| 一条原始数据是 | 一个采购方案 | 一个补录业务 | **一天的一条收发流水** | 一天的一条汇总 |
| 要展开几层才拿到物料 | **4 层** | 2 层 | 0 层（本身就是物料级）| 0 层（**根本没有物料**）|
| 有没有业务名称 | 有 | 有 | **没有，得拼** | **没有，得拼** |
| 有没有板块名称 | 有（但是脏的）| 有（但是脏的）| **全是 NULL** | 有 |
| 时间字段叫什么 | `final_time` 公示时间 | `update_time` | `final_time` | `statistic_date` 统计日期 |
| 是否集采从哪来 | 查 `sc_scheme` 表 | 主表 `is_jc` 字段 | **固定"是"** | **固定"是"** |

五个来源，五种取法。**代码里那五个 `syncXxData` 方法，差异全在这张表上。**

---

# 第三部分：技术扫盲 —— 用你熟悉的 Node 世界做锚点

这一部分是你说的"非代码类技术知识"。每个概念我都给一个 Node 类比，**并说明类比在哪里会失效**（类比用错比不用更危险）。

## 3.1 微服务：为什么有几十个仓库

你的 Koa 项目大概是一个 repo、一个进程、一个 `npm start`。

这套系统是**几十个独立的服务**，每个是一个独立的 Java 进程，跑在自己的容器里：

```
scm-source-all     采购寻源服务   ← 你主要在这里干活
scm-report-all     报表查询服务   ← 你也改了这里
scm-order-all      订单服务      ← 同事的地盘
scm-ubm-all        用户/组织/字典服务
scm-gateway-all    网关
... 还有 20 多个
```

**Node 类比**：像你把一个 Koa 大项目拆成 20 个小 Koa 服务，各自 `npm start`，互相通过 HTTP 调用。

**类比边界**：Node 微服务通常各自有各自的数据库。这套系统里**多个服务共用同一个 MySQL 库**（`scm_source_test`）。所以你会看到 `scm-report-all` 也能读到 source 的表 —— 这在纯粹的微服务架构里算"不规范"，但企业系统里非常常见。

## 3.2 一个仓库里的几十个"模块"

打开 `scm-source-all`，你会看到 47 个子目录：`scm-source-bid`、`scm-source-deal`、`scm-source-chase`……

**这些不是 47 个服务**，它们最终打包成**一个** Java 进程。

**Node 类比**：像 monorepo 里的 workspace packages。`packages/user`、`packages/order`……最后一起构建成一个 app。

它们的关系在 `pom.xml` 里声明（相当于 `package.json` 的 `dependencies`）：

```
scm-source-chase/pom.xml 里写了依赖 scm-source-source、scm-source-deal、scm-source-scheme
→ 所以 chase 模块的代码可以 import 那三个模块的类
→ 这就是为什么这次我不用改 pom：要用的 Mapper 早就在依赖里了
```

**为什么你的代码写在 `scm-source-chase`**：chase = "追数据"，这个模块专门放"从业务表捞数据同步到 ES"的逻辑。同目录下 `TradeDataReportServiceImpl` 就是同事以前写的同类东西。

## 3.3 Maven ≈ npm，但有个大坑

| npm | Maven |
|---|---|
| `package.json` | `pom.xml` |
| `node_modules/` | `~/.m2/repository/`（**全机器共用一份**，不是每个项目一份）|
| `npm install` | `mvn install` |
| npm registry | 公司私服 Nexus |
| `1.0.0-beta.3` | `1.0.0-JDSN-SNAPSHOT` |

**SNAPSHOT 是那个大坑。** 它相当于 npm 的 `latest` tag —— 版本号不变，内容随时会变。

于是就出现了工作规范里那条：**"同事从不本地编译"**。

原因：你本地 `~/.m2` 里的 `scm-source-source-1.0.0-SNAPSHOT.jar` 可能是三个月前下载的，而别人的源码早就改了。你本地全量编译，会在**别人的文件**里报一堆错 —— 不是你的问题，是你手里的 jar 过期了。

**所以我们验证代码的方式是"隔离编译"**：不管整个项目编不编得过，只把**我们改的那几个文件**拿出来，对着真实依赖单独编一遍。零报错 = 我们的代码是健全的，剩下的交给 Jenkins（它每次都拉最新的构建）。

这次三期开发，每次收工我跑的就是这个，退出码 0。

## 3.4 Spring 注解：那些 `@` 符号是干嘛的

Koa 里你要手动组装：

```js
const service = new UserService(db)
router.post('/users', ctx => service.create(ctx.request.body))
```

Spring 里你不组装，**你贴标签，框架自己组装**：

| 注解 | 意思 | Koa 对应 |
|---|---|---|
| `@RestController` | 这个类是对外的接口入口 | `router` |
| `@PostMapping("e/business/...")` | 这个方法处理这个 POST 路径 | `router.post('/path', fn)` |
| `@Service` | 这个类是干活的，**请框架帮我造一个实例** | `new XxxService()` |
| `@Autowired` | **请框架把那个实例塞给我** | 手动 `require` + 传参 |
| `@Data`（Lombok） | 自动生成 getter/setter | JS 对象天然就有属性 |

**核心区别叫"依赖注入"**：你从来不写 `new`，框架启动时扫描所有 `@Service`，造好实例，然后按类型塞进所有 `@Autowired` 的字段里。

**在哪扫描的？** [SourceWebConfig.java:16](01zhaocai-end/scm-source-all/scm-source-web-jar/src/main/java/com/pcitc/scm/source/webconfig/SourceWebConfig.java:16)：

```java
@ComponentScan(basePackages = {
    "com.pcitc.scm.source.*.controller",
    "com.pcitc.scm.source.*.service",   // ← 你的 ServiceImpl 因为这行被扫到
    ...
})
@MapperScan(basePackages = {"com.pcitc.scm.source.*.repositories"})  // ← Mapper 因为这行
```

**这解释了一个你可能困惑的事**：为什么我写完 `@Service` 就能直接用，不用在任何地方"注册"一下？因为包路径匹配上了，框架自动扫到。反过来，**如果你把类放错包**（比如放到 `com.pcitc.scm.source.chase.util`），它就不会被扫描，运行时报"找不到 bean"。

## 3.5 MySQL vs ES：为什么要两套，不能直接查库？

这是整个架构里最值得理解的一个决策。

**MySQL** = 你熟悉的关系型数据库，业务系统的"真相源头"。所有增删改都在这。

**Elasticsearch** = 搜索引擎。可以粗暴理解成"一个为查询优化过的、极其快的大 JSON 集合"。

**为什么不直接查 MySQL 出报表？** 三个理由，从弱到强：

1. **要跨 5 个系统**。五个来源的表结构完全不同，直接 join 出一张统一报表，SQL 会写成噩梦，而且云采的数据根本在另一个服务的表里。
2. **要展开成一行一物料**。招采平台要 4 层展开（方案→定标方案→供应商→物料），每次查报表都实时 join 四张表 + 翻字典，慢且重。
3. **报表查询会拖垮业务库**。领导拉一次全年报表，扫几十万行，正在下单的人跟着卡。

所以做法是：**把"算好、拍平、翻译好"的结果预先存进 ES，报表只读 ES，完全不碰业务库。**

**Node 类比**：像你在 Koa 里为了首页性能，把几张表 join 好的结果预先算进 Redis，前端只读 Redis。

**类比边界**：Redis 是纯 KV，ES 能全文搜索、能分页排序、能聚合统计 —— 它更像"一个只读的、超快的、能模糊搜中文的数据库"。

**代价**（这个必须知道）：ES 里的数据是**快照，不是实时的**。MySQL 改了，ES 不会自己变，要等下次同步任务跑。所以报表数据永远比业务系统"晚一点"。这是这类设计的固有取舍，不是 bug。

## 3.6 ES 里的东西长什么样

不用背术语，看一条真实的（我从 test 上查出来的）：

```json
{
  "_index": "index_centralized_procurement_report_test",   // 索引名 ≈ 表名
  "_id": "10930000-XJ-FPT-2026-00285467890",               // 主键
  "_source": {                                             // 真正的内容
    "buName": "水泥集团",
    "purchaseCompanyName": "唐山市盾石信息技术有限公司",
    "supplierName": "北京金隅数智科技有限公司",
    "planCode": "10930000-XJ-FPT-2026-0028",
    "materialName": "电脑",
    "taxTotal": 339000.0,
    "bookTime": "2026-07-10T01:20:10.000+00:00",
    "dataSource": "FP"
  }
}
```

对照记忆：

| ES | MySQL |
|---|---|
| index 索引 | table 表 |
| document 文档 | row 行 |
| field 字段 | column 列 |
| `_id` | 主键 |
| mapping | 表结构 |

**`_id` 是理解这次两个大坑的钥匙**：ES 写入时，**`_id` 相同就覆盖，不同就新增**。没有"重复主键报错"这回事，它默默覆盖。

所以主键设计错了 → 数据被悄悄吃掉 → **不报错，你还以为成功了**。这两个坑后面 4.4 讲。

## 3.7 网关：为什么接口路径长成那样

前端不直接连 20 个服务，只连一个**网关**（`10.0.54.16:32740`）。网关看路径，转发给对应的服务。

```
浏览器 → http://10.0.54.16:32740/gateway/e/business/source/centralizedProcurementReport/syncZc
                                          └─ 网关看这段，转发给 scm-source-web 服务
```

**Node 类比**：nginx 反向代理，或者 API Gateway。

**这里有个已经踩过的坑**（写在工作规范 §3）：网关**不剥前缀**。Controller 里必须把完整路径写全：

```java
@PostMapping("e/business/source/centralizedProcurementReport/syncZc")   // ✅ 网关能路由到
@PostMapping("/api/trade-data-report/sync")                             // ❌ 路由不到，404
```

`scm-source-chase` 里那个老的 `TradeDataReportController` 用的就是 `/api/...`，**那是同事留的本地调试入口，网关根本走不到**。写新接口别照抄它。

**还有一个更阴的坑**：往网关发一个不存在的路径，可能返回

```json
{"status":true,"code":"000000","data":"ok"}
```

看着像成功，其实是网关的兜底响应。**判断接口通没通，要看返回内容是不是你自己接口该返回的东西**，不能只看 `status:true`。

## 3.8 Nacos：配置中心

代码里索引名不是写死的：

```java
@Document(indexName = "#{@sourceChaseConfig.getCentralizedProcurementReportIndex()}")
```

这个值从 **Nacos** 里读。Nacos 是个网页，运维在上面填配置，服务启动时去拉。

**Node 类比**：`.env` 文件 + dotenv，但是**放在服务器上集中管理的**，改完不用重新打包，服务能热更新。

**为什么要这样**：test / uat / prod 三套环境的索引名不一样（`..._test` / `..._uat` / `..._prod`）。写死的话每个环境得打包三次。

配置在哪：[bootstrap-test.yml:5-8](01zhaocai-end/scm-source-all/scm-cloud-source/src/main/resources/bootstrap-test.yml:5) 指明了 Nacos 地址和命名空间 `scm-jdsn-test`。

## 3.9 XXL-JOB：定时任务

**Node 类比**：`node-cron`，但是**独立的一个管理后台**，谁都能在网页上配、能看执行日志、能手动触发一次。

这个项目的调用方式有点绕，值得知道（不然你看不懂那串参数）：

XXL-JOB 后台配一个任务，handler 填 `simpleJobHandler`，参数填：

```
source-centralizedProcurementReportService-syncZcData-{}
```

这串东西被 [XxlJobExecutorServiceImpl.java:28](01zhaocai-end/scm-assemblies-all/scm-scheduling-all/xxl-job-executor/src/main/java/com/pcitc/scm/xxljob/executor/service/impl/XxlJobExecutorServiceImpl.java:28) 按 `-` 切成 4 段：

| 段 | 值 | 含义 |
|---|---|---|
| 1 | `source` | 发给哪个服务 |
| 2 | `centralizedProcurementReportService` | 哪个 Service（对应代码里 `@TypeMapping`）|
| 3 | `syncZcData` | 哪个方法（对应 `@MethodMapping`）|
| 4 | `{}` | 参数 JSON |

**第 1 段 `source` 是服务的外号**，定义在 [SourceAIMService.java](01zhaocai-end/scm-source-all/scm-source-api/src/main/java/com/pcitc/scm/source/api/SourceAIMService.java) 里：`String APP_ROOT = "source";`

**为什么参数只能传 `{}`**：按 `-` 切割，而日期 `2026-01-01` 里全是 `-`，传进去会被切碎。所以定时任务固定同步前一天，指定日期补跑只能走手动接口。

### 3.9.1 `@TypeMapping` / `@MethodMapping` 到底是什么（不只是给 XXL-JOB 用的）

> **更正**：本文早先把这两个注解说成"XXL-JOB 专用"，那是讲窄了。以下是读框架源码后的准确说法。

这两个注解属于公司自研的一套内部调用框架 **AIM**。本质是：
**给一个 Service 起一个"字符串外号"，让调用方能用字符串调它，而不必持有 Java 类型。**

```java
@TypeMapping("centralizedProcurementReportService")  // 贴在【接口】上 = 服务外号
@MethodMapping("syncZcData")                          // 贴在方法上   = 方法外号
```

**为什么需要"用字符串调用"** —— 因为有三类调用方拿不到 Java 类型：

| 调用方 | 为什么只能用字符串 |
|---|---|
| XXL-JOB | 配置是网页文本框，只能填字符串 |
| **跨模块调用** | 模块 A 想调模块 B 的方法，但不想在 pom 里直接依赖它 |
| **跨服务调用** | 完全不同的容器，Java 类型不在一个进程里（走 Feign）|

**本项目里的真实例子**（和定时任务毫无关系的纯内部调用），[DealExportServiceImpl.java:114](01zhaocai-end/scm-source-all/scm-source-deal/src/main/java/com/pcitc/scm/source/deal/service/impl/DealExportServiceImpl.java:114)：

```java
sourceAIMService.request(new AIMRequest<>(dealParam, "dealService", "getDealInfo"));
//                                                    ↑服务外号     ↑方法外号
```

`scm-source-all` 里有 20 多个 Service 用了这套机制。

**机制本身**（[MethodHandlerServiceImpl.java:36](01zhaocai-end/scm-assemblies-all/scm-share-aim-all/scm-share-aim-spring/src/main/java/com/pcitc/scm/share/aim/spring/impl/MethodHandlerServiceImpl.java:36)）：
服务启动时扫描所有带 `@TypeMapping` 的 bean，建一张 `外号 → 实际对象` 的表；调用时按外号查表。

```java
cs = object.getClass().getInterfaces();      // 拿到 bean 实现的所有接口
tm = c1.getAnnotation(TypeMapping.class);    // 从【接口】上读注解
```

⚠️ **注意最后一行**：注解是从**接口**上读的。贴在 Impl 类上**不生效**。
这也顺带解释了"为什么 Service 要拆成接口 + 实现两个文件"——**接口不是形式主义，它是这套机制的挂载点。**

### 3.9.2 能不能把这两个注解删掉？

**事实**：全代码库搜 `centralizedProcurementReportService` 这个字符串，除注解声明本身外没有任何调用方。
所以**目前只有 XXL-JOB 会用到它们**，删掉不影响编译，手动接口（走 Controller，与 AIM 无关）照常工作。

**但不该删**：产品要的是"每天自动同步昨天数据"。删掉注解 = 每天要人手动点接口。
**这两个注解不是技术负担，它们就是"每天自动跑"这个需求的实现方式。**
它们也不产生运行开销——只在服务启动时被扫描登记一次。

只有当产品明确说"不要定时任务"时，才连同 import 一起删干净。

## 3.10 Jenkins / Docker / K8s：代码怎么变成线上服务

你点一下 Jenkins 的"构建"，背后发生的事（从 [Jenkinsfile](01zhaocai-end/scm-source-all/scm-cloud-source/Jenkinsfile) 里读出来的）：

```
1. 拉代码        git clone，切到 test 分支
2. 编译          mvn clean install -U          ← 打成 jar 包
3. 镜像构建      docker build                  ← jar + JDK + 配置 → 一个"集装箱"
4. 镜像推送      docker push 到公司镜像仓库
5. 部署          kubectl apply                 ← K8s 拉新集装箱，替换旧的
```

**Node 类比**：
- **Docker 镜像** ≈ 把 `node_modules` + Node 运行时 + 你的代码，一起冻成一个文件，保证"在我机器上能跑"= "在服务器上能跑"
- **K8s** ≈ pm2 的集群版，管着几十个容器的启动、重启、扩容、滚动更新
- **Jenkins** ≈ GitHub Actions，只不过是公司自己搭的

**为什么必须是你点，不是我点**：Jenkins 要登录、有权限，而且部署是**对外有影响的动作**。这也是 AGENTS.md 里那条红线的由来。

### 3.10.1 Jenkins 和 K8s 都在"跑命令"，为什么要拆成两个？

这是最容易混淆的一对，值得讲透。

**一句话：Jenkins 是动词，K8s 是名词。**

打开 [Jenkinsfile](01zhaocai-end/scm-source-all/scm-cloud-source/Jenkinsfile)，每一行都在说"**做什么**"：拉代码 → `mvn clean install` → `docker build` → `docker push` → `kubectl apply`。

打开 [deploy-docker.yaml](01zhaocai-end/scm-source-all/scm-cloud-source/deploy-docker.yaml)，**一个动词都没有**，全是"**我要什么**"：

```yaml
replicas: 1              # 我要 1 个副本活着
restartPolicy: Always    # 死了永远给我重启
resources:
  limits:
    cpu: '2'             # 最多用 2 核
    memory: 5Gi          # 最多 5G 内存
minReadySeconds: 30      # 新容器活满 30 秒才算数
```

这不是命令，是**一张愿望单**。这个区别有个正式名字：**命令式 vs 声明式**。

**最关键的差别：Jenkins 会下班，K8s 不下班。**

Jenkins 跑完 5 步 → 打印 SUCCESS → **进程结束，从此不管**（和你敲 `npm run build` 一样）。
K8s 从收到愿望单那一刻起开始值班，**永不结束**：

| 凌晨 3 点发生 | Jenkins | K8s |
|---|---|---|
| 容器 OOM 崩了 | 早下班了，无事发生 | 30 秒内拉起新的 |
| 整台服务器宕机 | 无感知 | 把容器调度到别的机器 |
| 内存吃到 5G | 无感知 | 按 `limits` 掐掉重启 |
| 想扩到 3 个实例 | 得改脚本重新构建 | 改 `replicas: 3`，立刻生效 |

**交接的那一瞬间**在 [Jenkinsfile:75](01zhaocai-end/scm-source-all/scm-cloud-source/Jenkinsfile:75)：

```bash
envsubst < $PROJECT_DIR/deploy-docker.yaml | kubectl apply -f -
```

`kubectl apply` **不是"启动程序"**，是"提交一份期望状态"。提交完 Jenkins 就下班了，程序此时还没起来——是 K8s 收单后自己去拉镜像、自己启动、自己盯着。

> ⚠️ **由此得出一条实用结论：Jenkins 显示 SUCCESS ≠ 服务已经好了。**
> Jenkins 只保证"愿望单递交成功"。服务起没起来要去 KubeSphere 看。这个坑很多人踩过。

**为什么非拆不可**：

1. **一次构建，三处部署** —— 同一个镜像先部 test、验过再部 uat、再部 prod。绑在一起的话每个环境各构建一次，**uat 上跑的就不是 test 上验过的那个东西了**。这条最重要。
2. **重要性差一个量级** —— Jenkins 宕机 = 今天发不了版，线上照跑；K8s 宕机 = 事故。
3. **回滚** —— K8s 一条命令切回上个镜像，几十秒；绑一起就得切代码重新构建，慢且未必构建出一模一样的产物。
4. **谁半夜重启** —— 总得有个一直醒着的东西，Jenkins 的模型天生做不到。

**和 pm2 对比（说清边界）**：

| | pm2 | K8s |
|---|---|---|
| 崩了重启 | ✅ | ✅ |
| 跨多台机器调度 | ❌ 只管本机 | ✅ |
| 声明式 | ❌ 你敲 `pm2 start` | ✅ 你写"我要 1 个"，它自己达成 |
| 滚动更新 | 半支持 | ✅ 新的活满 30 秒才杀旧的 |
| 资源上限 | ❌ | ✅ `limits: 2核/5G` |

`minReadySeconds: 30` 是 pm2 给不了的：新容器健康存活 30 秒后才杀旧容器，这 30 秒内新的若崩了，旧的原封不动继续服务，用户无感。

**中间还夹着"镜像"这一层**（[Dockerfile](01zhaocai-end/scm-source-all/scm-cloud-source/Dockerfile)）：拿装好 JDK 8 的基础环境 + 编译出的 jar + 启动命令（`java -jar -Xms2g -Xmx4g ...`），冻成一个文件。它的意义是**切断"在我机器上能跑"这个借口**——K8s 不需要知道你是 Java 还是 Node，只管拉起集装箱。

```
Jenkins   造集装箱     （工厂，造完下班）
镜像仓库   存集装箱     （仓库）
K8s       运营集装箱   （永远在岗的场地管理员）
```

### 3.10.2 出问题该看哪个（最实用）

| 症状 | 看哪 |
|---|---|
| Jenkins 页面红了 | **Jenkins 构建日志** —— 编译错误、拉不到依赖 |
| Jenkins 绿了但接口 404 | **KubeSphere 容器日志** —— 服务启动失败（配置错、连不上库…）|
| 接口通了但数据不对 | **代码 / MySQL / ES** —— 和部署无关了 |
| 接口时好时坏 | **KubeSphere 看容器重启次数** —— 可能在反复崩溃重启 |

**分界线一句话：Jenkins 日志看到"镜像推送成功"为止，之后所有问题都在 KubeSphere 看。**

## 3.11 四套环境

| 环境 | 干什么 | 你的权限 |
|---|---|---|
| dev | 开发者本地/联调 | — |
| **test** | 测试环境，你主要在这验证 | 直连数据库、调接口 |
| uat | 用户验收，给业务方点的 | 部署时才碰 |
| prod | 生产，真实业务 | **不碰** |

每套环境有**自己的 MySQL、自己的 ES、自己的 Nacos**。所以索引名才有 `_test` 后缀。

**同事让你部署 uat 给你 SQL** —— 现在你知道为什么了：uat 的数据库是独立的一套，test 上建过的表，uat 上没有，得再执行一遍。

---

# 第四部分：跟着 IDEA 读代码

## 4.0 读之前：三个仓库先摆清楚

在 IDEA 里你会同时开好几个窗口，别搞混：

| 仓库 | 位置 | 装什么 | 本次分支 |
|---|---|---|---|
| `scm-source-all` | `01zhaocai-end/` | **写入侧**：从 MySQL 捞数据写进 ES | `feature/jicai-mtjd-yang` |
| `scm-report-all` | `01zhaocai-end/` | **查询侧**：从 ES 读，分页 + 导出 Excel | `feature/jicai-report-yang` |
| `scm-vue-all-procurementscheme` | `02zhaocai-front/` | **页面** | `features/jicai-report-yang` |

**IDEA 快捷键**（读代码全靠这三个，先记住）：

| 快捷键 | 作用 | 什么时候用 |
|---|---|---|
| `Ctrl+B` / `Ctrl+左键` | 跳到定义 | 看到一个方法名，想知道它干嘛 |
| `Alt+F7` | 查找谁用了它 | 看到一个方法，想知道谁在调 |
| `Ctrl+Shift+F` | 全局搜文本 | 只知道接口路径，想找代码 |
| `Ctrl+N` | 按类名找文件 | 知道文件名，想直接打开 |

**从接口路径找到代码的通用套路**（这个最实用）：拿到一个接口路径 `/e/business/source/centralizedProcurementReport/syncZc`，`Ctrl+Shift+F` 搜 `centralizedProcurementReport/syncZc`，直接定位到 Controller。

## 4.1 第一段：写入侧（source）

> 行号对应 2026-07-23 `feature/jicai-mtjd-yang` 分支的状态（`ServiceImpl` 共 947 行）。
> 如果对不上，用 `Ctrl+F` 搜方法名，别死磕行号。

### 4.1.0 先建立空间感：6 个文件的分工

打开 `scm-source-all`，`Ctrl+N` 输入 `CentralizedProcurementReport`，会跳出 5 个文件。加上一个配置类，一共 6 个：

| 文件 | 一句话职责 | 行数 | Node 类比 |
|---|---|---|---|
| `...Controller.java` | 接 HTTP 请求 | ~110 | Koa 的 router |
| `...Service.java` | **接口**：声明有哪些能力 | ~110 | 无（JS 不需要）|
| `...ServiceImpl.java` | **真正干活的**，全部逻辑在这 | **947** | service 层 |
| `...Model.java` | ES 里一条记录长什么样 | ~100 | 无（JS 直接给对象）|
| `...Repository.java` | 往 ES 写的那只手 | **9 行** | 无 |
| `SourceChaseConfig.java` | 从 Nacos 读索引名 | 共用 | config 模块 |

**新手最容易懵的是"为什么一个功能要 6 个文件"。** 记住这个顺序就不乱了：

```
HTTP 请求
   ↓
Controller     只做三件事：接参数、调 Service、包装返回。不写业务逻辑。
   ↓
Service        接口。只声明"我能干什么"，不说"怎么干"。
   ↓
ServiceImpl    怎么干全在这。查库、转换、写 ES。
   ↓  ↓
Mapper       Repository
(读 MySQL)    (写 ES)
```

**为什么 Service 要拆成接口 + 实现两个文件？** 三个真实原因（不是形式主义）：

1. **AIM 机制要求**：`@TypeMapping` 必须贴在接口上才会被扫描到（见 3.9.1），没有接口 = 定时任务用不了。
2. **Spring 的事务/AOP 依赖接口代理**。
3. 团队所有 Service 都这么写，你不这么写会显得很突兀。

### 4.1.1 第 1 站：Controller —— 门口

打开 `CentralizedProcurementReportController.java`，你会看到**四个几乎一样的方法**。挑 `syncZcToEs` 看：

```java
@ApiOperation("手动同步集采管理业务统计报表(招采平台)数据到ES")
@PostMapping("e/business/source/centralizedProcurementReport/syncZc")
public Result<String> syncZcToEs(@RequestBody(required = false) DateRangeParam param) {
    int count = centralizedProcurementReportService.syncZcData(
            param != null ? param.getStartTime() : null,
            param != null ? param.getEndTime() : null);
    return Result.success("集采管理业务统计报表(招采平台ZC)数据同步成功，写入 " + count + " 条");
}
```

**逐行拆**：

| 代码 | 干什么 | Koa 对应 |
|---|---|---|
| `@PostMapping("e/business/...")` | 注册路由。**路径必须写全**，网关不剥前缀 | `router.post('/path', fn)` |
| `@RequestBody(required=false)` | 把请求体 JSON 转成 Java 对象；`false` = 允许不传 | `ctx.request.body` |
| `DateRangeParam` | 参数的形状（文件最下面那个内部类） | JS 直接解构，Java 必须先定义类 |
| `Result.success(...)` | 包装成公司统一响应格式 | `ctx.body = { status:true, data }` |

**注意 Controller 有多"薄"** —— 5 行，没有任何业务逻辑。这是刻意的：**Controller 只负责"翻译 HTTP"，换成从定时任务触发时，压根不经过它。**

**`DateRangeParam` 里那个注解值得看一眼**：

```java
@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "GMT+8")
private Date startTime;
```

告诉框架：前端传的 `"2026-01-01 00:00:00"` 按**北京时间**解析。不写 `timezone` 会按服务器时区，容易差 8 小时。

> **IDEA 操作**：光标放在 `syncZcData` 上按 `Ctrl+B` → 跳到 Service 接口。这就是下一站。

### 4.1.2 第 2 站：Service 接口 —— 能力清单

```java
@TypeMapping("centralizedProcurementReportService")     // ← 服务外号（给 AIM/XXL-JOB 用）
public interface CentralizedProcurementReportService {

    @MethodMapping("syncZcData")                        // ← 方法外号
    int syncZcData(SyncParam param);                    // ← 给定时任务用

    int syncZcData(Date startTime, Date endTime);       // ← 给 Controller 用
    ...
}
```

**每个来源都是"一对方法"，这个设计要理解**：

| 版本 | 参数 | 谁调 | 为什么 |
|---|---|---|---|
| `syncZcData(SyncParam)` | 字符串日期 | XXL-JOB | 定时任务只能传字符串 |
| `syncZcData(Date, Date)` | Java 日期 | Controller | HTTP 已经帮你转好了 |

**同名不同参数 = Java 的"方法重载"**，JS 里没这东西（你会写一个函数判断参数类型）。

`SyncParam` 那个内部类只有两个 String 字段，就是为了接 XXL-JOB 传来的 JSON。

> **IDEA 操作**：在接口方法名上按 `Ctrl+Alt+B`（跳到实现），进入 ServiceImpl。

### 4.1.3 第 3 站：ServiceImpl —— 947 行，但别怕

**先看结构，别从第 1 行读。** 在 IDEA 里按 `Alt+7` 打开 Structure 面板，或者用这张地图：

| 行 | 内容 | 要不要细读 |
|---|---|---|
| 58-92 | 12 个 `@Autowired` 工具 | 扫一眼 |
| 94-167 | 常量定义 | **要读**，业务口径都在这 |
| 169-317 | FP 非平台录入同步 | 读一个就够 |
| **319-517** | **ZC 招采平台同步** | **重点读这个**，最复杂 |
| 519-616 | MT 煤炭SAP 同步 | 快速扫 |
| 618-718 | JD 机电四大类同步 | 快速扫 |
| 720-947 | 私有工具方法 | 按需跳转 |

**读法建议：只精读 ZC 那一段（319-517）。** 它是四个里最复杂的（4 层展开），读懂它，另外三个扫一眼就懂。

#### 3-1：12 个工具（58-92 行）

```java
@Autowired private CentralizedProcurementReportRepository centralizedProcurementReportRepository;  // 写 ES
@Autowired private SourceMapper sourceMapper;              // 读 sc_source
@Autowired private SchemeMapper schemeMapper;              // 读 sc_scheme
@Autowired private DealResultMapper dealResultMapper;      // 读 sc_deal_result
@Autowired private DealDetailMapper dealDetailMapper;      // 读 sc_deal_detail
@Autowired private DealProductMapper dealProductMapper;    // 读 sc_deal_product
...
@Autowired private DictFeignService dictFeignService;      // 调 UBM 服务查字典
```

**一个 Mapper = 一张表的操作入口。** 看这份清单就知道这个类会碰哪些表。

**`DictFeignService` 是特殊的那个** —— 它不查数据库，而是**通过 HTTP 调另一个服务**（UBM 用户/组织/字典服务）。Feign 的魔法是：你只写一个接口声明，框架自动生成 HTTP 调用代码。

**Node 类比**：像你封装了一个 `userService.getDict()`，内部其实是 `axios.post('http://ubm/dict')`。

#### 3-2：常量（94-167 行）—— 这里藏着全部业务口径

**这段一定要读，它比代码更重要。** 产品的每一条规则最后都变成了这里的一个常量：

```java
private static final String DATA_SOURCE_ZC = "ZC";                    // 数据来源编码

private static final List<String> ZC_PURCHASE_METHODS = Arrays.asList( // 需求说的 5 个页面
        PurchaseMethodEnum.BID.getCode(),                // 101 招标
        PurchaseMethodEnum.SINGLE.getCode(),             // 102 单一来源
        PurchaseMethodEnum.COMPARE.getCode(),            // 103 询价
        PurchaseMethodEnum.COMPETE_NEGOTIATE.getCode(),  // 104 竞争性谈判
        PurchaseMethodEnum.COMPETE_CONSULT.getCode());   // 105 竞争性磋商

private static final String MT_SUPPLIER_NAME = "唐山冀东水泥股份有限公司北京分公司"; // 需求要求写死
private static final String JD_EXECUTE_UNIT_NAME = "机电公司";
private static final int IS_CENTRALIZED_YES = 1;

private static final int QUERY_BATCH_SIZE = 500;   // 查库分批
private static final int SAVE_BATCH_SIZE = 1000;   // 写 ES 分批
```

**为什么用 `PurchaseMethodEnum.BID.getCode()` 而不直接写 `"101"`？**
因为 `Ctrl+B` 能跳到枚举定义看清 101 是什么；写死 `"101"` 半年后没人知道是啥。
**这是 Java 项目里最常见的可读性手法**，你以后写代码也这么干。

> **IDEA 操作**：光标放 `PurchaseMethodEnum` 上 `Ctrl+B`，能看到 7 个采购方式的完整定义（含没被选中的"竞价"和"资格预审"）。

#### 3-3：入口的参数处理（332-353 行）

```java
public int syncZcData(Date startTime, Date endTime) {
    Date actualStartTime = startTime;
    Date actualEndTime = endTime;

    if (actualStartTime == null && actualEndTime == null) {
        Date yesterday = calculateYesterdayDate();
        actualStartTime = getStartOfDay(yesterday);      // 昨天 00:00:00.000
        actualEndTime = getEndOfDay(yesterday);          // 昨天 23:59:59.999
    } else if (actualStartTime != null && actualEndTime == null) {
        actualEndTime = getEndOfDay(actualStartTime);    // 只传开始 → 补当天结束
        actualStartTime = getStartOfDay(actualStartTime);
    } else if (...) { ... }
```

**这段是"同一个方法同时服务定时任务和手动补数"的关键**：

| 调用方式 | 效果 |
|---|---|
| 定时任务传 `{}` → 两个都是 null | 同步昨天 |
| 手动传一个日期 | 同步那一整天 |
| 手动传两个日期 | 同步这个区间（历史补数就靠这个）|

**注意 `getEndOfDay` 用的是 `23:59:59.999`**（935 行）。如果写成 `23:59:59`，那一秒内的数据会被漏掉。这种边界处理在报表类需求里很常见。

#### 3-4：四层展开（355-443 行）—— ZC 最难的部分

回忆业务：`sc_source`（方案）→ `sc_deal_result`（定标方案）→ `sc_deal_detail`（供应商）→ `sc_deal_product`（物料）。

代码就是**一层层往下捞**，每层都是同一个套路：

**第 1 层 — 捞方案（356-361）**：

```java
LambdaQueryWrapper<Source> sourceQuery = Wrappers.lambdaQuery();
sourceQuery.eq(Source::getState, SourceStateEnum.IS_COMPLETED.getCode());  // state='127' 已公示
sourceQuery.in(Source::getPurchaseMethod, ZC_PURCHASE_METHODS);            // 5 种采购方式
sourceQuery.ge(Source::getFinalTime, actualStartTime);                     // 公示时间 >=
sourceQuery.le(Source::getFinalTime, actualEndTime);                       // 公示时间 <=
List<Source> sources = sourceMapper.selectList(sourceQuery);
```

**`LambdaQueryWrapper` = 拼 SQL 的构造器**，上面这段等价于：

```sql
SELECT * FROM sc_source
WHERE state = '127' AND purchase_method IN (101,102,103,104,105)
  AND final_time >= ? AND final_time <= ?
  AND delete_sign = 0        -- ← 这行你没写，MyBatis-Plus 自动加的
```

**Node 类比**：knex 的 `.where().whereIn()` 链式调用。

**类比边界**：`Source::getState` 是 Java 的"方法引用"。框架靠它反推出列名 `state`。好处是**改字段名时编译器直接报错**，不像字符串拼 SQL 要等运行时才炸。

> ⚠️ **那行自动加的 `delete_sign = 0` 必须理解**，`bootstrap.yml` 里配的：
> ```yaml
> logic-delete-field: deleteSign
> logic-delete-value: 1
> logic-not-delete-value: 0
> ```
> **只要实体类有 `deleteSign` 字段，每条查询自动加过滤。**
> 做煤炭SAP 时这条差点要命：`sc_source_collection` 的 `delete_sign` 列 **2792 行全是 NULL**。
> 万幸 `SourceCollection.java` 里没定义这个字段，不会加过滤，否则**一条都查不到**。
> **IDEA 验证法**：打开实体类 `Ctrl+F` 搜 `deleteSign`，搜不到 = 不会自动过滤。

**第 2 层 — 是否集采（380）**：

```java
Map<String, Integer> collectionMap = queryCollectionMap(schemeIds);
```

`Ctrl+B` 跳到 793 行。做的事：拿一堆 schemeId 去 `sc_scheme` 查 `is_collection`，返回一个 `方案ID → 0/1` 的字典。

**注意它是分批查的**（795 行 `i += QUERY_BATCH_SIZE`）：一次最多 500 个 ID。
**为什么**：`WHERE scheme_id IN (...)` 塞几万个 ID 进去，SQL 会超长直接报错。

**第 3 层 — 定标方案（383）**：

```java
Map<String, DealResult> dealResultMap = queryLatestPassedDealResult(schemeIds);
```

`Ctrl+B` 跳到 814 行。这个方法有业务含金量：

```java
resultQuery.eq(DealResult::getState, DealStateEnum.PASSED.getCode());   // 只要审批通过的
...
for (DealResult result : results) {
    DealResult exists = map.get(result.getSchemeId());
    if (exists == null || isAfter(result.getCreateTime(), exists.getCreateTime())) {
        map.put(result.getSchemeId(), result);      // 保留 create_time 最新的那份
    }
}
```

**为什么要"只要通过的 + 取最新"**：定标方案被拒后可以重做，所以一个采购方案可能有好几份（实测有"审批中/通过/拒绝"三种）。不过滤 = 把被拒绝的方案也统计进报表。

**第 4、5 层 — 供应商和物料（394-435）**：套路一样，分批 `IN` 查询。

最后两行是精髓：

```java
Map<String, List<DealProduct>> productMap = dealProducts.stream()
        .collect(Collectors.groupingBy(DealProduct::getDealDetailId));
```

**`groupingBy` = JS 的 `reduce` 分组**。把物料列表按"属于哪个供应商记录"分成一组组。

**Node 类比**：
```js
const productMap = _.groupBy(dealProducts, 'dealDetailId')
```

**为什么要分组**：下一步循环时要快速拿到"这个供应商的所有物料"，用 Map 是 O(1)，每次遍历一遍是 O(n²)。

> 💡 **这里体现了一个重要模式：批量查 + 内存分组，而不是循环里查库。**
> 如果写成"对每个供应商查一次物料"，1000 个供应商 = 1000 次数据库往返，这叫 **N+1 查询问题**，
> 是后端性能事故的头号原因。你在 Node 里用 ORM 也会遇到同样的坑。

#### 3-5：转换（446-501 行）—— 一条物料变成一条 ES 记录

```java
for (DealDetail detail : dealDetails) {              // 外层：每个中标供应商
    Source source = sourceMap.get(detail.getSchemeId());
    if (source == null) continue;                    // 找不到方案，跳过
    List<DealProduct> products = productMap.get(detail.getDetailId());
    if (products == null || products.isEmpty()) continue;

    for (DealProduct product : products) {           // 内层：这个供应商的每个物料
        CentralizedProcurementReportModel model = new CentralizedProcurementReportModel();

        model.setId(buildZcId(source.getSchemeCode(), detail.getTenderNum(),
                              detail.getSupplierCode(), product.getProductCode()));
        model.setBuCode(source.getBuCode());
        model.setBuName(resolveBuName(buNameMap, source.getBuCode(), source.getBuName()));
        ...
        models.add(model);
    }
}
```

**双层循环 = 报表"一行一物料"的由来。** 一个供应商 3 个物料，就产生 3 条 ES 记录。

**三个值得单独看的赋值：**

**① 主键（461 行）** — `Ctrl+B` 进 `buildZcId`（847 行）：

```java
return schemeCode + tenderNum + supplierCode + productCode;   // 四段拼接
```

方法上的注释就是它存在的理由：同一方案同一物料可被多家供应商分标、还可能分标段，只用"方案号+物料编码"会互相覆盖。**这是这次开发发现的第一个大坑**（详见 4.4）。

**② 板块名（466 行）** — `Ctrl+B` 进 `resolveBuName`（872 行）：

```java
if (buCode != null && buNameMap.containsKey(buCode)) {
    return buNameMap.get(buCode);     // 优先用字典标准名
}
return rawBuName;                     // 字典查不到才用表里的原名
```

**为什么不直接用表里的 `bu_name`**：实测同一个 `BU004` 在表里有"冀东集团""冀东发展"好几种写法，还有 NULL。直接用会导致同一板块在报表里显示成好几个名字，跟下拉框对不上。

那个 `buNameMap` 从哪来？443 行调 `queryOwningplateDictMap()`（882 行）：

```java
Result<List<DictDTO>> result = innerFeignService.execute(() -> dictFeignService.getDictByType(param));
```

**这行是跨服务调用** —— HTTP 请求 UBM 服务拿字典。注意它在**循环外面**只调一次，把结果缓存成 Map。放循环里就是每条数据一次 HTTP，直接把服务打挂。

**③ 采购业务类型（494 行）** — `resolvePurchaseType`（865 行）：

```java
return isJc != null && isJc == 1 ? "集团集采" : "分散采购";
```

产品那句"是否集采=是→集团集采，否→分散采购"，就是这一行。

#### 3-6：写 ES（508-516 行）

```java
for (int i = 0; i < models.size(); i += SAVE_BATCH_SIZE) {
    List<...> saveBatch = models.subList(i, Math.min(i + SAVE_BATCH_SIZE, models.size()));
    centralizedProcurementReportRepository.saveAll(saveBatch);
    log.info("已写入 {}/{}", ...);
}
return models.size();
```

**每 1000 条写一次。** 一次性 `saveAll` 十万条会撑爆内存和 ES 的请求体限制。

**`saveAll` 的行为就是本文 3.6 讲过的**：`_id` 相同覆盖，不同新增。**所以重复跑同一天不会产生重复数据** —— 这个特性叫**幂等**，是所有同步任务的必备品质。

### 4.1.4 第 4 站：Repository —— 9 行的文件为什么能写 ES

```java
public interface CentralizedProcurementReportRepository
        extends ElasticsearchRepository<CentralizedProcurementReportModel, String> {
}
```

**大括号里是空的，一行实现都没有。** 但你能调 `saveAll()`、`findById()`、`deleteById()`。

**因为 Spring Data 在运行时自动生成了实现类。** 你声明"我要一个操作 `CentralizedProcurementReportModel`、主键是 `String` 的仓库"，框架就把 CRUD 方法都给你实现好。

**Node 类比**：像 Mongoose 的 `mongoose.model('X', schema)` —— 你只给 schema，`find`/`save` 白送。

**类比边界**：Mongoose 是运行时对象，这个是**编译期就有类型的接口**，写错方法名编译不过。

### 4.1.5 第 5 站：Model —— ES 字段合同

```java
@Document(indexName = "#{@sourceChaseConfig.getCentralizedProcurementReportIndex()}", dynamic = Dynamic.FALSE)
public class CentralizedProcurementReportModel implements Serializable {

    @Id
    @Field(type = FieldType.Keyword)
    private String id;

    @Field(type = FieldType.Text, analyzer = "ik_max_word")
    private String buName;

    @Field(type = FieldType.Keyword)
    private String buCode;
    ...
}
```

**四个注解逐个说**：

| 注解 | 作用 |
|---|---|
| `@Document(indexName=...)` | 这个类对应哪个 ES 索引。`#{@bean.method()}` 是 Spring 表达式，运行时从 Nacos 取值 |
| `dynamic = Dynamic.FALSE` | **不许自动加字段**。防止某天有人多 set 一个字段，ES 里悄悄多出一列 |
| `@Id` | 这个字段当 `_id` 用 |
| `@Field(type=...)` | 这个字段在 ES 里是什么类型 |

**`Keyword` vs `Text` 是最需要理解的一对**（对应 4.2 讲的 term/match 查询）：

| 类型 | 存储方式 | 用在哪 | 后果 |
|---|---|---|---|
| `Keyword` | **整个值当一个词**，不切 | `buCode`、`planCode`、`dataSource` | 只能精确匹配 |
| `Text` + `ik_max_word` | **中文分词后存** | `purchaseCompanyName`、`supplierName` | 能搜简称、关键词 |

**`ik_max_word` 是中文分词器**："唐山市盾石信息技术有限公司" → 「唐山/市/盾石/信息/技术/有限公司」。所以搜"盾石"能命中。

**这就是为什么查询侧企业名必须用 `matchQuery` 而不是 `termQuery`** —— 字段本身是切碎存的，你拿完整公司名去精确匹配，反而匹配不上。

> **写入侧和查询侧两个 Model 的唯一区别**：查询侧多了 `createIndex = false`（不要抢着建索引）。见 4.2 最后。

### 4.1.6 另外三个来源：对比着看，10 分钟搞定

读懂 ZC 后，另外三个只看**差异**：

| | FP 非平台 | ZC 招采 | MT 煤炭SAP | JD 机电 |
|---|---|---|---|---|
| 行号 | 182-317 | 332-517 | 532-616 | 631-718 |
| 主表 | `sc_supplier_green_channel` | `sc_source` | `sc_source_collection` | `sc_collection_daily_amount_ledger` |
| 展开几层 | 2 层 | **4 层** | **0 层** | **0 层** |
| 时间字段 | `update_time` | `final_time` | `final_time` | `statistic_date` |
| 主键方法 | `buildId` (857) | `buildZcId` (847) | `buildMtId` (740) | 直接用 `ledgerNo` |
| 是否集采 | 表里 `is_jc` | 查 `sc_scheme` | 固定 1 | 固定 1 |
| 特殊处理 | — | 4 层展开 | 板块名全靠字典、业务名要拼 | 类目名走字典去脏值 |

**MT 和 JD 因为是 0 层展开，代码短得多** —— 没有嵌套循环，一条原始数据直接变一条 ES 记录。

**MT 唯一的难点是主键**（740 行 `buildMtId`）：

```java
String day = finalTime == null ? "" : new SimpleDateFormat("yyyyMMdd").format(finalTime);
return purchaseBusinessCode + day + productCode;
```

**中间那个日期是这次开发的第二个大坑**：SAP 按天拉流水，同订单同物料每天一条，不加日期会把 18 天压成 1 条（详见 4.4）。

**JD 的类目名处理值得看一眼**（765 行 `resolveCategoryName`）：跟板块名一个套路，按编码查字典取标准名。因为实测台账里有条脏数据叫"钢材-接口测试修改"，编码还是 1501，走字典后正常显示"钢材"。

### 4.1.7 写入侧自测（合上文档答）

1. Controller 里为什么只有 5 行，业务逻辑去哪了？
2. 同一个 `syncZcData` 有两个版本，分别谁调，为什么要两个？
3. `LambdaQueryWrapper` 拼出的 SQL 里，那个 `delete_sign = 0` 是谁加的？什么情况下它会害你查不到数据？
4. `queryOwningplateDictMap()` 为什么写在循环外面？放循环里会怎样？
5. 为什么要"批量查 + 内存 groupingBy"，而不是在循环里逐个查库？
6. `Repository` 文件里一行实现都没有，`saveAll()` 哪来的？
7. `Keyword` 和 `Text` 什么区别？`supplierName` 为什么是 `Text`？
8. 重复跑同一天，ES 里数据会翻倍吗？为什么？
9. `buildZcId` 为什么要拼四段？`buildMtId` 为什么要塞一个日期进去？

## 4.2 第二段：查询侧（report）

这段之前没人给你讲过，**重点读**。

### 入口

打开 `scm-report-all`，找 [SourceMetaQueryController.java:286](01zhaocai-end/scm-report-all/scm-report-source/src/main/java/com/pcitc/scm/report/source/controller/SourceMetaQueryController.java:286)：

```java
@PostMapping("e/business/report/source/centralizedProcurementPageList")
@DataRequestTransfer(value = {
    @DateTransfer(root = "model", jsonPath = "queryTimeArr", value = { "beginTime", "endTime" })
}, exportValue = {@ExportRequestTransfer(exportMax=10000)})
public Result<PageList<CentralizedProcurementReportMetaModel>> getCentralizedProcurementPageList(
        DefaultPageBean<CentralizedProcurementReportMetaModel> page) {
```

**三个值得注意的地方**：

1. **路径是 `report` 不是 `source`** —— 不同的服务，不同的容器。
2. **`@DateTransfer`** 是公司自己写的注解：前端传一个数组 `queryTimeArr: [开始, 结束]`，它自动拆成 `beginTime` / `endTime` 两个字段。这是团队约定，不是 Spring 自带的。
3. **`DefaultPageBean<T>`** 是分页包装：里面有 `currentPage`、`limit` 和 `model`（真正的查询条件）。

所以前端发过来的 JSON 长这样：

```json
{
  "currentPage": 1,
  "limit": 10,
  "model": { "purchaseCompanyName": "唐山", "buCode": "BU002" }
}
```

### 核心查询

`Ctrl+B` 点进 `getCentralizedProcurementPageList`，到 [SourceMetaQueryServiceImpl.java:1618](01zhaocai-end/scm-report-all/scm-report-source/src/main/java/com/pcitc/scm/report/source/service/impl/SourceMetaQueryServiceImpl.java:1618)。

这个方法就干一件事：**把前端传的条件，翻译成 ES 的查询语句**。

```java
BoolQueryBuilder bl = QueryBuilders.boolQuery();   // 一个"条件容器"

// 数据权限：只能看自己有权限的公司
if (Boolean.TRUE.equals(sourceChaseConfig.getUsePerssion())) {
    List<String> r = reportFacedAdapter.getPermissionCompanyIds();
    if (!r.isEmpty()) {
        bl.must(QueryBuilders.termsQuery("purchaseCompanyCode", r));
    }
}

// 企业名称：模糊匹配
if (StringUtils.isNotBlank(param.getPurchaseCompanyName())) {
    bl.must(QueryBuilders.matchQuery("purchaseCompanyName", param.getPurchaseCompanyName())
            .operator(Operator.AND));
}

// 板块：精确匹配
if (StringUtils.isNotBlank(param.getBuCode())) {
    bl.must(QueryBuilders.termQuery("buCode", param.getBuCode()));
}
```

**`must` = SQL 的 AND。** 每个 `if` 判断"前端传了这个条件吗"，传了才加进去。

**`matchQuery` 和 `termQuery` 的区别 —— 这是 ES 最核心的概念，务必搞懂：**

| | `termQuery` | `matchQuery` |
|---|---|---|
| 含义 | **精确相等** | **分词后匹配** |
| SQL 类比 | `= 'BU002'` | 类似 `LIKE '%唐山%'`（但更聪明）|
| 用在什么字段 | `keyword` 类型 | `text` 类型 |
| 本报表里 | `buCode`、`dataSource` | `purchaseCompanyName`、`supplierName` |

**为什么名称类字段要用 match**：ES 会把"唐山市盾石信息技术有限公司"用中文分词器切成「唐山 / 市 / 盾石 / 信息 / 技术 / 有限公司」这些词存起来。你搜"盾石"，能命中。如果用 `termQuery`，必须一字不差输入整个公司名才能查到 —— 那就没法用了。

`.operator(Operator.AND)` 的意思是：**你输入的词必须全部命中**。搜"唐山盾石"，切成「唐山」「盾石」，两个词都得有。不加这个默认是 OR，搜"唐山盾石"会把所有带"唐山"的公司都查出来。

> 这一条是踩过坑改过来的：早先的实现用了短语查询，导致输入公司简称查不到。现在实测简称能查到，不存在的名字返回 0 条。

**排序和分页**：

```java
PageRequest pageRequest = PageRequest.of((page.getCurrentPage() - 1), page.getLimit());
NativeSearchQueryBuilder searchQueryBuilder = new NativeSearchQueryBuilder()
        .withQuery(bl)
        .withPageable(pageRequest)
        .withSorts(SortBuilders.fieldSort("bookTime").order(SortOrder.DESC));  // 时间倒序
```

注意 `currentPage - 1`：前端第 1 页，ES 里是第 0 页。

**执行查询**（[:1669](01zhaocai-end/scm-report-all/scm-report-source/src/main/java/com/pcitc/scm/report/source/service/impl/SourceMetaQueryServiceImpl.java:1669)）：

```java
SearchHits<CentralizedProcurementReportMetaModel> searchHits =
        elasticsearchOperations.search(searchQuery, CentralizedProcurementReportMetaModel.class);
```

**注意写入侧和查询侧用的工具不一样**：

| | 工具 | 为什么 |
|---|---|---|
| 写入（source）| `Repository.saveAll()` | 简单，只是批量塞数据 |
| 查询（report）| `elasticsearchOperations.search()` | 要拼复杂条件，Repository 做不到 |

### 导出 Excel

同文件 [:1682](01zhaocai-end/scm-report-all/scm-report-source/src/main/java/com/pcitc/scm/report/source/service/impl/SourceMetaQueryServiceImpl.java:1682)，逻辑很直白：

```java
page.setCurrentPage(1);
page.setLimit(10000);                                    // ← 一次拉 10000 条
PageList<...> data = getCentralizedProcurementPageList(page);   // 复用上面的查询

// 定义 15 个表头
headers.add(Arrays.asList("序号"));
headers.add(Arrays.asList("板块名称"));
...

// 一行一行拼数据
for (...) {
    row.add(i + 1);
    row.add(model.getBuName());
    ...
    row.add(convertIsCentralized(model.getIsCentralized()));   // 1/0 → 是/否
    row.add(convertDataSource(model.getDataSource()));         // ZC → 招采平台
}

EasyExcel.write(fos).head(headers).sheet("集采管理业务统计报表").doWrite(rows);
```

**`convertDataSource` 就是你问过的那个"报表和前端不用改"的原因** —— 五个来源的中文转换早就全写好了：

```java
case "ZC": return "招采平台";
case "FP": return "非平台录入";
case "YC": return "云采平台";
case "MT": return "煤炭-SAP";
case "JD": return "机电四大类";
```

**⚠️ 这里有个已知限制**，你以后会遇到：`page.setLimit(10000)` 是写死的，因为 ES 默认分页上限就是 1 万。现在数据少无所谓，等五个来源真实数据都进来（光煤炭SAP 一个月就 503 条），**很容易超过 1 万行，超出的部分会被静默丢掉，导出的 Excel 少数据但不报错**。到时候要改成 ES 的 `scroll` 或 `searchAfter` 分批取。

### 读模型和写模型的区别

打开 `CentralizedProcurementReportMetaModel.java`（report 侧），对比 `CentralizedProcurementReportModel.java`（source 侧）：

**18 个字段一模一样，只有一处不同**：

```java
// source 写入侧
@Document(indexName = "#{@sourceChaseConfig.getCentralizedProcurementReportIndex()}", dynamic = Dynamic.FALSE)

// report 查询侧
@Document(indexName = "#{@sourceChaseConfig.getCentralizedProcurementReportIndex()}", createIndex = false, ...)
```

**`createIndex = false` 是什么意思**：报表服务启动时**不要去创建索引**。因为索引由 source 服务负责建，两个服务同时抢着建会打架。

**为什么要两个几乎一样的文件？** 因为它们在**两个不同的仓库**，Java 没法跨仓库共享类（除非发布成 jar，那更麻烦）。所以两边各写一份，靠"字段名和类型必须一致"这个人肉约定维系。这就是文档里反复强调的 **"ES 字段合同"**。

## 4.3 第三段：前端

打开 `scm-vue-all-procurementscheme`。

### 路由

[src/router/index.js:463](02zhaocai-front/scm-vue-all-procurementscheme/src/router/index.js:463)：

```js
{
  path: '/reportForms/centralizedProcurement',
  name: 'centralizedProcurementReport',
  component: () => import('../views/reportForms/centralizedProcurementReport/index.vue')
}
```

**这里有个坑值得记**：这条固定路由必须写在通用路由 `/reportForms/:purchaseRepot` **前面**。Vue Router 是从上往下匹配的，写后面会被那个动态路由先抢走。

### 页面调接口

[index.vue:208](02zhaocai-front/scm-vue-all-procurementscheme/src/views/reportForms/centralizedProcurementReport/index.vue:208)：

```js
const { data, status, msg } = await request(
  '/e/business/report/source/centralizedProcurementPageList',
  { method: 'POST', ...this.buildQueryParams(params) }
)
```

`request` 来自 `scm-request`（公司封装的 npm 包），**它自动做了**：拼网关地址、带上登录 token、统一处理错误。**Node 类比**：你封装过的 axios 实例。

导出用 `downloadFile`，专门处理文件流下载。

### 中文转换在前端也有一份

```js
const dataSourceMap = {
  ZC: '招采平台', FP: '非平台录入', YC: '云采平台', MT: '煤炭-SAP', JD: '机电四大类'
}
```

**注意：这套映射存在三个地方** —— ES 里存编码、report 导出时转、前端表格显示时转。改一个中文名，三处都要改。这是当前实现的一个小重复，知道就行。

## 4.4 重点：这次开发发现的两个"会悄悄吃数据"的坑

这两个是这次开发最有价值的部分，也最能体现"为什么需要人去核实数据而不是照着需求写"。

### 坑一：招采平台，同一物料多家供应商分标

**表面**：主键定为「采购方案编号 + 物料编码」，看起来天经地义。

**真相**：同一个方案的同一个物料，**可以被多家供应商分着中标**。

实测 test 库：方案 `12870012-XJ-2026-0001` 的物料 `260101010002`，3 家供应商各中 300 / 100 / 670.9 元。历史数据里同一组合最多重复 **7 次**。

**后果**：3 条数据主键完全相同 → 写进 ES 互相覆盖 → 只剩 1 条 → 报表少 2 行，金额从 1070.9 变成只剩一笔。**而且不报任何错。**

**解法**：主键改成「方案编号 + 标段号 + 供应商编码 + 物料编码」。

### 坑二：煤炭SAP，按天拉的流水

**表面**：跟其他来源一样，「订单号 + 物料编码」。

**真相**：SAP 数据是**按天拉的收发流水**，同一订单的同一物料**每天一条**。

实测：订单 `4500931416` 的「烟煤^4500~5000kcal」，从 1 月 9 日到 27 日 **18 条**，每天金额 2.9 万到 40 万不等。

**后果**：18 条压成 1 条。全量算下来 503 条会掉到 375 条。

**解法**：主键加上公示日期 → 「订单号 + yyyyMMdd + 物料编码」。加完实测 503 条 → 503 个不同主键，0 碰撞。

### 这两个坑教给你的方法论

**需求文档描述的是"页面上看到什么"，不是"数据库里长什么样"。** 产品看到详情页一个物料一行，不会知道底下是三家供应商分标。

所以拿到需求后必须做的一件事：**用真实数据验证主键会不会重复**。一条 SQL 的事：

```sql
SELECT 主键的那几个字段, COUNT(*) c
FROM 表 WHERE 过滤条件
GROUP BY 主键的那几个字段 HAVING c > 1;
```

返回空 = 安全。返回有数据 = 会丢行。

**以后遇到任何"往 ES/Redis 里按主键写"的需求，这条 SQL 都该跑一遍。**

---

# 第五部分：一次完整的数据旅行（把所有东西串起来）

用煤炭SAP 的一条数据走完全程：

```
【第 0 步】SAP 系统每天把煤炭采购数据推过来
          → 落进 MySQL：scm_source_test.sc_source_collection
            data_source='SAP', purchase_business_code='4500931416',
            product_code='100101000003', tax_total=148649.40,
            final_time='2026-01-27', bu_code='BU002', bu_name=NULL ← 注意是空的

【第 1 步】XXL-JOB 每天定时触发（或你手动调接口）
          POST /e/business/source/centralizedProcurementReport/syncMt
                    ↓ 网关转发给 scm-source-web 容器
          CentralizedProcurementReportController.syncMtToEs()

【第 2 步】ServiceImpl.syncMtData() 干活
          ① 算时间范围（没传 → 昨天）
          ② sourceCollectionMapper.selectList()  查 MySQL
          ③ queryOwningplateDictMap()            调 UBM 服务拿板块字典
             BU002 → "水泥集团"    ← bu_name 是 NULL，全靠这一步补上
          ④ new Model() 逐字段赋值：
             id        = "4500931416" + "20260127" + "100101000003"
             buName    = "水泥集团"        ← 字典翻译过的
             supplierName = "唐山冀东水泥股份有限公司北京分公司"  ← 需求要求写死
             planName  = "烟煤^4500~5000(不含)kcal商品采购"      ← 拼出来的
             taxTotal  = 148649.40
             bookTime  = 2026-01-27
             isCentralized = 1, purchaseType = "集团集采"   ← 固定值
             dataSource = "MT"
          ⑤ repository.saveAll()  写进 ES

【第 3 步】数据静静躺在 ES 索引 index_centralized_procurement_report_test 里
          和 FP、ZC、YC、JD 的数据混在一起，共 N 条

【第 4 步】用户打开页面
          浏览器 → /procurementScheme/index.html#/reportForms/centralizedProcurement
          Vue 路由匹配 → 加载 index.vue → created() 里调 init()

【第 5 步】前端发查询
          POST /e/business/report/source/centralizedProcurementPageList
          { currentPage:1, limit:10, model:{ purchaseCompanyName:"唐山" } }
                    ↓ 网关转发给 scm-report-web 容器（注意：另一个服务）

【第 6 步】SourceMetaQueryServiceImpl.getCentralizedProcurementPageList()
          ① 拼 ES 查询条件：权限过滤 + matchQuery(企业名) + 时间范围
          ② 按 bookTime 倒序、分页
          ③ elasticsearchOperations.search() 执行
          ④ 返回 PageList

【第 7 步】前端拿到 JSON，渲染表格
          dataSource: "MT"  → dataSourceMap 转成 "煤炭-SAP"
          isCentralized: 1  → 显示 "是"
          bookTime          → moment 格式化成 "2026-01-27 00:00"

【第 8 步】用户点导出
          POST .../centralizedProcurementDownload
          → 复用第 6 步的查询（limit 改成 10000）
          → convertDataSource("MT") → "煤炭-SAP"
          → EasyExcel 生成 xlsx → 浏览器下载
```

**整条链路跨越**：2 个 Java 服务 + 1 个 Vue 前端 + 1 个 MySQL + 1 个 ES + 1 个网关 + 1 个字典服务 + 1 个定时任务平台。

这就是为什么你觉得复杂 —— **它确实复杂**，这是企业系统的常态。

---

# 第六部分：读完自测

能不看文档答出来，就算真懂了。答不出的回去翻对应章节。

### 业务层

1. 报表的"一行"代表什么？为什么不是一笔采购业务一行？（→ 2.2）
2. 五个数据来源分别叫什么编码？哪个是同事负责的？（→ 1.1）
3. 招采平台要展开几层才拿到物料？煤炭SAP 呢？（→ 2.3）

### 架构层

4. 为什么不直接查 MySQL 出报表，非要过一遍 ES？说三个理由。（→ 3.5）
5. ES 里数据不是实时的，会带来什么问题？（→ 3.5 最后）
6. 写入侧和查询侧为什么是两个不同的服务、两个不同的仓库？（→ 4.0、4.2 最后）
7. 同一个索引，为什么要写两份几乎一样的 Model 文件？（→ 4.2 最后）

### 技术层

8. `termQuery` 和 `matchQuery` 什么区别？企业名称该用哪个，为什么？（→ 4.2）
9. `@Service` 和 `@Autowired` 各干什么？框架在哪里扫描它们？（→ 3.4）
10. 什么是 SNAPSHOT？为什么同事从不本地编译？（→ 3.3）
11. Nacos 是干什么的？索引名为什么不写死在代码里？（→ 3.8）
12. XXL-JOB 的参数为什么只能传 `{}`，不能传日期？（→ 3.9）
13. 点一次 Jenkins 构建，背后发生了哪 5 步？（→ 3.10）
14. **MySQL 建表和 ES 建索引，哪个要手动、哪个自动？为什么不一样？**（→ 1.2）

### 排错层

15. 调接口返回 `{"status":true,"code":"000000","data":"ok"}`，能说明接口通了吗？（→ 3.7）
16. 主键设计错了会怎样？ES 会报错吗？用什么 SQL 提前发现？（→ 4.4）
17. MyBatis-Plus 的逻辑删除是怎么自动生效的？什么情况下会让你一条数据都查不到？（→ 4.1）

### 定位能力（打开 IDEA 实操）

18. 只给你接口路径 `/e/business/report/source/centralizedProcurementPageList`，找到它的实现方法。
19. 找到"数据来源编码转中文"的代码在哪个文件哪一行。
20. 找到煤炭SAP 的主键是怎么拼的，说出为什么要加日期。

---

# 附：名词速查

| 名词 | 一句话 | Node 类比 | 类比边界 |
|---|---|---|---|
| Spring Boot | Java 的 Web 框架 | Koa / Express | 靠注解自动装配，你不写 `new` |
| Maven | 依赖管理 + 构建 | npm | `~/.m2` 全机器共享一份 |
| SNAPSHOT | 会变的开发版本号 | `latest` tag | 是本地编译坏掉的元凶 |
| MyBatis-Plus | 操作数据库的库 | knex / Sequelize | 用方法引用而非字符串指字段 |
| Mapper | 一张表的操作接口 | knex 的 `table()` | 只写接口不写实现，框架生成 |
| Entity / Model | 一张表一个类 | 无直接对应 | Java 必须先定义结构 |
| Elasticsearch | 搜索引擎 / 查询库 | Redis 缓存（弱）| 能全文搜、能分页排序聚合 |
| index / document | 表 / 行 | — | `_id` 相同会**静默覆盖** |
| 网关 | 统一入口 + 转发 | nginx 反代 | 不剥前缀，路径要写全 |
| Nacos | 配置中心 | `.env` + dotenv | 集中在服务器，可热更新 |
| XXL-JOB | 定时任务平台 | node-cron | 有网页后台，参数按 `-` 切割 |
| Jenkins | 自动构建部署 | GitHub Actions | 公司自建，你点按钮 |
| Docker 镜像 | 程序+环境打包 | — | 保证各环境跑得一样 |
| K8s / KubeSphere | 容器编排 / 它的网页 | pm2 集群版 | 排错看日志去这 |
| Feign | 调别的服务的接口 | axios 调内部 API | 写个接口就能调，框架实现 |
| Lombok `@Data` | 自动生成 getter/setter | JS 对象天然有属性 | **隔离编译时必须显式列出源文件，否则不生成** |
| DTO / VO | 传输对象 / 展示对象 | 接口的 response 类型 | Java 要显式定义类 |
