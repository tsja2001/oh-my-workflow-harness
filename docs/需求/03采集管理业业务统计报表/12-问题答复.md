问题
1. 主键改用：采购业务编号+物料编码
2. 类目编码 类目描述 空着没问题
3. 全量统计，内部供应商和外部供应商都算；不同步旧交易报表的内部企业过滤条件。
4. ES建表，这个同事负责新建
5. 就按照他写的来吧，用updatetime
6. 新报表单独使用定时任务：首次同步2026年7月1日至上线前一天的符合条件数据；上线后每天按审批结束时间同步前一天审批通过的数据，并保留指定日期补跑功能。旧交易报表及其历史数据不处理。
7. 所属板块：
   - **结论**：下拉框直接复用现成接口 `POST /e/business/source/collectionDailyAmountLedger/queryOwningplateDict`（返回 `{code, name}` 列表，10 个板块：BU001 金隅集团 / BU002 水泥集团 / BU003 混凝土集团 / BU004 冀东发展集团 / BU005 新材产业化集团 / BU006 地产集团 / BU007 投资物业集团 / BU008 天津建材集团 / BU009 财务公司 / BU010 北京建研总院）。需求样例里写的"水泥集团"就是 BU002 的字典名，不是另起一套，11 文档里"名字对不上"那段判断作废。
   - **筛选**：前端把选中的 `bu_code`（BU002 这种）回传，后端按 `bu_code` 过滤，不要按 `bu_name` 过滤。
   - **展示（开发要注意的坑）**：报表"板块名称"列**必须按 `bu_code` 查字典取标准名**（比如 BU002 显示成"水泥集团"），**不能直接用主表 `bu_name` 字段**。实测同一 bu_code 在主表里 bu_name 有多种值甚至 NULL：BU002 有 NULL/"冀东水泥"两种；BU004 有 NULL/"冀东发展"/"冀东集团"三种；BU006 是"北京金隅地产开发集团有限公司"（字典叫"地产集团"）；BU009 是"北京金隅财务有限公司"（字典叫"财务公司"）。直接用 bu_name 展示的话，同一板块会显示成好几种名字，跟下拉框对不上。
   - **做法**：同步进 ES 时把 bu_code 也存上，板块名称列在同步时用 bu_code 查字典一次性补成标准名再存；运行时不要再每次查字典。
   - **更正 11 文档**：`11-待确认问题-cc.md` 问题 7 背景"系统里实际值是冀东水泥"那段是误判（把脏数据当成了系统标准值），建议下一版删掉。
   - **更正 10 文档**：`10-拆解规划-cc.md` 第 4 节字段映射表"板块名称 | 主.bu_name"这条要改成"按主.bu_code 查字典取标准名"，否则报表展示会乱。

8. 2026-07-20 用户补充确认主键异常处理：
   - ES 主键直接使用“采购业务编号 + 物料编码”拼接。
   - 不对物料编码为空、同一业务内物料编码重复等情况增加明细 ID 兜底，也不单独拦截；按拼接结果直接写入 ES。

9. 2026-07-20 用户确认交易金额口径：
   - 报表一行对应一条物料明细。
   - 交易金额取该物料明细自己的含税总金额 `sc_supplier_channel_projects.total_price`，不把整单 `total_amount` 重复到每条物料上。

10. 2026-07-20 用户明确“是否集采”的取值口径：
    - 以《报表查询-供应商参与企业业务统计表》逐级进入“查看-基本信息”后，页面实际展示的“是否集采”为准，不再讨论由采购业务分类反推。
    - 代码核实后的技术含义：该招采平台页面通过 `pageList_supplier` 返回的 `schemeId` 调 `getSchemeDetail`，读取 `sc_scheme.is_collection`；本期非平台记录没有 `schemeId`，其自己的 `getGreenChannelDetail` 页面展示同一业务含义的 `sc_supplier_green_channel.is_jc`，因此本期直接取 `is_jc`。
    - “采购业务类型”按需求由该结果派生：是→集团集采，否→分散采购。

11. 2026-07-20 用户决定暂缓两项非业务口径问题：
    - 当前先不追问“我方只做 ES 写入，还是连查询、导出、前端一起做”的人员分工。
    - 当前先不催收 ES 索引名、mapping 和 Nacos 配置分工；等正式进入开发且索引建完后再做技术验收。
    - 这两项不再阻塞当前业务研究，但 ES mapping 仍是正式写代码前必须核实的技术输入。

12. 2026-07-20 同事提供公共 ES Model 草案：
    - 模型名：`CentralizedProcurementReportModel`。
    - 字段统一为：`id`、`buName`、`buCode`、`purchaseCompanyName`、`purchaseCompanyCode`、`supplierName`、`supplierCode`、`planCode`、`planName`、`materialName`、`materialCode`、`categoryName`、`categoryCode`、`taxTotal`、`bookTime`、`isCentralized`、`purchaseType`、`dataSource`。
    - 这套字段已足够作为非平台写入侧的开发合同；旧规划字段 `documentId/businessCode/businessName/materialDesc/dealTime/isCollection/purchaseBusinessType` 全部停用。
    - 当前 test ES 尚未查到包含 `isCentralized` 的实际 mapping，因此“Java Model 已定义”和“索引已部署”要分开看。
    - 当时列为待补齐的 test 物理索引名和 Git 协作方式，已由后续第 18 条更新；实际 mapping 仍需等订单侧部署后由 AI 验收。
    - 第 11 条中“暂不催收 ES 信息”的决定至此结束：字段合同已到、即将进入开发，运行信息现在转为部署联调前置。

13. 2026-07-20 用户确认五类来源与协作方式：
    - 公共编码固定为：`ZC` 招采平台、`FP` 非平台录入、`YC` 云采平台、`MT` 煤炭-SAP、`JD` 机电四大类。
    - 用户负责开发 `FP` 非平台录入；同事给出的 Model 使用 `orderESConfig`，可判断它位于订单侧，但她具体负责哪些来源仍以接口交接答复为准。双方共享同一个 ES 字段合同和物理索引，不是跨服务共用同一个 Java 文件。
    - 主键最终规则已确定：`id = planCode + materialCode` 直接拼接，不考虑跨来源碰撞，不增加 `dataSource` 前缀、分隔符或其他兜底。
    - Git 已同步检查：`scm-source-all` 应从最新 `prod@8eef3668f` 新建本需求长期分支，不能沿用当前台账分支或已合入 prod 的旧 `features/jicai`。

14. 2026-07-20 Phase 4 开工结论：
    - Git、Java/Maven、MySQL、样板代码和模块依赖探针均通过，技术条件已具备；是否立即落码以后续第 17 条协作冻结决定为准。
    - 当前 test ES 尚未出现本需求实际索引和 mapping；这只阻塞部署联调，不阻塞本地写代码和隔离编译。
    - 下一步从最新 `prod@8eef3668f` 新建 `feature/jicai-report-yang`，完成 Model、Repository、Service、ServiceImpl、Controller 和 `SourceChaseConfig`，再做隔离 javac 与本地提交。

15. 2026-07-20 索引名及剩余疑问处理结论：
    - `centralizedProcurementBusinessManagementStatistics` 视为业务英文标识，不作为物理索引名。证据是它含大写字母，且同事代码的 `@Document` 实际从 `orderESConfig.getCentralizedProcurementReportIndex()` 读取 Nacos 值。
    - 同事接口具体负责哪些来源不再阻塞：FP 固定由 `scm-source-all` 独立抽数、写 ES、补数和定时执行。
    - 实际 mapping 不再让用户问或转述：本地先严格复制同事 Model；同事部署后由 AI 查询 ES 自动识别、验收。
    - 原计划只有部署后查不到索引时才追问；该计划已被后续第 17 条覆盖，现改为开发前一次性确认 test 实际索引名/Nacos 值。

16. 2026-07-20 同事确认 ES 创建方式：
    - 当前没有在 ES 后台手工创建本需求索引。
    - 订单侧代码部署后，Spring Data Elasticsearch 会根据 Model/Repository 自动创建索引和 mapping；调用一次同步接口后再把业务数据写进去。
    - 部署后的正常验收顺序是：出现新的小写物理索引 → mapping 含约定字段 → 文档数量大于 0；AI 使用 `scripts/esq.sh` 完成核对。

17. 2026-07-20 用户决定开发前先完成跨服务协作冻结：
    - 不再按“先写代码、部署时再核实”的顺序推进；先一次性确认公共 Model 的 Git 仓库/分支/commit/文件路径、test Nacos 实际索引值、唯一建索引方和同事同步接口的数据来源范围。
    - 双方共享的是字段名和字段类型合同，不是同一个 Java 文件；同事订单侧使用 `orderESConfig`，我方 source 侧使用 `sourceChaseConfig`。
    - 当前推荐由订单侧作为唯一索引创建方；我方 source Model 设置 `createIndex=false`，只写已经创建好的索引。该部署责任需同事明确答复后再落码。
    - 同事答复并提交公共 Model 后，AI 先拉取核对该 commit，再从 source 最新 `prod` 创建本需求 feature 分支。

18. 2026-07-20 同事补充确认实际索引名和 Git 协作：
    - test 实际 ES 物理索引名为 `index_centralized_procurement_report_test`。
    - 同事说明双方无需担心 Git 冲突；本地进一步核实 `scm-order-all` 与 `scm-source-all` 各自拥有独立 `.git`，双方不会修改同一仓库中的同一个 Model 文件。
    - 第 17 条中“必须先拿到同事分支/commit 才能开始”的保守门槛取消；公共字段合同继续以已收到的 `CentralizedProcurementReportModel` 为准，部署后用实际 mapping 验收。
    - 我方 source 新增 `source.chase.es.centralizedProcurementReportIndex`，test 值使用上述物理索引名；Model 使用 `sourceChaseConfig` 并设置 `createIndex=false`，避免与订单侧争抢创建索引。

19. 2026-07-20 同事和用户确认采用分阶段开发：
    - 第一阶段先把 FP 数据通过手动接口写进 ES，验证数量、字段和重复执行覆盖。
    - 手动写入通过后，再补齐历史数据处理和每天自动同步的独立 XXL-JOB。
    - ES 数据链路稳定后，最后开发 report 查询导出和前端报表功能。
    - 后续 AI 必须按阶段闸门推进；当前只允许进入第一阶段，不把定时任务、report 和前端混在第一批代码中。
