# 集采管理业务统计报表 · 本地与 test 前置测试报告

> 日期：2026-07-22（2026-07-28 追加 MT 运行验收）
> 工具：codex
> 当前状态：五种来源均已有 test 数据；MT/JD 手动历史同步、幂等、ES 对账和 report 分页读取均通过。
> 下一阶段：补测浏览器页面、数据权限账号差异、Excel 下载、菜单权限，以及 MT/JD 的 XXL-JOB。

## 测试结论

**有条件通过。** MT、JD 的“源表 → 同步接口 → ES → report 分页”链路已经通过；完整联调仍缺浏览器页面、不同账号的数据权限、Excel 下载、菜单权限和定时任务。

## 2026-07-28 · MT 煤炭 SAP 运行验收

| 用例 | 结果 | 证据 |
|---|---|---|
| 部署探针 | 通过 | 先用 2099-01-01 空窗口调用 `syncMt`，返回煤炭专属成功文案、写入 0 条 |
| 首次历史同步 | 通过 | `syncMt` 同步 2026-01-01~07-28，返回写入 503 条 |
| 数量与主键对账 | 通过 | MySQL 503 行/503 个目标主键；ES 503 行/503 个唯一 ID |
| 金额对账 | 通过 | MySQL 与 ES 含税金额合计均为 141212787.61 |
| 逐日对账 | 通过 | 北京时间 2026-01-01~01-27 共 26 个有数据日期，每日行数和金额全部一致 |
| 关键字段空值 | 通过 | ES 中企业编码、时间、板块编码、供应商名称、金额、物料编码和类目编码均 0 缺失/空串 |
| 幂等重跑 | 通过 | 同一区间重跑仍返回处理 503 条，ES 中 MT 仍为 503、全索引仍为 558 |
| report 分页读取 | 通过 | `dataSource=MT` 返回 `totalCount=503`，前两条字段映射完整 |
| 其他来源隔离 | 通过 | 同步后 FP=35、ZC=11、YC=4、JD=5，均与执行前一致 |

ES 的 `bookTime` 以 UTC 保存，例如北京时间 `2026-01-01 00:00:00` 存为 `2025-12-31T16:00:00Z`；按 `+08:00` 聚合后日期与 MySQL 完全一致，属于既有时区设计，不是少一天。

## 2026-07-27 · JD 机电四大类运行验收

| 用例 | 结果 | 证据 |
|---|---|---|
| 只执行 JD 历史同步 | 通过 | `syncJd` 同步 2026-01-01~07-27，返回写入 5 条 |
| 数量与主键对账 | 通过 | MySQL 5 行/5 个流水号；ES 5 行/5 个唯一 ID |
| 金额对账 | 通过 | MySQL 与 ES 含税金额合计均为 50345.93 |
| 关键字段空值 | 通过 | ES 中企业编码、时间、板块编码、供应商名称均 0 空值 |
| 幂等重跑 | 通过 | 同一区间重跑后 ES 仍为 5 条 |
| report 分页读取 | 通过 | `dataSource=JD` 返回 `totalCount=5` |
| 煤炭隔离 | 通过 | MT 执行前后均为 0；未调用 `syncMt` |

原有板块筛选边界已在 2026-07-27 修正：4 条 `SNJT→BU002`，1 条 `HNT→BU003`。修正前先事务试跑并回滚，确认严格命中 5 条；正式执行后重新同步 JD，数量和金额均未变化。

| 板块修正验收 | 结果 |
|---|---:|
| JD 全部 | 5 |
| `BU002` 水泥集团 | 4 |
| `BU003` 混凝土集团 | 1 |
| 旧编码 `SNJT` | 0 |
| 旧编码 `HNT` | 0 |
| MT 煤炭 | 0 |

非目标记录 `JCRJE-20260709-008`（质量公司、`SNJT`）保持原样，证明没有进行全表替换。完整执行与回滚 SQL 见 `sql/99-test-JD板块编码修正.sql`。

## 测试结果

| 项目 | 结果 | 证据/说明 |
|---|---|---|
| 前端迁移位置 | 通过 | 正确项目增加固定路由和独立页面；错误 `productmgt` 项目恢复干净 |
| 路由优先级 | 通过 | `/reportForms/centralizedProcurement` 位于 `/reportForms/:purchaseRepot` 前 |
| 前端正式构建 | 通过 | `npm run build` 退出码 0；仅项目原有 CSS 顺序、包体积告警 |
| 后端需求代码编译 | 通过 | 新模型、控制器、服务均生成 class |
| 后端全量源码编译 | 基线阻塞 | 81 个源码中只有未修改的 `SourceCockpitQueryServiceImpl.java:368` 因本地 SNAPSHOT 缺方法报错 |
| 合入 test 模拟 | 通过 | 两仓 `merge-tree` 均无冲突；差异范围分别只有 report 5 文件、前端 2 文件 |
| 采集同步接口 | 通过 | 2099-01-01 空窗口返回 `status=true`、写入 0 条，未改变 ES 现有数据 |
| 板块字典接口 | 通过 | 返回 10 个可用板块编码/名称 |
| ES mapping | 通过 | 18 个公共字段齐全，`bookTime` 支持 ISO 日期和毫秒时间戳 |
| ES 数据现状 | 通过 | 当前 FP=35、ZC=11、YC=4、JD=5、MT=503，共 558 条 |
| 企业简称筛选 | 通过 | 修正查询后，已知简称命中 4 条；不存在名称命中 0 条 |
| 供应商简称筛选 | 通过 | 已知简称命中 1 条 |
| 板块筛选 | 通过 | BU006 精确命中 1 条 |
| 日期筛选 | 通过 | 指定北京时间单日精确命中 1 条 |
| 来源+板块组合及排序 | 通过 | FP+BU002 命中 4 条，结果按 `bookTime` 倒序 |
| report 分页接口 | 通过 | test 按 JD 返回 5 条、按 MT 返回 503 条；JD 按 BU002/BU003 分别返回 4/1 条 |
| report 导出接口 | 待部署 | test 实测 HTTP 404，路径明确落到 `scm-report-web` |
| 浏览器页面与菜单权限 | 待配置 | 前端未部署，菜单尚未增加 |

## 部署后必须补测

1. 空条件分页与 4 个单条件查询。
2. 企业 + 供应商 + 板块 + 日期组合查询，核对总数和倒序。
3. 普通企业账号与平台账号的数据权限差异。
4. 导出文件可打开、15 列顺序和页面一致、时间按北京时间显示、来源和是否集采显示中文。
5. 菜单可见岗位、刷新直达路由、分页/重置/导出按钮交互。
