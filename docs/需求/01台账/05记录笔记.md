# 涉及三个模块
1. scm-source-all - API代码
2. scm-cloud-starters-web - 启动入口（不用动）
3. scm-ubm-all - 权限 菜单 数据字典配置


# 理解需求
有两块：一是新增日金额统计台账的业务表和管理接口，支持增删改查和excel导出；二是新增按统计日期汇总台账数据的刷新逻辑，通过 XXL Job 每天 24 点把前一天数据更新到集采驾驶舱。还需要配合菜单、岗位权限、字典、组织机构和导出模板配置。

# 准备思路
增加"集采日金额统计台账"模块:
1. scm-source-source-api/CollectionDailyAmountLedgerDTO 增加台账dto param等
2. scm-source-source/CollectionDailyAmountLedger 增加台账 增删改查+导出的 业务代码
3. 增加驾驶舱数据读取逻辑（这块没明白，是这里加定时任务吗，这个模块目录放哪）
4. 新增的台账表：sc_collection_daily_amount_ledger
```
ledger_id：主键
ledger_no：流水号
execute_unit_code / execute_unit_name：集采执行单位
category_code / category_name：集采类目
statistic_date：统计日期
purchase_company_code / purchase_company_name：采购企业
tax_amount：交易金额
bu_code / bu_name：所属板块
publish_time：发布时间或最近修改时间
publish_by / publish_by_name：发布或最近修改人
create_by / create_by_name / create_time：创建人和创建时间
update_by / update_by_name / update_time：更新人和更新时间
delete_sign：逻辑删除标记
```

# 已有资源
前端：http://10.0.54.16:32740/ 10930002 SSLLSX666&


# 需要/待确定内容
1. 需要测试权限的账号，用来测试“集采管理-执行单位集采管理岗”，可以查询台账

2.（下面这三个是干什么，再给我解释一下）
后端网关地址
Swagger / Knife4j 地址
XXL Job 调度中心地址
另外我问道了前后端都是用Jenkins部署，提交到test分支之后到Jenkins上点击部署。然后又输了k8s什么的，我没听懂。这个先不聊，这是部署相关，先把开发相关的流程跑通了。

3. 数据库连接方式
集团集采类目 所属板块 字典表（帮我确认一下我是不是要这个字典表数据库，还需要什么表你也给我说一下）

导出模板怎么配（待确定什么意思）

4. 组织机构控件前端返回哪些字段

需求：4、每日24：00系统按照“统计日期”自动取前一日的数据并自动更新“集采驾驶舱”；如7月2日24点，取“统计日期=7月1日”的数据；当前项目定时任务实现逻辑是什么，前端有注册定时任务入口，还是直接在Java上写死？

8.2 Service：驾驶舱有“实时汇总”和“读缓存表”两种模式没听懂，你再解释一下，让我理解了让后问同事。





已确定后端是K8S，同事说是在K8S上看报错

