1. 驾驶舱问题

2. 数据库操作流程
  新建一张台账表，是自己在test库里执行吗，数据库账号密码？
	导出模板是页面上配还是执行 SQL
	该怎么配置字典
	配菜单 配岗位找群里的张雨吗。

3. test环境怎么测接口，XXL Job 调度中心如何配定时任务


新业务表的建表 SQL 谁来执行？我发给谁，还是我自己有权限在 test 库执行？
-直接在test里执行sql，稍后拿账号密码

EXCEL导出模板是页面上配还是执行 SQL？
- 前端界面模板（待确认具体使用方法）

test 环境有没有开 Swagger/Knife4j 接口文档页面？没有的话，大家平时用什么方式直接测后端接口？
- 没有，直接Postman测，或者让AI来写测试

XXL Job 调度中心的地址和账号有吗 新任务是我自己配还是找谁配？
- 不用做定时任务，每次打开时调接口计算就行

数据字典在哪个管理页面维护？帮我看下'集采类目'（钢材/电线电缆/润滑剂/轴承及备件）和'所属板块'这两个下拉框有没有现成的字典分类，分类编码是什么？没有的话我自己在页面上新建行不行？
- 有前端界面
	
	