# 导出异常调查需求

> 安全说明：原始 Copy as fetch 中的 authorization 和 Cookie 已脱敏，不在普通文档中保留凭据。

## 执行进度

- 日期：2026-08-05
- 工具：Codex
- 当前状态：✅ 本文的“列缺失/模板缺失”问题已完成。UAT 日计划 11 列、价格趋势 9 列均正常；test 缺失/错误模板已修复。
- 根因：与 2026-08-04 的模板 ID 迁移无关；test 的 `priceTrend` 原先没有模板，`centralPurchaseStatistics` 原先只有 4 列且标题错位。
- test 修复结果：`centralPurchaseStatistics` 已为 11 列，`priceTrend` 已为 9 列；数据库主从关联、状态、排序和字段唯一性均通过。
- 文件验收：test/UAT 的两个接口均实际下载为合法 Excel 2007+ 文件；日计划首行 11 列（A∼K），价格趋势首行 9 列（A∼I）。
- 修复脚本：`sql/01-test-导出模板修复.sql`；回滚脚本：`sql/02-test-导出模板修复回滚.sql`。
- 后续补充：实际单元格的日期格式问题已转入 `02需求.md`；其中价格趋势 test/UAT 模板已继续修复，集采日统计需要 order 代码发布。

## 背景

昨天因导出模板 ID 有问题，使用以下 SQL 处理了 test 和 UAT 环境的导出模板 ID：

- `sql/03-test-导出模板ID规范化.sql`
- `sql/05-uat-导出模板ID规范化.sql`

昨天测试没有问题，今天在 test 环境发现两个导出异常；相同功能在 UAT 环境导出正常。

## 问题一：test 价格趋势维护的 Excel 格式有问题

- 环境：test
- 接口：`POST /e/business/source/priceTrend/queryPageList`
- 页面：`procurementScheme/index.html`
- 请求体：

```json
{
  "currentPage": 1,
  "limit": 10000,
  "model": {},
  "method": "POST",
  "exportRequest": "true",
  "exportFileName": "价格趋势维护",
  "exportTemplateCode": "priceTrend"
}
```

UAT 环境中同样的价格趋势维护导出正常。

## 问题二：test 集采日计划统计台账导出缺少字段

- 环境：test
- 接口：`POST /d/business/order/centralPurchaseStatistics/queryPage`
- 页面：`procurementScheme/index.html`
- 请求体：

```json
{
  "pageNo": 1,
  "pageSize": 10000,
  "model": {
    "statisticsDate": null,
    "createTime": null
  },
  "method": "post",
  "exportRequest": true,
  "exportTemplateCode": "centralPurchaseStatistics"
}
```

UAT 使用同一接口和请求体导出正常。

## 调查目标

1. 先确认是否由昨天的导出模板 ID 数据库修改导致。
2. 如果不是，继续检查 test 与 UAT 的导出模板配置、接口返回字段和代码逻辑差异。
3. 先定位根因；根因确认后，经用户授权直接修复 test 数据库配置。
