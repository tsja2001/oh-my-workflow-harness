我发现了个大问题，我看你给我准备的sql，docs/需求/04和生产环境运维对接需求1-4/sql/01-
  uat-集采四页面部署.sql，这里对于报表模板比如scm_simple_template的创建都是用sql执行的，
  但实际上该去页面里配置。带来的问题就是template_id这个字段本来是该生成的，结果变成了插
  入，和其他行的数据格格不入。你先去帮调研一下是不是现在test和uat环境的数据都是这样直
  接插入的，看一下现在id生成的规则是什么，能不能直接改，改的话要改哪几条，准备把id从什么
  换成什么。对这个模板取值逻辑上会不会因为id的更改影响逻辑，全面调研一下然后给我文档，先不要改代码
