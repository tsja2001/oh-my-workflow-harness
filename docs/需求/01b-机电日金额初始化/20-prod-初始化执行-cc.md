# 01b 机电日金额初始化 · PROD 交接包（给用户/运维执行，AI 不碰 prod）

> 日期：2026-09-04 ｜ 工具：cc ｜ 状态：**交接包就绪，未执行** ｜ 下一步：运维跑预检回传 → cc 确认后灌库 → 调度中心执行一次 → 对账
>
> 姊妹文档：`20-test-初始化执行-cc.md`（test 已通过）、`20-uat-初始化执行-cc.md`（uat 已通过，2026-09-04 三层验证全对）。
> 环境事实（均已实证）：业务库 **`scm_source_prod`**（19 指标改名部署同款）；字典库 **`scm_ubm`**（01台账 99 脚本注明）；ES 索引 **`index_centralized_procurement_report_prod`**（14 部署文档 §Nacos 配置）；同步任务「集采业务统计报表-机电四大类」在 14 部署第六节任务 #5。

## 0. 与 uat 的差异（为什么不能只改库名）

| 项 | uat | prod |
|---|---|---|
| 业务库 | scm_source_uat | **scm_source_prod** |
| 字典库 | scm_ubm_uat | **scm_ubm**（无 _prod 后缀） |
| ES 索引 | …_uat | **…_prod** |
| 组织名/板块名 | 已实查 | **未实查（AI 不碰 prod）**——02 内置的是 uat 实查值作默认，必须经预检②③比对 |
| 既有基线 | 2 行 / 18,019.68（实测） | **未知**——预检⑤实测 |
| 同步方式 | api.sh 接口 | **调度中心网页「执行一次」**（prod 无 token） |
| 驾驶舱开关 | categoryAmount=1（实测） | **未知**——预检⑥实测，7 月时 prod 是 PENDING |

## 1. 交付物（`sql/`）

| 文件 | 用途 | 执行人 |
|---|---|---|
| `01-prod-预检.sql` | 六节只读检查 | 运维 |
| `02-prod-初始化.sql` | 81 行幂等灌库 | 运维（cc 确认预检后放行） |
| `03-prod-回滚.sql` | 逻辑删 81 行 | 仅出问题时 |
| `04-prod-对账.sql` | 同步后 MySQL 四口径 | 运维 |

## 2. 执行步骤

**① 预检**：跑 `01-prod-预检.sql`，**整段结果原样回传 cc**。七节期望：
- ⓪ 库定位：ubm_dict/ubm_organization 挂在哪个库、台账表在哪个库，**以这条查询结果为准**（文档记载字典库=scm_ubm，若不一致停下回传，cc 改好再走）
- ① 八个统计日流水号占用全 0；② 六组织存在（**名字若与 02 内置不一致 → 停，回传重生成**）；③ 六板块短名；④ 类目码 4 行（若 3 行=prod 也有脏码，回传出修正脚本）；⑤ **基线行数/金额照抄回传**（若非 0 且统计日期与 2026-02~09 的月初重叠，需人工判断是否双算）；⑥ collectionShow：categoryAmount/situA 应为 1

**② 灌库**（cc 回复放行后）：跑 `02-prod-初始化.sql`，末尾自检期望 **81 ｜ 128,740,939.09**。

**③ 同步**：调度中心网页 → 找任务「集采业务统计报表-机电四大类」→ 执行一次，参数：

```
source-centralizedProcurementReportService-syncJdData-{"startTime":"2026-01-01 00:00:00","endTime":"2026-09-01 23:59:59"}
```

（每日 03:40 自动任务照常，无需改配置；ES 按 ledger_no 覆盖写，重跑不翻倍）
⚠️ 若调度中心里**找不到**这条任务（14 部署文档第六节任务 #5 是否在 prod 落地，即当初待确认 #4，一直没核实过）——停下回传 cc，按 14 文档 §六的任务定义补建后再执行。

**④ 对账**（三处一致才算通）：
```sql
-- MySQL：bash scripts/dbq.sh "$(cat docs/需求/01b-机电日金额初始化/sql/04-prod-对账.sql)" scm_source_prod 由 cc 代跑（只读）
```
- MySQL ①：81 / 128,740,939.09；②：81 + 预检⑤基线
- ES：`index_centralized_procurement_report_prod`，`dataSource=JD` 的 count = 81+基线N，sum(taxTotal) = 128,740,939.09 + 基线金额
- 门户页 procurementPortal：overview 接口四大类四格 = 预检基线 + 下表增量（单位万元）

| 类目增量 | 行数 | 金额（元） | 万元（页面显示） |
|---|---|---|---|
| 钢材 | 22 | 82,811,628.57 | 8281.16 |
| 电线电缆 | 21 | 20,112,227.72 | 2011.22 |
| 润滑剂 | 21 | 14,860,017.45 | 1486.00 |
| 轴承及备件 | 17 | 10,957,065.35 | 1095.71 |

**⑤ 回滚**（出问题）：跑 `03-prod-回滚.sql`，然后**重新执行一次 ③ 同步**（ES 覆盖写，数字即回落）。

## 3. 风险与注意

1. **驾驶舱开关（预检⑥）**：若 categoryAmount=0，驾驶舱页四格走旧缓存表（失真数，2026-01-29 后未刷新），本次数据不会体现在驾驶舱——门户 procurementPortal 页不受影响（overview 实时算 ES）。要切需产品/领导签字后执行 `docs/需求/01台账/sql/99-cockpit-switch-PENDING.sql`（执行库 scm_ubm）。
2. **单位坑**（99 脚本注明）：驾驶舱按元入、万元显示（/10000），单条 <5000 元显示 0——我们单条最小 3,057.51 元（新材-轴承 1 行）在明细里会显示 0，属显示规则，合计不受影响。
3. **日期重叠双算**：若预检⑤发现 prod 已有页面录入的机电行且统计日期与我们的 8 个月初重叠，同一笔钱会算两遍——回传明细由 cc 判断处理。
4. 类目名映射已内置（润滑油→润滑剂 1701 / 电缆→电线电缆 2302 / 轴承→轴承及备件 2502），02 里写的是字典标准干净码。

## 4. 执行记录（执行后回填）

- [ ] 预检结果回传（＿＿＿）
- [ ] cc 放行灌库（＿＿＿）
- [ ] 灌库自检 81 / 128,740,939.09（＿＿＿）
- [ ] 调度中心执行一次完成（＿＿＿）
- [ ] ES count=sum 对上（＿＿＿）
- [ ] 门户四格增量对上（＿＿＿）
- [ ] 驾驶舱开关状态 ／ 是否需要切（＿＿＿）
