# 机电日金额初始化 · 任务拆解计划

> 日期：2026-08-31 ｜ 工具：oc ｜ 状态：**拆解完成。方案 v3（定稿）：真正零代码——模板 + SQL 初始化 + 调度中心网页触发同步（复用 14 部署文档的任务 #5 机制）。待确认 6 个问题后开工** ｜ 下一步：用户拿 `11-待确认问题-oc.md` 去问产品/机电公司，答案回来后出模板和 SQL
>
> 方案沿革：v1 零代码+curl（用户无 prod token，prod 走不通）→ v2 加 XXL-JOB 处理器 → **v3 发现 14 部署文档已有现成任务和「执行一次」操作范式，v2 的代码改动取消**。

---

## ⭐ 执行进度

**已完成：**
1. ✅ 业务链路梳理完毕（本文第一部分第 2 节）——用户理解**基本正确**，有一处需要修正（见下）
2. ✅ 代码/库/ES/分支逐项核实：同步入口三个环境都在、台账表唯一键、流水号生成机制、test/uat 现状
3. ✅ 方案定型 v3：**不改任何代码**。prod 同步走调度中心网页（14 部署文档同款操作），不需要 token、不需要发版

**需要你做的：**
1. 拿 `11-待确认问题-oc.md` 的问题去问产品（重点是第 1、2 条：时间范围和采购企业维度）
2. 把初始化模板发给机电公司（模板等答案回收后我出）
3. 问运维一句：14 那份 prod 部署文档（含 5 个定时任务）执行到哪了（见待确认 #4）

**今天做不到的：** 生成正式 SQL——依赖机电公司的明细数据 + 问题答复

---

# 第一部分：写给你看的

## 1. 这个需求到底是什么

一句话：**机电公司把轴承、润滑油、钢材、电线电缆四大类的日金额明细线下整理好，我们提供模板 → 他们填 → 我们把数据灌进「集采日金额统计台账」→ 触发同步 → 驾驶舱和集采首页就有数了。**

「后台做数据初始化」= 往台账表里批量插历史数据，**不需要开发新功能**。台账的录入/查询/导出页面、后台同步接口、驾驶舱取值接口，全部是现成的，三个环境（test/uat/prod）都已部署。

## 2. 业务链路梳理（你要求补的课）

### 2.1 全链路：一笔机电日金额从录入到上屏

```
机电公司每日 10:00 前在台账页面录入（或我们这次批量初始化）
        ↓ 写入
MySQL 台账表 sc_collection_daily_amount_ledger（scm_source 库）
  关键行字段：执行单位=机电公司(编码 12300000)、类目、统计日期、采购企业、金额、板块
        ↓ 触发「机电四大类同步」接口 syncJd（按统计日期窗口捞数据）
统一 ES 货架 index_centralized_procurement_report_*（数据来源标记 JD）
        ↓ 实时聚合查询
① 集采首页 HPC（06取值需求做的）：「集团一级集采品类」里的 钢材/电线电缆/润滑油/轴承及备件 四格
② 集采驾驶舱（06_2做的）：左上七格里的四格、右上「机电公司」格、头部累计金额、日/月/季/年四个数
```

**你要的确认**：录入目的就是为了取值汇总和驾驶舱展示——**对**。补充修正一点：

> 你说「在后续开发的取值需求里做汇总，然后展示」——顺序上其实是**取值需求（06/06_2）已经上线了**，接口都在实时算，
> 只是**没有数据可算**：驾驶舱「钢材/电缆/润滑/轴承」四格和首页对应四格，**只认机电台账这一路数据**（产品定的口径：集团指定四大类由机电公司统一采购，只有机电台账记的才算统采）。
> 现在 prod 上机电公司基本没录过数（test 只有 15 条、uat 只有 2 条测试数据），所以这几格几乎是空的。
> **这次初始化就是给这几格补上历史数据**——数据一灌进去，页面立刻有数，一行页面代码都不用改。

### 2.2 台账线到现在为止的全貌（帮你恢复记忆）

| 阶段 | 做了什么 | 和本需求的关系 |
|---|---|---|
| 01台账（7月） | 「集采业务管理台账」菜单 + 日金额统计台账页面（增删改查+导出），执行单位集采管理岗操作 | **本需求的数据落点就是这张表** |
| 02台账需求2（7-8月） | 煤炭重点坑口日指标台账（另一张表 JCKK） | 无关，不要混 |
| 03采集管理报表 | 五路数据源（FP/ZC/YC/MT/JD）同步进统一 ES | **JD 路就是读这张台账表**，同步接口 syncJd 是现成的 |
| 06取值需求（8月上） | 集采首页 HPC 汇总接口 + 品类四格 | 消费方① |
| 06_2驾驶舱（8月中） | 驾驶舱四块实时计算 | 消费方②，已发 uat 和 prod |
| 15集采目录维护台账 | 售后 bug 修复 | 无关 |

### 2.3 四个类目名要对齐（细节但会出错）

| 产品这次的原话 | 系统字典（collection_category） | 编码 |
|---|---|---|
| 钢材 | 钢材 | 1501 |
| 电线电缆 | 电线电缆 | 2302 |
| 润滑油 | **润滑剂** | 1701 |
| 轴承 | **轴承及备件** | 2502 |

模板里必须写字典标准名，避免机电公司按习惯填「润滑油」对不上。

## 3. 产品原话逐条翻译

| 产品原话 | 大白话翻译 | 谁来做 |
|---|---|---|
| 机电公司日金额涉及的轴承、润滑油、钢材、电线电缆类明细金额整理出来后 | 机电公司线下把手里的四大类明细数据整理成我们能收的格式 | 机电公司（拿我们的模板填） |
| 你后台可以做数据初始化么 | 问的是能不能把历史数据批量灌进台账表——**能** | AI（生成 SQL，test/uat 执行）+ 运维（prod 执行） |
| 初始化模板你提供一个 | 给机电公司一份 Excel 填写模板 | AI 出，产品转发 |
| 让机电公司按照你的模板填写信息 | 机电公司填完发回来，我们转换灌库 | 机电公司填，AI 转换 |

## 4. 方案 v3（定稿）：四步走，零代码改动

> **v3 依据（2026-08-31）**：`docs/需求/14和生产环境对接报表取值/报表和取值部署.md` 第六节——prod 定时任务清单里 **#5 就是机电四大类同步**
> （`source-centralizedProcurementReportService-syncJdData-{时间窗JSON}`），且「手动执行一次+带时间范围参数」正是该文档写明的标准操作（UAT 已实跑验证）。
> 处理器 `simpleJobHandler` 是公共组件里的通用反射桥（`scm-assemblies-all/scm-scheduling-all/xxl-job-executor/XxlJobComponent.java:57`，
> 参数协议 `split("-",4)`＝应用-Bean-方法-JSON，日期短横线安全），**不需要为 01b 写任何代码**。

| 步骤 | 做什么 | 在哪做 | 交付物 |
|---|---|---|---|
| ① 出模板 | Excel 模板 + 填写说明（给机电公司） | - | `20-初始化模板-机电日金额.xlsx` + 填写说明 |
| ② 转换 SQL | 机电公司填回的 Excel → 校验（类目/采购企业/日期/金额）→ 幂等 INSERT SQL | AI 本地 | `sql/` 里 ①test ②uat ③prod 三个包 + 回滚 SQL |
| ③ 灌库+同步 | 执行 SQL → 触发同步：test/uat 用 `scripts/api.sh` 调现成 syncJd 接口；**prod = 调度中心网页对任务「机电四大类」点执行一次，参数填初始化时间窗**（照 14 文档第六节 #5 的格式） | test/uat 我来；prod 运维网页操作 | 网页操作说明（复用 14 文档格式） |
| ④ 对账 | 台账表行数/金额 = ES JD 条数/金额 = 驾驶舱接口四格数字，三处一致才算通 | test/uat 我来；prod 运维按对账 SQL 回查 | 对账记录 |

**前置依赖**：14 部署文档的 prod 侧（Nacos ES 配置 + 5 个定时任务）需已落地——01b 的 prod 步骤挂在它后面。

### 4.1 模板设计（**字段定稿 2026-08-31**：执行单位/类目/统计日期/采购企业/金额/填写人；xlsx 已按定稿重新生成 `20-初始化模板-机电日金额-oc.xlsx`）

三个 sheet：

| Sheet | 内容 |
|---|---|
| 1 填写说明 | 固定机电公司不用填执行单位；一行=日期×类目×采购企业（多笔自行加总）；日期范围 2026-01-01 后（待产品确认）；不合并单元格不改表头；填写人=贵司整理数据的人（存创建人展示名） |
| 2 日金额明细 | 6 列（字段定稿）：集采执行单位(预填唐山冀东机电设备有限公司到2000行)｜统计日期(必)｜集采类目(必，下拉4选)｜采购企业(必，照 Sheet3 名单)｜交易金额含税元(必，2位小数)｜填写人(必，中文名) |
| 3 采购企业参考名单 | 从 test 库 ubm_organization 导出 842 家：公司全称/常用名/所属板块，帮机电公司对名称（转换时仍以各环境库实查为准） |

**板块处理（定稿：Excel 不含板块列，全自动带）**：
- 转换脚本按 `ubm_organization.belong_bu_code` 自动带（例：冀东水泥→BU002）。
- **边界：机电公司自己 12300000 的 belong_bu_code=NULL（dbq 已核实）**——采购企业=机电公司的行进人工复核清单问板块。
**填写人处理（定稿）**：create_by_name/publish_by_name 直接存中文名；create_by/publish_by 留空（可空，导出显示的是 name 字段，同步 ES 不映射该字段）。台账导出「创建人」列显示机电公司填写人姓名——已提醒用户跟产品顺口确认。

**为什么不写导入功能/不做页面？** 你的约束是不动前端不加入口；数据初始化用 Excel→SQL 由 AI 转换比开发导入功能便宜得多。日后机电公司的日常录入走已有台账页面，每天凌晨 3:40 的定时任务（用户已配）自动同步上屏。

## 5. prod 交接为什么好描述

给运维的东西只有三样，全部是一步步照做的，且同步那步**就是 14 部署文档第六节已经写过的操作**：
1. **预检 SQL**（查 prod 台账表现有数据，确认和初始化数据不重叠）
2. **初始化 SQL**（幂等，重复执行不出错；附回滚 SQL）
3. **调度中心网页操作**：对既有任务「集采业务统计报表-机电四大类」点「执行一次」，任务参数
   `source-centralizedProcurementReportService-syncJdData-{"startTime":"2026-01-01 00:00:00","endTime":"<初始化最大统计日> 23:59:59"}`
   → 按对账 SQL 回查。**全程网页+SQL，不需要 token，不需要理解代码。**

> 前置条件：14 部署文档的 prod 侧（Nacos ES 配置 `index_centralized_procurement_report_prod` + 5 个定时任务）已落地。

## 6. 系统里现成的"样板"（都验证存在）

| 要用的 | 位置 | 验证方式 |
|---|---|---|
| 台账表结构 | `scm_source[_test/_uat].sc_collection_daily_amount_ledger` | dbq 查过，DDL 在 `01台账/sql/00-prod-deploy.sql` |
| JD 同步接口 | `CentralizedProcurementReportController.java:77` `syncJd`，支持 startTime/endTime | 代码读过；prod/uat 分支都有此文件；test ES 已有 15 条 JD 数据证明跑通过 |
| 同步过滤条件 | 只捞 `execute_unit_code=12300000`（机电公司组织编码） | `CentralizedProcurementReportServiceImpl.java:141,666` |
| 驾驶舱取值 | 钢材/润滑剂/电线电缆/轴承及备件四格 = JD 路按类目前缀聚合 | `06_2/10-会后需求对照与开发基线-cc.md` §七，代码已上 uat/prod |

## 7. 需要你去确认的事

见 `11-待确认问题-oc.md`，6 个问题：时间范围、采购企业维度（最重要）、prod 现有数据、日常同步谁触发、审计字段显示、模板粒度。

## 8. 你需要知道的背景知识

- **台账表的一行** = 某「统计日期」×某「类目」×某「采购企业」的一笔金额（执行单位固定机电公司）。机电公司给你的「明细」可能是每笔订单级的，我们转换时会聚合成这个粒度（除非答复说保持明细行）。
- **板块是带出来的**：采购企业定 → 板块就定（组织表里每个公司挂着所属板块，如唐山分公司→BU002）。所以模板里机电公司**不填板块**，我们按采购企业自动带，填了也不认。
- **驾驶舱只算 2026 年的**：年度/累计的起点写死 2026-01-01。机电公司如果整理出 2025 年的数据，灌进去页面也不显示——所以要先问清数据从哪天开始。
- **幂等**：SQL 按唯一键写，重复执行不产生重复数据；同步接口按流水号覆盖写 ES，重跑也不会翻倍。这是「prod 好交接」的技术底座。

## 9. 整件事的推进顺序（你的行动清单）

1. 去 `11-待确认问题-oc.md` 拿问题问产品（1、2 最急）
2. 答案回来 → 我出模板 → 你/产品发给机电公司（无发版、无代码）
3. 机电公司填好发回 → 我做校验+转换 → test 灌库同步对账 → 给你看驾驶舱数字变了
4. uat 同样跑一遍
5. 我出 prod 交接包（预检 SQL + 初始化 SQL + 回滚 SQL + 调度中心操作步骤 + 对账 SQL，同步步骤照 14 文档格式）→ 你转运维
6. 运维执行完回查 → 收口，工作日志+INDEX 更新

---

# 第二部分：给执行 AI 的技术方案

## 0. 总体纪律

1. **开发范围 = 不改任何前后端代码、不新增任何定时任务。** 产出物只有：Excel 模板、SQL 脚本、网页操作说明、文档。prod 同步复用 14 部署文档已定的任务 #5（机电四大类）「执行一次」范式。如发现必须改代码/加任务才能走通，先停下来写进待确认，不要顺手改。
2. 调度机制参考（都已核实）：通用处理器 `scm-assemblies-all/.../xxl-job-executor/XxlJobComponent.java:57` `@XxlJob("simpleJobHandler")`；参数协议 `XxlJobExecutorServiceImpl.java:28` `split("-",4)`＝应用-Bean-方法-JSON；路由走 AIM（`scm-share-aim-all`）；prod 任务清单见 `docs/需求/14和生产环境对接报表取值/报表和取值部署.md` 第六节。
3. 所有 SQL 进本主题 `sql/` 目录，按团队规矩分环境存档，可重放可回滚。
4. 金额单位元、decimal(20,2)；不硬编码任何组织编码进模板（模板给名称，码由转换脚本查库落）。

## 1. 已核实的技术事实（每条都有出处）

| # | 事实 | 出处 |
|---|---|---|
| 1 | 同步只认 `execute_unit_code='12300000'`（唐山冀东机电设备有限公司），**test 表里 execute_unit_code=JDGS/ZLGS 等杂码的行永远不进 ES** | CentralizedProcurementReportServiceImpl.java:141,666 |
| 2 | ES 文档 id = ledger_no → 同步是覆盖写，重跑幂等 | 同上 :685 |
| 3 | 台账表 `uk_ledger_no` 唯一键 → INSERT 撞号会报错（好事，不静默） | 01台账/sql/00-prod-deploy.sql:55 |
| 4 | 页面录入的流水号中段是**录入当天**日期（JCRJE-录入日-NNN），GID 序列自增 | CollectionDailyLedgerCodeSequence.java getCenterStr()=new Date() |
| 5 | bookTime（驾驶舱聚合的时间字段）= statistic_date，且 @TableLogic 过滤已删除行 | 同步实现 :714、Model :119 |
| 6 | 驾驶舱/首页年窗起点写死 2026-01-01 → 2025 数据不显示 | 06_2 交接 §五 |
| 7 | 同步接口三环境都在：test/uat/prod 分支均有 Controller（prod 提交 4b8b7d1d3） | git cat-file 验证 |
| 8 | test 现状：机电 15 行(07-01~08-06, 43.39万)，ES JD=15；uat：机电 2 行(06-30/07-27)，ES JD=2 | dbq+esq 2026-08-31 |
| 9 | 定时任务机制：`simpleJobHandler` 通用反射处理器在**公共组件** scm-assemblies-all（不在业务仓，搜业务仓会漏）；参数协议 `split("-",4)`；UAT 已配 5 个每日任务（用户操作，含机电四大类 03:40）；「执行一次+带时间窗 JSON」在 14 prod 部署文档中是既定操作 | XxlJobComponent.java:57、XxlJobExecutorServiceImpl.java:28、14部署文档§六 |
| 10 | 日常台账页面入口/菜单/岗位已上线（01台账交付物），无需动 | 01台账线交付记录 |

## 2. 初始化 SQL 生成规则

```
ledger_id    : 32位内唯一（雪花或 'INIT'+序号，避开现有格式即可，无业务含义）
ledger_no    : JCRJE-<统计日期yyyyMMdd>-<三位序>，序号接在该日期现有最大值之后
               （与页面录入的「录入日中段」天然错开；统计日期一律 ≤ 昨天，杜绝与当天页面录入撞段）
execute_unit : code=12300000, name=唐山冀东机电设备有限公司（固定）
category     : 明细名称→字典映射（润滑油→润滑剂，轴承→轴承及备件），映射失败进人工复核清单
statistic_date: 仅接受 2026-01-01 ~ 昨天
purchase_company: 名称→ubm_organization 查 org_code + short_name（先精确后模糊），查不到进复核清单
bu_code/name : 采购企业对应 belong_bu_code/name（不许手填）
tax_amount   : 元，两位小数
publish_time : 初始化执行时刻；create_by_name/publish_by_name=Excel 填写人中文名，create_by/publish_by 留空（已定，见 11 答复区 #5）
幂等          : INSERT ... ON DUPLICATE KEY UPDATE（重放只更新不翻倍）
回滚          : 按本次生成的 ledger_no 清单 DELETE（物理删）或 UPDATE delete_sign=1（逻辑删，推荐后者）
```

## 3. 同步与对账（每环境三步）

```bash
# ① 触发（test/uat 用现成接口；prod 用调度中心网页「执行一次」，见 §0 第 2 条）
bash scripts/api.sh POST /e/business/source/centralizedProcurementReport/syncJd \
  '{"startTime":"2026-01-01 00:00:00","endTime":"<昨天> 23:59:59"}'
# ② 对账一：MySQL vs ES
bash scripts/dbq.sh "SELECT count(*), sum(tax_amount) FROM sc_collection_daily_amount_ledger
  WHERE delete_sign=0 AND execute_unit_code='12300000'" scm_source_test
bash scripts/esq.sh search index_centralized_procurement_report_test '<dataSource=JD 的 count+sum agg>'
# ③ 对账二：驾驶舱接口
bash scripts/api.sh GET /e/business/source/source/cockpit_collection_data_categoryAmount
#    四格增量 = 初始化的分组金额；situA2(机电公司) 同步涨
```

prod 交接包给运维同样的三条（SQL 版+curl 版），并附「expected 数字表」让他执行后比对。

## 4. 待确认参数占位

- 初始化起始日期：＿＿＿（等产品/机电公司答复）
- 采购企业维度缺失时的默认口径：＿＿＿
- 审计字段（create_by_name/publish_by_name）显示：＿＿＿（默认「数据初始化」）
- 明细是否聚合成 日×类目×采购企业：＿＿＿（默认聚合）

## 5. 风险与对策

| 风险 | 对策 |
|---|---|
| 机电公司填的采购企业名称和组织表对不上 | 转换脚本输出「未匹配清单」退回人工确认，宁停勿猜（红线5） |
| prod 台账已有正式录入，与初始化日期重叠 | 交接包含预检 SQL；重叠日期跳过并入人工确认清单 |
| 数据量大（预估 200 天×4类×N公司） | SQL 分批 INSERT；同步接口本身分批写 ES |
| 「润滑油/轴承」习惯名填错类目 | 模板用下拉数据有效性限制四选一；转换时再校验一遍 |
| 14 的 prod 部署未落地（ES 索引/任务未配） | 待确认 #4 先问清状态；01b prod 步骤排在它之后 |

## 6. 自检（Phase 1）

- 样板路径全部验证存在 ✅（表 DDL、Controller:77、ServiceImpl:141/666/685、序列类）
- 每条产品原话都有「谁来做」✅（第一节翻译表）
- 清楚/模糊二分 ✅：清楚=模板+SQL+同步+对账（可立即做）；模糊=时间范围/采购企业维度/prod现状/审计口径（进 11）
