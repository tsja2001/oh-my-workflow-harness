现在有个更改需求，说“/business/source/collectionDailyAmountLedger/queryOrgForSelect”这个接口返回的orgName太长了，字段应该取错了，应该有个稍微短一点的字段能取，
queryOrgForSelect这接口应该有两个，你研究一下是不是逻辑共用的，你先不修改，直接去数据库找对应的字段应该是哪个，然后告诉我在哪个库哪个表里。现在是哪个字段，应该改成哪个字段

根据项目根目录里的skill完成这个操作
