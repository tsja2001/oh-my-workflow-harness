驾驶舱：
1. 历史数据怎么办：屏幕上这些是累计值（比如钢材 23350 万），台账是新上的、从零开始。切换后是"老数据当底数，台账往上累加"，还是"只看台账、从零重新算"？
只看台账，之前的数据不要了

2. 中间那排全集团的数（日集采/月度/季度/年度/累计总金额）：它们里面包含四大类目的部分，要不要也跟着改成从台账取？不改的话，同一个屏幕上"分项数"和"总数"会对不上；改的话，要确认原来的自动数据还进不进老表，避免同一笔钱算两遍。
这个接口需求里有说吗，必须处理这一个吗，我还没问领导

3. 导出模板，有专门的前端界面可以配置。
我看到需要输入下面的信息：
文件类型
文件编号
文件名称
文件描述
版本号
上传文件
备注
状态启用
停用

比如我点开一个已经有的模板：
文件类型	下载模板	
文件编号	collaborationItemImport
文件名称	协同事项文件模板	
文件描述	协同事项文件模板
版本号	1	
文件链接	协同事项文件模板http://10.0.54.16:30370/gateway/obs/dowload/file/view/e517df64-7fe0-4a46-acd4-f44dc6b4334e.xlsx
备注		状态	启用

这个是是不是你告诉我传入什么，我自己手动传入就行，你也参考好了数据库现有的数据，剩下的给我来添加就行了。

4. 字典不用SQL：前端可以直接添加
我看到界面上有很多分类，分类还能树状展开比较复杂
然后每个分类下有有几个字典类型，可以添加；还有字典值可以添加，看上去也都挺复杂的。
你是不是可以进到数据库里看清楚数据，然后告诉我怎么添加

5. test 环境怎么测接口："test 环境有没有开 Swagger/Knife4j 接口文档页面？没有的话，大家平时用什么方式直接测后端接口？"
都没有，直接让AI写点测试方案，curl或者postman测试

6. 关于数据库执行SQL
直接连上测试数据库就行
（连接信息已统一移至 ai-docs/creds.env，不进 git）

7. 定时任务
那个不用定时任务算，直接前端请求后实时计算就行，规则上符合需求的逻辑就行。
