
### 1. 云采 YC

```bash
curl --insecure --connect-timeout 10 --max-time 1800 \
  -X POST '<生产云采网关地址>/d/business/order/centralizedProcurementReport/syncToEs' \
  -H 'Authorization: bearer <云采生产TOKEN>' \
  -H 'Content-Type: application/json' \
  --data-raw '{"startTime":"2026-01-01 00:00:00","endTime":"<上线前一天> 23:59:59"}'
```

### 2. 非平台采购 FP

```bash
curl --insecure --connect-timeout 10 --max-time 1800 \
  -X POST '<生产招采网关地址>/e/business/source/centralizedProcurementReport/sync' \
  -H 'Authorization: bearer <招采生产TOKEN>' \
  -H 'Content-Type: application/json' \
  --data-raw '{"startTime":"2026-01-01 00:00:00","endTime":"<上线前一天> 23:59:59"}'
```

### 3. 招采平台 ZC

```bash
curl --insecure --connect-timeout 10 --max-time 1800 \
  -X POST '<生产招采网关地址>/e/business/source/centralizedProcurementReport/syncZc' \
  -H 'Authorization: bearer <招采生产TOKEN>' \
  -H 'Content-Type: application/json' \
  --data-raw '{"startTime":"2026-01-01 00:00:00","endTime":"<上线前一天> 23:59:59"}'
```

### 4. 煤炭 SAP（MT）

```bash
curl --insecure --connect-timeout 10 --max-time 1800 \
  -X POST '<生产招采网关地址>/e/business/source/centralizedProcurementReport/syncMt' \
  -H 'Authorization: bearer <招采生产TOKEN>' \
  -H 'Content-Type: application/json' \
  --data-raw '{"startTime":"2026-01-01 00:00:00","endTime":"<上线前一天> 23:59:59"}'
```

### 5. 机电四大类 JD

```bash
curl --insecure --connect-timeout 10 --max-time 1800 \
  -X POST '<生产招采网关地址>/e/business/source/centralizedProcurementReport/syncJd' \
  -H 'Authorization: bearer <招采生产TOKEN>' \
  -H 'Content-Type: application/json' \
  --data-raw '{"startTime":"2026-01-01 00:00:00","endTime":"<上线前一天> 23:59:59"}'
```
