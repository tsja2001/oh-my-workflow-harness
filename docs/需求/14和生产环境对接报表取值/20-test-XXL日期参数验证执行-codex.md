# test XXL-JOB 日期参数验证 · 执行记录

> 日期：2026-08-19
> 工具：codex
> 当前状态：test 报表 ES 的五类业务文档已备份并清空，等待先用 ZC 单任务验证带日期的 XXL-JOB 参数。
> 下一步：用户在 test XXL-JOB 手动执行 ZC；执行后先核对任务日志和 ES 数量，成功才继续其余四路。

## 1. 清理前事实

- 目标索引：`index_centralized_procurement_report_test`
- 总数：609
- 分来源：`MT=503`、`YC=45`、`FP=34`、`JD=15`、`ZC=12`

## 2. 备份与清理结果

- 备份索引：`index_centralized_procurement_report_test_backup_20260819_before_xxl_param_test`
- `_reindex` 结果：读取 609、创建 609、失败 0；备份索引复查为 609 条。
- 原索引仅按 `dataSource in (YC,FP,ZC,MT,JD)` 执行 `_delete_by_query`：命中 609、删除 609、失败 0。
- 清理后原索引复查为 0 条；原索引和 mapping 均保留，未删除索引。

## 3. 验证顺序

1. 先只执行 ZC，时间范围 `2026-01-01 00:00:00` 至 `2026-08-18 23:59:59`。
2. 检查 XXL-JOB 执行结果和业务日志中的实际时间范围。
3. 复查 ES 中 `dataSource=ZC` 是否恢复；预期基线为 12 条，但最终仍以执行时源库实际查询结果为准。
4. ZC 成功后再逐路执行 YC、FP、MT、JD；任一路失败立即停止后续执行。

## 4. 恢复办法

如果日期参数验证失败且需要恢复原现场，可把备份索引 609 条重新 `_reindex` 回目标索引；目标索引 mapping 仍在。恢复前先复查目标索引现有数量，避免覆盖验证期间新写入数据。

## 5. 第一次手动验证及再次清理

- 2026-08-19 15:28:52、15:29:30，用户确认误执行了两次 ZC；调度库两次记录均为 trigger 200、handle 200。
- 两次执行后目标索引为 `ZC=24`，此时无法用结果判断单次应写入多少。
- 再次按五个 `dataSource` 清理：清理前 24、删除 24、失败 0；清理后目标索引 0，原备份索引仍为 609。
- 下一步只执行一次 ZC，再复查 ES 数量和执行记录。
