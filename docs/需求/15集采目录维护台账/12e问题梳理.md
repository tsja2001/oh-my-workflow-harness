## 1. 生产环境初始化
是一刀切，全部 层级 = 集团集采，强管控 = 否，还是按类目处理？

要不要把生产的数据搬到uat里？

答案：初始化成 层级 = 集团集采，强管控 = 是

## 2. 现在uat的7000多条数据，层级和强管控初始化成什么
层级 = 集团集采，强管控 = 是？

## 3. 弱管控 + 集团集采怎么办
能改，原流程走


## 4. 二级集团/区域层级的单子，谁做采购方案
现在集团集采是派给对应的集采公司（机电公司） =》 计划
二级集团/区域层级 就本企业自己做


## 5. 判重算法
类目 + 物料 + 层级 + 影响范围？

170101 冀东水泥配一条、唐山区域也配一条，唐山的企业提单听谁的？

范围越窄越优先（区域 > 二级集团 > 集团）？


## 6. 
- 集团配了 170101，二级集团能不能配上级的 1701？

  理解：能。匹配从下往上找、找到就停，走不到 1701（逻辑没问题）。

- 影响范围填了多个 BU，"向上一级报到二级集团"报给谁？
  报提单企业自己所属的那个 BU?

- Excel 导入 100 行有 3 行冲突，整批打回还是只拦那 3 行？
  只拦冲突行？
