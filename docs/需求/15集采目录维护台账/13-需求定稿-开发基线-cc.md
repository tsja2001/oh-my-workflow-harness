# 13 需求定稿 · 开发基线

> 📌 **【2026-09-08 晚】产品当面对完需求，答案在 `12f-产品录音答案回收-cc.md`。§1 已逐条标注【已确认】/【推翻】。**
> **§1 D1（判重）、D24（期初强管控）、D10（向上一级报谁）被推翻，按 `12f` §1 做；导入冲突改成整批打回。**
> **上线时间已改：国庆前 → 国庆后；测试单独留 9/21~9/24 一周。§7 排期按 `12f` §5 修正。**
>
> 📌 **【2026-09-08】§4/§5/§7（两周任务清单与排期）已重梳理进 `13c-两周总览-任务与影响范围-cc.md`，看那一份即可。**
> 本文其余部分（§1 默认决议、§2 建表、§3 接口契约、§6 分支提交、§8 上线回退、§9 不做范围）**仍然有效**。

> **日期**：2026-09-08 ｜ **工具**：cc
> **当前状态**：用户要求「待确定的先按你认为的默认」。本文**把 22 个待定问题全部按默认拍板**，
> 加上完整的建表、接口契约、任务清单、排期和上线方案。**此后按本文开发。**
> **下一步**：动手前先跑 `13b` §1 的三件准备（基线快照 / 数据体检 / 变量名给华姐）；
> 产品答案回来后，只改本文 §1 那张表，并在改动处标 `【已确认 日期】` 或 `【推翻 日期：新口径】`。
>
> ⚠️ **本文是"按默认值定稿"，不是"产品已确认"**。§1 每条都标了「猜错的返工成本」——
> 成本高的那几条仍然值得抢在开工前问掉。
>
> 依据：需求原件 `01c`；影响面 `10w`；四个影响点 `10v`；工程防线 `10u`；录入规则 `10q`；业务流程 `10r`。

---

## 0. 怎么用这份文档

| 你想干什么 | 看哪一节 |
|---|---|
| 知道某个没定的事按什么做 | §1 默认决议表 |
| 建表 / 写 SQL | §2 |
| 改接口 / 对接华姐 | §3 |
| 今天该干哪一件活 | §4（第一周）、§5（第二周）、§7（按天排期） |
| 开分支 / 提交 / 发版 | §6 |
| 上线当天做什么、出事怎么退 | §8 |
| 这次**不做**什么 | §9 |
| 产品改主意了要返工多少 | §10 |

---

## 1. 默认决议表（22 条，全部拍板）

### 1.1 🔴 开工闸门级（4 条，猜错要推倒重来）

| # | 问题 | **默认决议** | 依据 | 猜错的返工成本 |
|---|---|---|---|---|
| **D1** | 什么算「重复」（`10q` Q1、`12c` 问题1） | 🔴 **【推翻 2026-09-08：口径见 `12f` §1】**<br>~~四字段全同才算重复：`类目码 + 物料码 + 集采目录层级 + 影响范围`~~<br>**新口径（两段式）**：① 某类目/物料被「集团集采」占过 → **该类目及其下级**任何层级都不许再维护；<br>② 非集团集采的，判重粒度是**板块（BU）**——同板块内同类目/物料不许重复，**跨板块可以**；<br>③ **区域配的规矩要先换算成它所属的板块**再判重；④ 四字段完全相同当然也不允许。<br>仍然只跟「状态=生效」的行比，失效行**不占**位 | 产品当面口述（录音 12:42~15:07、**19:08~20:22**） | 中。仍收敛在 `isDuplicate` 一个方法。**"区域→板块"依赖已验通**（2026-09-08 实查 uat：`ubm_organization_manage.belong_plate_code → belong_bu_code`，36 个区域全部 1:1 归属唯一板块，0 个跨板块；细节与三个红灯见 `12f` §8） |
| **D2** | 同一节点多条规矩命中，听谁的（`10q` Q2、`12c` 问题2） | ✅ **【已确认 2026-09-08】**<br>**「越具体越优先」，两个维度分开：**<br>**① 纵向**（类目深浅）：物料码 → 末级类目 → 逐级向上，**找到就停**<br>**② 横向**（同一节点多条）：**影响范围越窄越优先** = 区域·企业 > 二级集团 > 集团<br>同层级再取 `create_time` 最新 | 6.2 原文就是纵向逐级；横向沿用同一套心智模型。3.2 已保证"集团配了别人不能配"，实际只有 二级集团 × 区域 会撞 | 中。收敛在 `pickOne(命中列表)` 一个方法 |
| **D3** | 「最小层级」是哪个方向（`10r` P10） | ✅ **【已确认 2026-09-08】数字最小 = 集团集采**（集团1 > 二级集团2 > 区域3，取数字最小的那个 = 走最严的）<br>⚠️ **术语对照**：需求文档写「取**最小**层级」、产品嘴里说「取**最高**层级」，**是同一件事**（都=集团集采）。开会别在这个词上对不齐 | 产品原话（录音 17:32~18:23）：「A 集团级、B 二级集团、C 区域……**按集团**吧」「这个选择最高层级」<br>＋ `OrganizationLevelEnum` 先例 `JT=1, GS=2, BS=3` | 小。一个比较器 |
| **D4** | 二级集团层级的单子，本企业能不能自己做方案（`10r` P1） | ✅ **【已确认 2026-09-08】**（产品原话：二级集团/区域「不需要集采执行单位审核，只需要企业自己审核完就 OK」，录音 10:47~11:34）<br>**能。采购方案那道拦截，改成只对「集团集采」生效。**<br>二级集团 / 区域层级 → 本企业自己做方案 | §5 明写"二级集团、区域层级无需配集采执行单位"；不填执行单位又拦住 = 业务死局 | 小（一个 if），但**不改就是上线后没人能接单** |

> ⚠️ **D2 和 D3 方向相反，最容易搞混，务必分清：**
>
> ```
> D2 匹配优先级  ── 一行物料命中了两条规矩，用哪条？  → 范围越窄越优先（区域赢）
> D3 表头取最小  ── 一张单子多行物料，表头记哪一级？  → 层级越高越优先（集团赢）
> ```
> 前者是「**这条物料归谁管**」，后者是「**这张单子按谁的规矩走审批**」。**两个问题，两套逻辑。**
>
> 📌 **订正**：`10v` §1.3 里把 D2 写成了"层级越高越优先"，那是把 D3 的答案套到了 D2 上。**以本表为准。**

### 1.2 🟡 影响开发方式（8 条）

| # | 问题 | **默认决议** | 理由 |
|---|---|---|---|
| **D5** | 判重用不用数据库唯一索引 | **不用。删掉 `range_product_idx`，判重全放应用层** | MySQL 唯一索引**不约束 NULL**，而"只管类目、物料码为空"恰恰是最常见的行；且索引表达不了"只跟生效的比"（`10u` 防线四） |
| **D6** | 软删怎么做 | **`delete_sign` + `@TableLogic`（框架兜底）＋ `status`/`invalid_time`（页面显示）**，两套并存 | 全仓 70 处已在用 `@TableLogic`；靠人逐处加过滤防不住第八处（`10u` 防线一） |
| **D7** | 台账列表怎么翻出失效行 | **列表分页那一处写自定义查询绕过 `@TableLogic`**，其余全部走框架 | 唯一需要看到历史的地方 |
| **D8** | 类目放开到末级，要不要做树选择器 | **第一周不做。** 只去掉导入的"必须二级"校验 | 台账目前只有导入一个入口，类目码从 Excel 来；**现有树接口写死 ≤4 位且 20+ 页面共用，绝不能改**（`10w` §5.1） |
| **D9** | 「区域·企业」这一档 | ⚠️ **【待重新确认 2026-09-08】** 原默认：字段和逻辑都做、开关控制、第一版关闭、「企业」那半边不做。<br>**但产品这次把区域讲得很细**（区域编码从组织机构取、按编码检索区域下企业清单、区域要换算成板块判重），**像是这期就要能用**。<br>👉 **下次见面必须问一句**（话术见 `12f` §7 第 2 条），问到之前**仍按原默认做** | **【订正 2026-09-08 实查 uat】不是 6 个 BU 为空，是 13 个板块里 9 个完全为空**；区域字段全表填充率仅 **27%**（854 个组织只有 232 个填了）。<br>真正在用的只有 **BU002 水泥集团**（27 个区域/137 家企业），BU004/BU007 填的还是公司名不是区域。**拿这组数字去问产品**（`12f` §8） |
| **D10** | 影响范围能填几个 | **允许多个**，沿用现有 `/` 分隔存法。<br>🔴 **【推翻 2026-09-08】** ~~审批上报取「提单企业自己所属的那个 BU」~~ → **「向上一级」= 报金隅集团（总部）**，不在影响范围那几个 BU 里挑（录音 27:49~28:41）。<br>**我这边不用改代码**（分支是流程侧配的），但别照旧表述往下传<br>📌 **填值形态（产品口述，录音 21:56~24:35）**：集团=全集团；二级集团=**板块编码**（如 `BU002`）；区域=**区域组织编码**（组织机构里维护的"所属区域"编码），页面显示区域名 | 字段和存法现成，不用改表 |
| **D11** | `bu_code` 字段长度 | **放长到 `varchar(2000)`** | 现在 256，`/` 分隔最多约 28 个码。放长几乎零成本，省掉一次改表（`10u` 防线四） |
| **D12** | 「启用时间」做不做 | **不做独立字段，用 `create_time` 当启用时间** | "管后不管前"用创建时间够了；真要预约生效得加状态机+定时任务，是另一个量级 |

### 1.3 🟡 影响页面和交互（6 条）

| # | 问题 | **默认决议** | 理由 |
|---|---|---|---|
| **D13** | 混采到底允不允许（`10r` P2） | **保持禁止。** 弹框改成明确拦截、可关闭（`$message.warning` + `return`），**去掉那个永不 resolve 的 Promise** | 整单只有一个「是否集采」的章，混采说不清；这是现有行为，动静最小 |
| **D14** | 弱管控改成否，走原流程还是新流程（`10r` P3） | ✅ **【已确认 2026-09-08】走原流程，不起新流程**（录音 03:44~04:01、08:50~08:58）。<br>**「弱管控 + 集团集采」这一格也归原流程**，6.5 不用为它单独加分支 | 弱管控的定义就是"改了没代价"；改了也要多审一级的话，弱管控和强管控就没区别，三条分支塌成两条 |
| **D15** | 采购计划加不加那两个字段（`10r` P7） | **加。三个编制页 + 三个详情页全加** | 章是在计划提交那一刻盖的，计划不算值后面全是空的；6.3/6.4 漏写采购计划是笔误 |
| **D16** | 详情页要不要加（`10w` §3 新发现） | **必须加。** 三个详情页各加 2 个只读字段 | **审批人打开的是详情页**；不加的话分支②那级审批看不到自己为什么收到这张单 |
| **D17** | 新加的两个字段只读还是可改（`10r` P9） | **只读** | 系统按台账算出来的事实，采购员能改的只有「是否集采」一个框，且只能是→否 |
| **D18** | 改成否要不要填理由（`10r` P5） | **要，必填。** 加一个文本框 + 一列 | 那是分支②那级审批**唯一的判断依据**，不加等于白设 |

### 1.4 🟢 其余（4 条）

| # | 问题 | **默认决议** |
|---|---|---|
| **D19** | 待采购池带不带这两个字段（`10r` P8） | **带。** 机电公司清单页要能看出层级和强弱来排优先级；方案从池子挑行时值直接带过来比重算可靠 |
| **D20** | 改成否之后层级章还留吗（`10r` P6） | **留着。** 层级是台账定的客观事实，是否集采是这张单的结果，两件事 |
| **D21** | 台账补不补新增/编辑（`10r` T1） | **这期不补。** 导入是纯追加，改一条 = 删一行 + 导一行。补了要连带做树选择器（D8），不划算 |
| **D22** | 三级层级下台账谁维护（`10r` T3） | **这期仍总部统一录，不下放。** 下放要做数据权限，是独立功能，不在两周排期里。**这句话要当场跟产品说明白** |

### 1.5 期初与数据（3 条，`10q` Q7 + `10w` 新发现）

| # | 事 | **默认决议** |
|---|---|---|
| **D23** | 7343 行的层级填什么 | ✅ **【已确认 2026-09-08】统一填「集团集采」** |
| **D24** | 7343 行的强管控填什么 | 🔴 **【推翻 2026-09-08】~~统一填「否」~~ → 统一填「是」**（产品原话：「我得是是 ok」「我肯定是要试呀」，录音 01:28~03:18）。<br>"上线当天全集团审批链一起变长"的风险已当面说过，产品接受。<br>🔴 **而且形态变了**：不是 UPDATE 补两列，而是**产品自己把 7000 条导出来、在 Excel 里填好值、再导进来**。<br>👉 我要出的是**导出模板 + 导入通道**（导出现在有 bug，得先修，见 `12f` §6 待办 5）；UPDATE 脚本仍留一份兜底 |
| **D25** | 🆕 `bu_code` 里的错编码 | **期初脚本必须修**：`BU0013` → `BU013`，并**补上 `BU012`**。<br>不修的话第二周影响范围过滤一开，这两个 BU 下 8 个组织的单子**静默判为非集采**（`10w` §5.2） |

### 1.6 ⚠️ 三条"默认值救不了、必须要到东西"的

| # | 要什么 | 为什么默认值不行 |
|---|---|---|
| ~~**X1**~~ | ~~**生产上现在实际管哪些类目的清单**~~ | ✅ **【解除 2026-09-08】不用要清单了**：产品会自己把生产 7000 多条导出来填好值再导进去（`12f` §2 ①）。<br>UAT 数据不全是产品已知并认可的；**上线前把生产数据同步一份到 UAT 用于验证**（产品已答应），这条排进上线准备清单 |
| **X2** | **三个工作流变量的最终命名** | 画分支图要用。我给了建议名（§3.2），**但必须跟流程侧对一次**。<br>⚠️ **【订正 2026-09-08】** 录音里产品点名的是**小韩**研究审批流（41:46），不是华姐。**发之前先问产品发给谁**。<br>📅 产品**周四 9/10** 给审批流逻辑文档；变量名**今天就发**，不等代码 |
| **X3** | **报表口径变更要提前通知使用方** | 第二周换判定口径 = 换了集中采购报表的分母（`10w` §2）。这不是技术问题，是"别让领导先发现" |
| **X4** | 🆕 **在途业务会不会被新流程变量搞坏**（**产品主动提的**，2026-09-08） | 产品说他们踩过一次坑：加了个金额字段控制审批流，**只配给了地产却影响了其他板块**，怀疑是**缓存**。<br>👉 **去问林展**这个缓存怎么回事；**小华会留一条在途测试流程**，部署后先拿它测再投新流程（`12f` §4.5、§7）。<br>⚠️ 这**推翻了 `10r` P11「在途单据照旧走完、不受影响」的乐观假设** |

### 1.7 🆕 录音带回来的其它口径（2026-09-08）

| 事 | 口径 | 出处 |
|---|---|---|
| **审批分支谁实现** | **全部由产品/流程侧配置**，后端只负责把 3 个变量算准、塞进流程上下文。产品说了三遍「影响是我们前台配置的事」「你得给我传值」「我自己去做判断」 | 录音 09:00~10:23、11:45~12:38、28:27~28:41 |
| **二级集团/区域算不算集采** | **算**。三档都是「是否集采=是」，区分靠新增的「集采层级」变量 | 录音 11:45 |
| **导入冲突怎么处理** | 🔴 **整批打回**（沿用平台标准导入行为）。~~原默认：只拦冲突行~~ | 录音 28:41~29:48 |
| **集团配了下级，二级集团能否配上级** | **能**。集团配 `170101` + 水泥配 `1701` → `170102` 归水泥、`1701010001` 仍归集团。产品要把这个场景写进他文档 5.6 | 录音 26:17~27:34 |
| **四个新字段** | 确认就是：集采目录层级、是否强管控、失效时间、状态 | 录音 30:07~30:43 |

---

## 2. 数据库设计

### 2.1 台账表 `sc_plan_product_range`（第一周，test + uat 都要跑）

```sql
-- ① 新增业务列
ALTER TABLE sc_plan_product_range
  ADD COLUMN collection_level      VARCHAR(16)  NULL COMMENT '集采目录层级 1集团集采 2二级集团集采 3区域企业集采',
  ADD COLUMN is_strong_control     TINYINT(1)   NULL COMMENT '是否强管控 1是 0否',
  ADD COLUMN status                VARCHAR(8)   NULL COMMENT '状态 1生效 0失效',
  ADD COLUMN invalid_time          DATETIME     NULL COMMENT '失效时间';

-- ② 补齐本仓库标准审计列（这张表现在连这两个都没有）
ALTER TABLE sc_plan_product_range
  ADD COLUMN update_by             VARCHAR(32)  NULL COMMENT '修改人',
  ADD COLUMN update_by_name        VARCHAR(64)  NULL COMMENT '修改人姓名',
  ADD COLUMN update_time           DATETIME     NULL COMMENT '修改时间',
  ADD COLUMN delete_sign           TINYINT(1)   NOT NULL DEFAULT 0 COMMENT '逻辑删除 0未删 1已删',
  ADD COLUMN data_version          INT          NOT NULL DEFAULT 1 COMMENT '数据版本';

-- ③ 影响范围放长（D11）
ALTER TABLE sc_plan_product_range MODIFY COLUMN bu_code  VARCHAR(2000) NULL COMMENT '影响范围编码，/分隔';
ALTER TABLE sc_plan_product_range MODIFY COLUMN bu_name  VARCHAR(4000) NULL COMMENT '影响范围名称，/分隔';

-- ④ 删掉唯一索引（D5）—— ⚠️ uat 有，test 没有，脚本要能容错
--    uat：DROP INDEX range_product_idx ON sc_plan_product_range;
--    test：这条会报 1091 错，属正常，跳过
```

**⚠️ 三条执行注意**

1. **test 和 uat 状态不一致**（`10w` §6.3）：test 没有 `range_product_idx`。
   DDL 脚本必须**先查再删**，不能直接 `DROP INDEX`：
   ```sql
   SELECT COUNT(*) FROM information_schema.STATISTICS
    WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME='sc_plan_product_range'
      AND INDEX_NAME='range_product_idx';
   -- 结果 > 0 才执行 DROP INDEX
   ```
2. **新列全部 NULL**，不加 `NOT NULL`——先上表结构、再刷数据、最后加校验，中间任何一步都不会卡住线上。
3. **期初脚本单独一个文件**，不跟 DDL 混在一起（DDL 可重跑，UPDATE 不可）。

### 2.2 期初数据脚本（上线当天，D23/D24/D25）

```sql
-- ① 全部置为生效
UPDATE sc_plan_product_range SET status='1', delete_sign=0 WHERE status IS NULL;

-- ② 层级统一集团集采、强管控统一否
UPDATE sc_plan_product_range SET collection_level='1' WHERE collection_level IS NULL;
UPDATE sc_plan_product_range SET is_strong_control=0  WHERE is_strong_control IS NULL;

-- ③ 🆕 修影响范围编码（D25）—— 先备份再改
--    BU0013 → BU013，并补 BU012
UPDATE sc_plan_product_range
   SET bu_code = 'BU001/BU002/BU003/BU004/BU005/BU006/BU007/BU008/BU009/BU010/BU011/BU012/BU013'
 WHERE bu_code = 'BU001/BU002/BU003/BU004/BU005/BU006/BU007/BU008/BU009/BU010/BU011/BU0013';

-- ④ 脏数据（约 600 条：207 主数据查不到 + 389 已停用）
--    默认：标为失效，不物理删（留痕，可回溯）
--    ⚠️ 具体清单要按【生产】数据重新算，不能照搬 UAT
```

> ⚠️ **③ 那条 UPDATE 的 `bu_code` 常量必须先在生产上核对**——生产的串可能和 UAT 不一样。
> **X1 那份清单没到之前，期初脚本只能算草稿。**

### 2.3 单据表（第二周，建议跟第一周 DDL 一起提）

对 `sc_plan`、`sc_plan_product`、`sc_scheme`、`sc_scheme_to_purchase`、`sc_supplier_green_channel` **各执行**：

```sql
ALTER TABLE <表名>
  ADD COLUMN collection_level  VARCHAR(16) NULL COMMENT '集采目录层级',
  ADD COLUMN is_strong_control TINYINT(1)  NULL COMMENT '是否强管控';
```

`sc_plan`、`sc_scheme`、`sc_supplier_green_channel` 三张**再加一列**（D18）：

```sql
  ADD COLUMN collection_change_reason VARCHAR(512) NULL COMMENT '是否集采改为否的原因';
```

> 💡 **新列默认 NULL，不影响任何现有逻辑**——所以可以第一周就提交，第二周只写代码。
> DDL 上环境要走流程，**攒一次比分两次省事**。

---

## 3. 接口契约

### 3.1 🔴 判集采接口改造（第二周最大的一块）

**地址不变**：`POST /e/business/source/plan/queryInProductRangeNum`（`PlanController.java:485`）
**调用方**：采购计划 / 采购方案 / 非平台录入**三个页面共用**（`10w` §附）

#### 现在的返回

```
Result<String>：  "0" = 一行没命中 ｜ "1" = 全命中 ｜ "螺纹钢、柴机油" = 部分命中
```

#### 改成

```jsonc
Result<PlanProductRangeCheckResultDTO>
{
  "header": {                       // 表头汇总，直接回写单据
    "isCollection": 1,              // 0/1；全部未命中=0，否则=1
    "mixed": true,                  // 是否混采（部分命中）——前端据此弹框
    "collectionLevel": "1",         // D3：跨行取"数字最小"= 层级最高
    "isStrongControl": 1,           // 6.4：有强取强
    "mixedProductNames": "螺纹钢、柴机油"   // 兼容旧文案，弹框直接用
  },
  "lines": [                        // 每行明细——6.3/6.4 汇总的依据，也是排查时的证据
    {
      "productCode": "10010001",
      "categoryCode": "170101",
      "hit": true,
      "hitRangeId": "xxx",          // 命中了台账哪一行（排查时最有用的一个字段）
      "collectionLevel": "1",
      "isStrongControl": 1,
      "collectionSupplierCode": "CN13000363"
    }
  ]
}
```

**三条约定**

| # | 约定 | 为什么 |
|---|---|---|
| 1 | **`hitRangeId` 必须返回** | 出问题时业务问"为什么这个物料算集采"，有这个字段一查台账就有答案，没有就得翻日志 |
| 2 | **旧的三态语义完整保留在 `header` 里** | 前端改造是"读新字段"而不是"重新理解"，三个页面的改法一致 |
| 3 | **接口签名一次改到位，第一周不动** | 第一周那 4 个影响点全部是方法内部加条件，**接口签名不变、前端一行不动**。这是两周里唯一一处签名变更 |

### 3.2 工作流变量表（⚠️ 现在就发给华姐，这是唯一能真并行的事）

| 变量名（`metaCode`） | 中文（`metaName`） | 类型 | 取值 | 三个入口现状 |
|---|---|---|---|---|
| `isCollection` | 是否集采 | String | `是` / `否` | 计划 ✅（只递这 1 个）<br>方案 ✅（第 7 个）<br>非平台 ❌ **要新加** |
| `collectionLevel` | 集采目录层级 | String | `集团集采` / `二级集团集采` / `区域企业集采` | **三处全要新加** |
| `isStrongControl` | 是否强管控 | String | `是` / `否` | **三处全要新加** |

> 🔴 **两条必须跟华姐说清楚的**
>
> 1. **值是中文，不是 1/0。** 现有 `isCollection` 递的就是中文 `"是"`/`"否"`（`SourceWorkflowAdapterBuilder.java:230-234` 实测）。新变量跟着来，否则她要写两套条件。
> 2. **上线顺序不能反**：
>    ```
>    ✅ 我的代码先发版（开始递新变量）→ 她再切流程图
>    ❌ 她先切图 → 新起的单子变量是空的 → 分支判断不出来 → 卡在审批里
>    ```
>    在途单据走老图，不受影响，不用特殊处理。

**代码落点**

| 入口 | 文件:行 | 动作 |
|---|---|---|
| 采购计划 | `SourceWorkflowAdapterBuilder.java:225-240` | 现在只递 1 个，追加 2 个 |
| 采购方案 | `SourceWorkflowAdapterBuilder.java:79-160` | 现在 7 个，追加 2 个 |
| 非平台录入 | `SourceWorkflowAdapterServiceImpl_GreenChannel.java:130-172` | 现在 3 个全无关集采，**从零加一整组** |

---

## 4. 第一周任务清单（9/8 ~ 9/11）

> **原则**：A（台账本体）和 B（保命改造）**必须一起上线**。只上 A 不上 B，三个采购页面当场提交不了。
> **提交粒度**：一个任务一个提交，出问题能精确回滚（`10u`）。

### 4.1 准备（动手写代码之前，⚠️ 过了这个点补不回来）

| # | 任务 | 说明 | 【估】 |
|---|---|---|---|
| **W1-00a** | 🔴 **录基线快照** | 挑 30~50 个真实物料调判集采接口，返回存文件。**必须在改代码之前**（`13b` §1.1） | 0.3 天 |
| **W1-00b** | **跑台账数据体检并存档** | test + uat 各一份（`13b` §1.2） | 0.2 天 |
| **W1-00c** | **变量名表发华姐** | §3.2 那张表。她立刻能开工 | 10 分钟 |

### 4.2 A · 台账本体

| # | 任务 | 落点 | 依赖 | 【估】 |
|---|---|---|---|---|
| **W1-A1** | DDL：台账加 9 列、放长 bu_code、条件删索引 | §2.1，**test + uat 都跑** | — | 0.3 天 |
| **W1-A2** | 实体加字段 + `@TableLogic` | `PlanProductRange.java` | A1 | 0.2 天 |
| **W1-A3** | 删除接口：物理删 → 逻辑删 + 记 `status`/`invalid_time` | `PlanProductRangeServiceImpl:97-100` | A2 | 0.2 天 |
| **W1-A4** | 列表分页绕过 `@TableLogic`，加状态/层级两个筛选 | `PlanProductRangeServiceImpl:70` | A2 | 0.4 天 |
| **W1-A5** | 判重方法 `isDuplicate()`（D1），替换导入里那段 | `ProductRangeImportServiceImpl:112-124` | A2 | 0.5 天 |
| **W1-A6** | 导入：去掉"必须二级"校验（D8）、加层级/强管控两个必填校验 | `ProductRangeImportServiceImpl:94-108` | A5 | 0.5 天 |
| **W1-A7** | 导入：追加 4 个新列（**只能加在第 6~9 位**）、去掉类目降级改写 | `ProductRangeImportServiceImpl:66-70,163-205` | A6 | 0.5 天 |
| **W1-A8** | 前端 JSON 加 4 列 + 2 个筛选，顺手修 4 处配置错 | **两份 JSON 都要改**（`10w` §4） | A4 | 0.3 天 |
| **W1-A9** | 前端【删除】文案改「作废」 | `procurementCatalogMgt/index.vue` | — | 0.1 天 |
| **W1-A10** | 导出模板：**test 从零建**、uat 加 4 列 + 修 `categoryName` 错字 + 补 `buName`/`createTimeStr` | `scripts/export-template.sh` | A1 | 0.4 天 |
| **W1-A11** | 导入模板 xlsx：做 10 列新文件、传 OBS、改 `scm_staticfile.staticfile_path` | test + uat | A7 | 0.3 天 |
| **W1-A12** | 区域开关字典（D9），默认关闭 | 沿用现有 `dictFeignService` 机制 | — | 0.2 天 |

### 4.3 B · 四个保命改造（`10v` 第一部分）

| # | 影响点 | 任务 | 落点 | 【估】 |
|---|---|---|---|---|
| **W1-B1** | ① | `pickOne(命中列表)` 方法（D2 的优先级） | 新增工具方法 | 0.3 天 |
| **W1-B2** | ① | `selectOne` → `selectList` + `pickOne`，**4 处**；`:687`/`:2034` 顺手补判空 | `PlanProductRangeServiceImpl:108,138`<br>`PlanTaskServiceImpl:687`<br>`PlanServiceImpl:2034` | 0.5 天 |
| **W1-B3** | ② | 6 处读表加状态过滤——**`@TableLogic` 自动覆盖 5 处**，只有列表那处要单独处理（已在 A4） | 靠 A2 的 `@TableLogic` | 0.1 天 |
| **W1-B4** | ③ | 云采采集去掉 `substring(0,4)`，改逐级向上（复用 `queryClassLine`）<br>🔴 **`s.setCategoryCode()` 必须保留订单自己的完整类目码，不要覆盖成台账那条的类目码**——驾驶舱和集采门户按这个字段做前缀聚合，覆盖了聚合口径会不稳（`10w` §7.5 N-2）。**照现有代码形状改，最自然的写法恰恰是错的那种。** | `SourceCollectionServiceImpl:205-226` | 0.5 天 |
| **W1-B5** | ③ | `queryClassLine` 补判空（放开末级后必被触发） | `MaterialClassServiceImpl:271-273` | 0.1 天 |
| **W1-B6** | ④ | 已合并进 A1（条件删索引）+ A5（应用层判重） | — | — |

### 4.4 C · 顺手修（成本几乎为零，一起做）

| # | 缺陷 | 落点 |
|---|---|---|
| **W1-C1** | 导入不回填 `bu_name`（uat 100% 为 NULL） | `ProductRangeImportServiceImpl:170-186` |
| **W1-C2** | JSON 里 `field:"执行单位名称"` 中文字段名 + title 写错 | 前端 JSON |
| **W1-C3** | JSON 里 `rowKey:"memberId"` 和 vue 的 `rangeId` 对不上 | 前端 JSON |
| **W1-C4** | 导出模板 `categoryName` 的中文标题写成了"集采物料编码" | 已在 A10 |

**第一周合计【估】约 5.5 人天**（准备 0.5 + A 3.9 + B 1.5 + C 顺手）。

---

## 5. 第二周任务清单（9/12 ~ 9/18）

| # | 任务 | 落点 | 依赖 | 【估】 |
|---|---|---|---|---|
| **W2-01** | DDL：4~5 张单据表各加 2 列 + 3 张加原因列 | §2.3（**建议第一周就提**） | — | 0.3 天 |
| **W2-02** | 实体 / DTO / VO / Mapper 跟着加字段 | `Plan` `PlanProduct` `Scheme` `SchemeToPurchase` `SupplierGreenChannel` 及各自 DTO | 01 | 0.7 天 |
| **W2-03** | 🔴 **判集采接口改返回结构** | `PlanProductRangeServiceImpl:119-160` + `PlanProductRangeCheckResultDTO` 新建 | W1-B2 | **1.5 天** |
| **W2-04** | 算值：是否集采（新口径 = 逐级向上 + 影响范围过滤 + 状态过滤） | 同上 | 03 | 1 天 |
| **W2-05** | 算值：层级取最小（D3）、强管控有强取强 | 同上 | 04 | 0.5 天 |
| **W2-06** | 三个值回写到单据（计划/方案/池子/非平台） | `PlanServiceImpl` `PlanTaskServiceImpl` `SchemeToPurchase*` `GreenChannel*` | 05 | 1 天 |
| **W2-07** | 🔴 三个**编制页**：接新返回结构、加 2 个只读字段、拆 `disabled`、修死锁弹框 | `saveOrSubmit.vue:264,1596`<br>`basicInfo2.vue:450,4316`<br>`increasedListAdd.vue:62,501` | 03 | 1.2 天 |
| **W2-08** | 🆕 三个**详情页**各加 2 个只读字段（D16） | `detail.vue:39`<br>`procurementSchemeInfo.vue:96`<br>`supplier/detail/index.vue:19` | 02 | 0.5 天 |
| **W2-09** | 采购方案拦截改成只对集团集采生效（D4） | `basicInfo2.vue:4330-4336` | 07 | 0.2 天 |
| **W2-10** | 「改成否」的原因输入框（D18） | 三个编制页 + 三个详情页显示 | 07/08 | 0.5 天 |
| **W2-11** | 递工作流变量：计划 +2、方案 +2、非平台从零一组 | §3.2 三个落点 | 06 | 0.8 天 |
| **W2-12** | 🆕 报表口径对账（不改代码，跑数对比） | `13b` §5 | 06 | 0.5 天 |

**第二周合计【估】约 8.7 人天。**

> ⚠️ **两周合计【估】14.2 人天，可用 9 个工作日。** 这个缺口是真实的，不是估高了。
> 应对见 §7.3「排期不够时砍什么」。

---

## 6. 分支、提交、编译、发版

### 6.1 分支（实测：**5 个仓库全部是分叉模式**）

```
git -C <仓库> merge-base --is-ancestor origin/prod origin/test  →  5 个仓库全部 NO
```

按 `ai-docs/团队规矩.md` §1，**分叉模式下 feature 从最新 `origin/test` 开**，不从 prod 开。

| 仓库 | feature 分支名 | 改什么 |
|---|---|---|
| `01zhaocai-end/scm-source-all` | `feature/jicai-mulu-yang` | 后端全部 |
| `02zhaocai-front/scm-vue-all-mdm` | `feature/jicai-mulu-yang` | 台账页面 JSON + vue |
| `02zhaocai-front/scm-vue-all-statics` | `feature/jicai-mulu-yang` | **同一份 JSON 的第二处** |
| `02zhaocai-front/scm-vue-all-plan` | `feature/jicai-mulu-yang` | 采购计划编制页 + 详情页 |
| `02zhaocai-front/scm-vue-all-procurementscheme` | `feature/jicai-mulu-yang` | 方案编制/详情、非平台录入/详情 |

> 📌 **两份 JSON 都要改**：查过 git 历史，原开发者当初就是**两个仓库各提交了一次**
> （mdm 仓 `cde83f8`、statics 仓 `73ef146`，提交信息都叫 `feat:集采范围台账开发`）。**照做。**
>
> ⚠️ `01zhaocai-end/scm-source-all` 和 `scm-vue-all-procurementscheme` 当前在 `feature/jiage-qushi-yang-prod` 上（价格趋势需求）。
> **开新分支前先确认那个需求的状态**，别在别人/别的需求的分支上叠加。

### 6.2 提交粒度

**一个提交 = 一件能独立上线的事**（团队规矩 §2）。建议切成：

```
feat: 集采台账加层级强管控状态字段          ← A1+A2
feat: 集采台账删除改为逻辑删除              ← A3
feat: 集采台账列表加状态与层级筛选          ← A4
feat: 集采台账判重改为四字段口径            ← A5
feat: 集采台账导入支持末级类目              ← A6+A7
fix: 集采规矩命中多行时按优先级取一条       ← B1+B2
fix: 云采采集按完整类目码逐级匹配           ← B4+B5
feat: 集采台账页面加四列                    ← A8+A9（前端仓）
```

提交信息**只写一行**，`feat:` / `fix:` 开头，**不带 scope、不带正文、不带 Co-Authored-By**（红线 6）。

### 6.3 编译验证

**每改完一块 Java 就跑隔离编译**，不等到最后：

```bash
bash scripts/jc.sh 01zhaocai-end/scm-source-all <改过的.java 文件...>
```

> ⚠️ 隔离编译要**显式列出 Lombok 实体**，否则 getter/setter 找不到（`ai-docs/经验教训.md`）。

### 6.4 发版链路

```
feature → test（合分支 + 用户点 Jenkins）
     ↓ 业务验收通过 → 冻结发布清单
uat（独立 worktree 从 origin/uat 拉交付分支，cherry-pick 冻结清单，MR 给兰宇）
     ↓ 在 uat 重做一遍 test 上做过的所有配置
prod（AI 只准备，不执行）
```

**每一级都要重做的配置**（很容易漏）：

- [ ] 导出模板 `planProductRangeExport`
- [ ] 导入模板 xlsx 传 OBS + 改 `scm_staticfile.staticfile_path`
- [ ] 区域开关字典
- [ ] 台账 DDL + 单据表 DDL
- [ ] 期初脚本

---

## 7. 排期

### 7.1 按天（9 个工作日）

> 【订正 2026-09-08：初版把星期标错了（`9/12` 写成周五，实为**周六**）。以下为按真实日历重排的版本。】

| 日期 | 干什么 | 产出 |
|---|---|---|
| **9/8（周二）** | W1-00a/b/c 准备三件套 ＋ W1-A1 DDL ＋ A2 实体 | 基线快照存档、体检报告、变量名已发华姐、表结构上 test |
| **9/9（周三）** | A3 逻辑删 ＋ A4 列表 ＋ A5 判重 ＋ B1/B2 保命改造① | 后端主体 |
| **9/10（周四）** | A6/A7 导入 ＋ B4/B5 采集 ＋ A8/A9 前端 ＋ A10/A11 模板 | 第一周功能齐 |
| **9/11（周五）** | 🔴 **第一周提测**：跑 `13b` 的 6 个场景 ＋ verify diff ＋ 修 bug | **第一批交付** |
| — | *9/12 六、9/13 日* | |
| **9/14（周一）** | W2-01/02 建表和实体 ＋ W2-03 接口改造起步 | |
| **9/15（周二）** | W2-03 接口改造完 ＋ W2-04 算值 | 🔴 最大的一块 |
| **9/16（周三）** | W2-05 汇总 ＋ W2-06 回写 ＋ W2-11 递变量 | |
| **9/17（周四）** | W2-07/08/09/10 六个前端页面 | |
| **9/18（周五）** | 🔴 **第二周提测**：全链路 ＋ W2-12 报表对账 | **第二批交付** |

**工作日核对**：第一周 **4 个**（9/8~9/11），第二周 **5 个**（9/14~9/18），合计 9 个。
对应工作量【估】第一周 5.5 人天 / 第二周 8.7 人天——**两周都超，第一周超 1.5 天，第二周超 3.7 天。**

### 7.2 并行的两条线

```
你（9/8 起）：  第一周台账 ────► 提测 ────► 第二周单据 ────► 提测
                     │
                     └─ 9/8 就把变量名发出去
                                  │
华姐（9/8 起）：              画三条分支图 ──────────────► 等你发版后切图
```

**华姐不需要等你写完代码**，这是两周里唯一真正的并行点。

### 7.3 ⚠️ 排期不够时，按这个顺序砍

【估】14.2 人天 vs 9 个工作日，**缺口是真的**。真到砍的时候，按这个顺序：

| 砍的顺序 | 砍什么 | 代价 | 为什么可以砍 |
|---:|---|---|---|
| 1 | **D18 改成否的原因框**（W2-10，0.5 天） | 分支②那级审批看不到理由 | 加一列不影响主流程，下期补 |
| 2 | **W2-12 报表对账**（0.5 天） | 领导可能先发现数字变了 | 可以挪到上线后一周内做，但**必须做** |
| 3 | **D19 待采购池带字段**（W2-01/02 的一部分，0.3 天） | 机电公司清单页看不到层级 | 方案可以自己重算一遍 |
| 4 | **三个详情页**（W2-08，0.5 天） | 🔴 审批人看不到新字段 | **最不该砍的一个**——砍了分支②就是走过场 |
| ❌ | **绝不能砍**：第一周的 B1~B5 | 线上直接坏 | — |
| ❌ | **绝不能砍**：W2-03 接口改造 | 6.3/6.4 算不出来 | — |

**更推荐的做法不是砍功能，是砍范围**：跟产品谈**「区域·企业」这一档延到下期**（D9 已经默认关闭了，
把它从"做了但关着"变成"这期不做"，能省掉 W2-04 里影响范围过滤的一半复杂度，约 0.5~1 天）。

---

## 8. 上线方案

### 8.1 上线顺序（错了会卡单）

```
1. DDL（台账 + 单据表）        ← 新列全 NULL，不影响任何现有逻辑，可提前
2. 期初脚本                    ← ⚠️ 必须在代码发版之前跑完，否则新代码读到 NULL
3. 换导入模板 xlsx             ← ⚠️ 必须和代码同时，否则旧模板错位落库
4. 后端 + 前端发版
5. 华姐切流程图                ← ⚠️ 必须在 4 之后
6. 上线后体检 + 报表对账
```

### 8.2 上线当天检查清单

- [ ] 期初脚本跑完，`SELECT COUNT(*) WHERE collection_level IS NULL` = **0**
- [ ] `bu_code` 里的 `BU0013` 已修成 `BU013`，`BU012` 已补（D25）
- [ ] 旧导入模板已下架，新 10 列模板已上传并可下载
- [ ] 导出模板 test/uat/prod 三个环境都配了
- [ ] 区域开关字典 = 关闭
- [ ] 跑一次 health 体检，跟上线前存档 diff
- [ ] **手动触发一次云采采集**，确认数据正常落库（页面上看不出来）
- [ ] 新提交一张单子，确认三个新字段有值（**如果是空，说明算值那段没生效**）
- [ ] 通知报表使用方口径已变更（X3）

### 8.3 回退方案

| 出什么事 | 怎么退 |
|---|---|
| 代码有问题 | Jenkins 回滚上一个镜像。**新列留着不删**（全 NULL 不影响旧代码） |
| 期初数据填错 | 提前把 `sc_plan_product_range` 全表 `CREATE TABLE ..._bak_20260918 AS SELECT *` 备份，从备份恢复 |
| 判重口径不对，导入全被拦 | 判重收敛在 `isDuplicate()` 一个方法，改一处重新发版 |
| 华姐切图早了，单子卡审批 | 让她切回老图；在途单据本来就走老图 |
| 报表数字异常 | 不用回滚代码——口径变化是预期内的，拿 W2-12 的对账报告解释 |

> 🔴 **唯一不可逆的是期初 UPDATE**。所以：**先备份表、再跑脚本**，备份表名写进发布清单。

---

## 9. 这次明确**不做**的（范围边界，别让它悄悄溜进来）

| # | 不做什么 | 为什么 | 什么时候做 |
|---|---|---|---|
| N1 | 台账「新增 / 编辑」表单（D21） | 要连带做末级类目树选择器，牵到 20+ 页面共用的公共接口 | 下期 |
| N2 | 改公共类目树接口 `queryProductClassTreeFull` | 20+ 页面共用，改一行全变（`10w` §5.1） | 要做就新开接口 |
| N3 | 「区域·企业」的**企业**那半边（D9） | 现有字段表达不了，且区域主数据 6 个 BU 全空 | 主数据补齐后 |
| N4 | 台账维护权限下放到板块（D22） | 要做数据权限，是独立功能 | 下期 |
| N5 | 寻源报名/报价那 9 处 `substring(0,4)` | 不读台账，本次不受影响 | 末级类目往寻源流的时候 |
| N6 | ES 集采索引历史数据回刷 | 口径变更是业务事实，回刷反而抹掉了历史真相 | 产品要求时 |
| N7 | 单元测试框架 | 仓库没有测试基建，搭一套的成本超过这次需求本身（`10u` §3） | — |

---

## 10. 每个默认决议猜错了，要返工多少

**这张表是给你判断"哪几条值得抢在开工前问掉"用的。**

| 决议 | 猜错的返工量 | 值不值得抢着问 |
|---|---|---|
| **D4** 方案拦截只对集团生效 | 🟢 一个 if | ⚠️ **值得**——不是因为改起来贵，是因为**猜错了上线当天不暴露**，等业务配了二级集团规矩才炸 |
| **D1** 判重口径 | 🟡 一个方法 + 可能重刷期初数据 | 值得 |
| **D2** 匹配优先级 | 🟡 一个方法 | 值得 |
| **D23/D24** 期初填什么 | 🔴 **要重刷 7343 行，且期间单子判定是错的** | 🔴 **最值得**，而且 `12c` 里你写的默认和我的不一致 |
| **X1** 生产类目清单 | 🔴 **期初脚本整个作废** | 🔴 **必须要到，没有替代方案** |
| D3 最小层级方向 | 🟢 一个比较器 | 有代码先例支持，可以不问 |
| D13 混采禁不禁 | 🟡 三个前端各改一处 | 可以不问 |
| D14 弱管控走哪条流程 | 🟡 华姐返工重画一条分支 | 值得顺口问一句 |
| D5~D12、D15~D22 | 🟢 都在 1 天以内 | 不用专门问 |

> **结论：真正值得抢在开工前问掉的只有 4 条 —— X1、D23/D24、D4、D1/D2。**
> 其余 18 条按本文默认做，猜错的代价都在可接受范围内。

---

## 附 · 与既有文档的关系

| 文档 | 关系 |
|---|---|
| `01c` 需求原件 | 本文的输入，未修改 |
| `10q` Q1~Q8 / `10r` P1~P11 / T1~T3 | **全部收口进本文 §1**，此后不再单独引用 |
| `10t` 两批拆解 | 任务清单以**本文 §4/§5** 为准（`10t` 有 4 处已订正） |
| `10u` 工程防线 | 本文采纳其全部建议（D5/D6/D9/D11 都来自它） |
| `10v` 四个影响点 | 本文 §4.3 是它的落地版；**§1 的 D2 订正了 `10v` §1.3 的一处说法** |
| `10w` 全量影响面 | 本文 §4/§5 的清单来自它的 §9 影响面总表 |
| `13b` 测试方案 | 本文的验收部分单独成篇 |
