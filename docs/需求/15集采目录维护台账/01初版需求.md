当前还未开会，看到了需求文档，想让你先来分析一下需求，确定一下问题文档，等待开会时候再去确定。再单独准备业务文档，给我讲明白这叽了咕噜说啥呢，这需求看得我眼花缭乱的。和前段时间开发的那些需求有有没有关联。


# 集采目录管控
系统现状：
招采平台中通过【集采范围台账】判断集团级集采范围，支持按照二级类目或物料编码进行管控；
未来管理需求：
1、集采目录管控：目录管理到末级（同云采）；完成物料目录到末级的梳理；
2（补充）集采目录层级管理：一级集采（全集团）、二级集团集采（按bu）、区域或企业级集采（要想一下区域怎么玩）
2、目前全集团只统一到二级，优先参考水泥集团现有类目标准，
3、是否强管控、按照bu、进行管控、启用时间、失效时间；(管后不管前)；
4、期初上线=初始化+新业务自动取值； 							

集采范围台账：http://10.0.54.16:32740/mdm/index.html#/mdm/procurementCatalogMgt
对应前端模块：02zhaocai-front/scm-vue-all-mdm

# 功能实现

1、现有功能【集采范围台账】改造；
2、增加“集采目录层级”、“是否强管控”、“失效时间”、“状态”字段；
2.1、集采目录层级：新增，必填，可选内容为：集团集采、二级集团集采、区域/企业集采；
2.2、是否强管控：新增，必填，可选内容包含是、否；强管控的，采购方案编制时/非平台录入允许修改“是否集采”字段信息，如果是改成否，执行新的审批流程（向上一级审批）；弱管控的，允许修改“是否集采”信息，修改后执行原流程；



是否集采=是&是否强管控=是（&集采目录层级=集团集采）：企业---二级集团---集采执行单位---总部；
是否集采=是&是否强管控=是（二级集团集采、区域/企业）：企业；”是否集采“改成“否”，向上一级审批：企业/区域发起---二级集团；二级集团发起-----总部；
是否集采=是&是否强管控=否（二级集团集采、区域/企业）：企业；”是否集采“改成“否”，按原流程执行；

2.3、失效时间：新增，只读，记录删除时间；
2.4、状态：新增，只读，内容包含：生效、失效；新增后，状态默认“生效”，如果删除后状态更改为“失效”；对后续影响：采购方案提交或者非平台录入提交时，物料编码属于集采台账的同时增加“状态”的判断条件；
3、是否集采：后续采购方案、采购计划、非平台录入，物料明细行中的类目只要属于“集采范围台账”自动回写“是否集采、集采目录层级”；
3、集采类目编码：支持选择到末级（有3、有4，类目编码清单？？---目前各二级集团2级类目已统一，3、4级未统一？？？？
4、影响范围编码：如果选择“集团集采”，“影响范围编码”录入所有二级集团BU编码，所有企业均执行集采逻辑；如果选择“二级集团集采”，录入指定的二级集团BU编码，指定BU编码的下属企业执行此集采逻辑；如果是“区域/企业集采”，录入指定的区域的组织编码【企业与区域的关联关系见右图】，区域编码下的关联企业均执行此集采逻辑；
5、集采执行单位编码：集采目录层级为“二级集团、区域/企业”的无需配置集采执行单位；
6、类目处理逻辑：
6.1、如果某一类目，按照物料编码控制，则”集采物料编码”录入物料编码，当有物料编码时，”类目编码”必须匹配到末级；
6.2、如果某3级下维护了某个4级，则此3级需要按照4级清单进行维护；如果某3级下未维护4级，可以直接维护3级，维护3级后控制下边所有的末级；
6.3、如果某2级下维护了某个3级，则此2级需要按照3级清单进行维护，如果某2级下未维护3级，可以直接维护2级，维护2级后控制下边所有的3级；
6.4、如果某1级下维护了某个2级，则此1级需要按照2级清单进行维护，如果某1级下未维护2级，可以直接维护1级，维护1级后控制下边所有的2级；
6.5、只对“状态=有效“的数据进行判重处理；相同类目“类目编码&物料编码&集采目录层级&影响范围编码“有效的不允许重复导入；

7、强管控的必须进行物料类目标准化；！！！！！							


# 前端界面
查询条件：和现在的查询条件一样，不用变
列表详情：相比现在要增加4列。
在集采物料名称与影响范围编码之间，增加“集采目录层级”列：新增，可选内容为：集团集采、二级集团集采、区域/企业集采
在集采执行单位名称和创建时间之间增加“是否强管控”：新增，可选内容：是、否；
在创建时间和操作之间增加两列：
失效时间：新增，记录删除时间；
以及
状态：新增，可选内容包含：生效、失效；

另外文档里对列“集采执行单位编码”有备注：目前此字段控制集采执行单位在线确认计划。

另外还有两个图片，我没太看懂。
docs/需求/15集采目录维护台账/01需求-图片1.png：组织单元编辑表单

docs/需求/15集采目录维护台账/01需求-图片2.png：编辑品类的界面，但我没找着这个界面在哪
