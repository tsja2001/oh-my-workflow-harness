# Cloudflare 家庭私网接入 · 家庭端最终复验

> 日期：2026-08-23  
> 工具：codex  
> 当前状态：家庭侧 Tunnel、CIDR Route、Mac Connector、固定 DHCP 地址、本地服务和恢复能力全部通过；公司 Windows 异地数据面仍待实机验收。  
> 下一步：公司 Windows 离开家庭 Wi-Fi 后安装并注册 Cloudflare One Client，完成最后一轮端到端测试。

> 本文接续 `30-家庭端测试报告-codex.md`，将原报告 T12“路由器固定租约未执行”更新为已通过；原报告保留作为历史过程记录。

## 1. 新增复验结果

| 编号 | 测试项 | 结果 | 证据 |
|---|---|---|---|
| T12-R1 | 路由器固定租约写入 | 通过 | 重新加载页面后静态绑定表仍有 Mac → `192.168.5.35` |
| T12-R2 | 设备标识一致 | 通过 | 路由器在线设备与 `ifconfig en1` 的私有 Wi-Fi MAC 一致 |
| T12-R3 | DHCP 续租地址稳定 | 通过 | 续租后 `yiaddr=192.168.5.35` |
| T12-R4 | DHCP 服务器和网关正确 | 通过 | 均为 `192.168.5.1` |
| T12-R5 | 续租后 SSH | 通过 | `192.168.5.35:22` TCP 成功 |
| T12-R6 | 续租后 Home Assistant | 通过 | HTTP 200 |
| T12-R7 | 续租后新 Tunnel | 通过 | `home-lan` Healthy、Route 正确 |
| T12-R8 | 旧 Tunnel 不受影响 | 通过 | `saturate` Healthy |

## 2. 当前结论

- 家庭端：最终通过。
- 云端控制面：通过。
- 路由器地址稳定性：通过。
- 公司 Windows 异地端到端：未执行，仍不能标记整个“家庭 ↔ 公司”链路最终通过。

## 3. 剩余验收

1. 公司 Windows 在非家庭 Wi-Fi 下注册到 Team `mute-cake-c395`。
2. 验证 `192.168.5.35:22`、`192.168.5.35:8123`、`192.168.5.1:80`。
3. 核验只有家庭 `/24` 和 Cloudflare注册端点进入 WARP。
4. 核验普通公网、公司内网和 DNS仍走原路径。
5. 断开/重新连接 Cloudflare One Client，确认家庭目标随之不可达/恢复。

