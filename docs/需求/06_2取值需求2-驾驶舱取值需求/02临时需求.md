在集采日计划统计台账页面里，增加两个数据，适配这次的需求：
已完成数量和已滞后数量，都改成已完成数量/率和已滞后数量/率
具体需求：
已完成数量/率：
1、已完成数量：每个月最后一天的“当月已完成数量”+当前月当前日期前一日的“当月已完成数量”的累计值；
2、完成率=已完成数量/计划总数量*100%

已滞后数量/率：
1、已滞后数量：每个月最后一天的“当月已滞后数量”+当前月当前日期前一日的“当月已滞后数量”的累计值；
2、滞后率=响应率-执行率-完成率

要是没问题的话直接帮我改了吧

另外前面还有没确定的需求，不是说这个表里计算的逻辑和新需求逻辑不一样么，我把完整文档给你，你看看和一开始判断的一不一样，问题要不要改：
1.计划总数量：	每个月最后一天的“当月计划总数量”+当前月当前日期前一日的“当月计划总数量”的累计值；
响应数量/率：	1、响应数量=每个月最后一天的“当月响应数量”+当前月当前日期前一日的“当月响应数量”的累计值；
2、响应率=响应数量/计划总数量*100%
执行中数量/率：	1、执行中数量：每个月最后一天的“当月执行中数量”+当前月当前日期前一日的“当月执行中数量”的累计值；
3执行率=执行中数量/计划总数量*100%

