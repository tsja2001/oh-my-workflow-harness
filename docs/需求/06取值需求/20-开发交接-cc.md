# 集采首页取值(06) · 开发交接

> 日期：2026-08-05 ｜ 工具：cc + codex 增量开发  
> 当前状态：**06 的前后端代码项已全部收口并完成本地运行验证；只剩推送部署、test 数据治理和受控对账。**  
> 下一阶段：用户决定推送和部署时点；部署后由测试 AI 通过 test 网关复验 overview、`kengk/mt/gc` 与门户页面。  
> 口径以 `13-需求定稿-开发基线-cc.md` 为准；本文只记"做到哪了、下一步做什么、有什么遗留"。
> 分支：后端 `scm-source-all/feature/jicai-portal-yang`，收口提交 `89be56b57`；前端 `scm-vue-hpc/feature/quzhi-yang`，提交 `292fbcd`。全部仅本地提交，未推送。
> **2026-08-05 codex 更新：**原评审中的性能、失败态、钢材月指标、提交清理、固定时间起点、缺 `buCode` 告警、假数/精度和 MT 编码均已处理；复评见 `40-评审报告-codex.md`。
> **2026-08-06 oc 更新：**13 定稿的收尾改动已落盘提交——后端补 `fix: 轴承2502精确匹配`（`2fc0a927c`），前端补 `feat: 首页增加平台总交易额`（`4957b71`）；`config.js` 的本地 token 已还原不提交。后端 merge-tree 预演：`feature/jicai-portal-yang` → `test` 无冲突，可直接合并（test 有 5 个无关系列提交）。
> **2026-08-06 oc 更新2：**用户已将前端 feature/quzhi-yang 合入本地 test，但 `4773532 修复前端依赖安装` 的 package-lock.json 改动被一并带入（内网源 10.0.66.31 条目），已本地重做合并 `42a552d`：package-lock.json 还原为 origin/test（=prod）版本，合并只含 06 业务文件；test 未推送，feature 已推送远端（含 4773532，历史不改）。推送 test 时会把分支带进来的 prod 系提交一并带上。

---

## 一、已完成（可测）

### 1. 首页汇总接口 overview
- **接口**：`POST /e/business/source/centralizedProcurementPortal/overview`，可选 body `{"queryDate":"yyyy-MM-dd"}`（不传＝服务器当天，统计到前一天）。
- **一次返回**：核心指标(月/季/年集采额、平台总额、集采率) + 各二级集团(6个+直管) + 6 个品类 + 煤炭/钢材 日月季累计。金额万元两位小数、比率百分比两位小数。
- **实现**：统一 ES 货架一次 `size:0` 的命名 `filters + sum` 聚合 + 三笔去重，不拉明细回 Java；一个页面请求只访问 ES 一次。口径严格按 `13` 定稿。
- **首页接口 4 个文件**：
  ```
  scm-source-chase/.../controller/CentralizedProcurementPortalController.java
  scm-source-chase/.../service/CentralizedProcurementPortalService.java
  scm-source-chase/.../service/impl/CentralizedProcurementPortalServiceImpl.java
  scm-source-chase/.../vo/centralizedProcurementPortal/PortalOverviewVO.java
  ```
- **验证**：隔离编译 0 报错；本地 `scm-source-web` 跑通；2026 查询结果与 ES 独立核算**逐项一致**（平台 50776.09万 / 年度集采 14143.72万 / 集采率 27.86% / 煤 14121.28万 / 钢 1.47万 / 办公 4.98万）；2027 查询验证年度/累计仍固定从 2026-01-01 起算。
- **测试件**：`21-接口测试-集采首页.postman_collection.json`（Postman 集合，baseUrl 默认本地 8110）。

### 2. 本地运行工具（顺带沉淀）
- `scripts/srun.sh`：一键 `build|start|wait|status|stop` 本地起 scm-source。用法与坑见 `note/backend-env-setup.md §0`。

### 3. 06 cockpit 三接口接台账

- **最终范围**：只做 06 定稿中的 `kengk/mt/gc`，不补原四台账的 `categoryAmount/situA/doRate`；中厚板无 test 数据不阻塞，也不造共享测试数据。
- **后端**：`kengk` 复用既有坑口台账查询；`mt/gc` 由 source 定义 Provider、ssc 实现，避免循环依赖。价格趋势按北京时间取当前月之前连续 12 个月；同月同指标取发布时间最新一条；删除、空价格和脏年月不进入结果；缺月/缺指标保留 `null`。
- **字段**：煤炭成交价/环渤海价映射 `dataValue/ext02`；钢材成交价/螺纹/中厚板映射 `dataValue/ext02/ext03`，中厚板指标名固定为 `中厚板月成交价格`。
- **前端**：HPC 的煤炭/钢材图表不再把 `null` 转成 0；钢材“主流品种月指标”从最新月份的 `ext02/ext03` 展示螺纹钢、中厚板，无值显示 `--`。相关页面已随前端收口提交 `292fbcd` 一并提交。
- **本地提交**：后端 `1f419cc84 feat: 驾驶舱接入台账`，只含 3 个后端目标文件；现有 D2 文件没有夹带，未推送远程。
- **验证**：3 个 Java 文件隔离编译 0 错误；source/ssc 两模块打包成功；`scm-source-web` 本地启动成功；带真实登录态实调三个接口全部 `code=000000/status=true`；HPC `npm run build` 成功。
- **接口数据快照（2026-08-04）**：`kengk` 返回最新 5 条 750/770/750/780/770；`mt` 返回 2025-08～2026-07 共 12 条，仅 2026-07 `ext02=500`；`gc` 同期 12 条均为空点。这与 test 台账独立查询一致，证明不再读旧缓存。

---

## 二、评审后下一步要做

| # | 事项 | 改哪里 | 冲突风险 | 说明 |
|---|---|---|---|---|
| A | ✅ **D2 已修正** | `CentralizedProcurementPortalServiceImpl.d2()` | 低 | 已加 `supplierCode=9111010577406207XG`；固定 BU002，其他集团 D2=0；隔离编译通过，待样本对账 |
| B | ✅ **overview 合并 ES 查询** | `CentralizedProcurementPortalServiceImpl` | 中 | 已改成命名 `filters + sum` 子聚合；代码只保留一次 `restTemplate.search()`，本地结果与改造前一致 |
| C | ✅ **cockpit 接台账** | `SourceCollectionDataServiceImpl`、source/ssc 价格读取接口 | 低 | `kengk/mt/gc` 已按 06 完成并本地实调；原四台账其他链路明确不在本次范围 |
| D | ✅ **修前端错误态和钢材月指标** | `overviewStore.js`、门户组件 | 低 | 成功/失败/真实 0 已区分；失败显示横幅和 `--`，支持重试；钢材月指标已接最新月份值 |
| E | ✅ **提交前清理** | `vue.config.js`、`public/statics/config.js`、`data.js` | 低 | 本地 8110 代理已删除，业务假数已清，金额统一两位小数；敏感配置仅留在本机且未提交 |
| F | ✅ **修时间起点并补告警** | overview 后端 | 低 | 年度/累计固定从 2026-01-01；同一次聚合检查缺失/空 `buCode` 并输出笔数、金额告警 |
| G | **受控对账** | test ES + 接口/页面 | 无 | 当前 test 无北分 FP 样本；准备北分与其他供应商煤炭样本验证只扣正确桶。共享 test 数据有任何增删前先征得用户同意 |

### 本轮代码收口（2026-08-05）

- 后端 `89be56b57 fix: 完善集采首页取值`：overview 53 次 ES 查询合并为一次；固定 2026 起点；补缺失 `buCode` 告警；MT 同步补 `supplierCode=CN11005095`。供应商名称因法人名称未确认，继续沿用 03 既定值。
- 前端 `292fbcd feat: 完善集采首页展示`：统一 overview store；所有激活卡片接真实数据；失败显示 `--` 与重试；真实 0、千分位和两位小数正确；清掉 mock 数字和随机圆环；未提交 `public/statics/config.js`。
- 验证：后端隔离编译、fat jar 构建、Spring 启动、2026/2027 两次 overview 实调通过；前端生产构建通过；Chromium 模拟成功、HTTP 500 失败和重试恢复三种状态均通过。

### C 的冲突面与处理结果（2026-08-04）
- 开发前，坑口/煤价/钢价三接口(`cockpit_collection_data_kengk/mt/gc`)读冻结旧缓存表 `sc_collection_data`；现已改读 `sc_coal_pit_daily_indicator` / `sc_price_trend`（螺纹钢/中厚板走 `sc_price_trend.indicator_name`）。
- 旧文档提到的 `feature/jcrje-cockpit-switch` 关键提交 `2f33fa77f` 实际只改 `SourceCollectionMapper.java` 10+/10-；它及合并提交 `b7fcf7cff` 都已是当前开发分支祖先，**不是仍在平行开发的阻塞分支**。
- `SourceQueryController.java` 本期不需要修改；通用 `{typeCode}` 路由已经存在。共享风险集中在 `SourceCollectionDataServiceImpl`，只给 `kengk/mt/gc` 增加早期分流，其他类型保持不变。
- `SourceCollectionDataServiceImpl.java` 历史上“坑口改实时查台账”加了又撤回，原因是当时交付延期；现有坑口查询转换代码仍保留，可以重新接线。这是历史背景，不是 Git 冲突。
- 价格趋势在 `scm-source-ssc`，而 ssc 已依赖 source；禁止让 source 反向依赖 ssc，建议用“source 定义读取接口、ssc 实现”的方式避免 Maven 循环依赖。
- 结论：后端已按上述边界完成独立本地提交 `1f419cc84`；只包含 source/ssc 三个目标文件，没有夹带 D2 或其他工作区修改。完整数据口径、实现和验收记录见 `21-驾驶舱交接-codex.md`。

---

## 三、北分识别结论

### ✅（2026-08-04 已闭环）FP 北分使用统一社会信用代码

产品确认：FP 北分使用统一社会信用代码 `9111010577406207XG`。该编码 18 位且校验位有效，D2 精确查询 ES `supplierCode`；不用 SCM 编码、名称或 SAP 的 `CN11005095`。

当前 test 的 FP 原表、ES、SRM、UBM 均没有该编码样本，因此编码值以产品/主数据答复为准，修复后必须用受控样本验证。MT 整路仍按产品确认直接取数，不需要 FP 编码。

06 首页只用编码筛选，不展示 D2 供应商名称，所以 MT 显示名历史差异不再阻塞本功能；若后续改 03 报表显示名，再单独确认法人名称。

### 其他遗留（不阻塞）
- 平台总额口径想跟领导顺口确认一句（`13 §9`）；先按需求原文写。
- 寻源侧 FP/ZC/MT/JD 四个同步定时任务未配（YC 已配），联调阶段由用户在调度中心补配。
- 中厚板标准指标名已从价格趋势维护页面确认是 `中厚板月成交价格`，映射 `gc.ext03`；用户明确无需补 test 样本，当前按空点验收。
- test ES 仍有一条 3.6 亿 FP 测试垃圾；未经用户确认未删除。D2 当前也没有北分 FP 样本，部署后需受控数据验证。

---

## 四、下一步交付
- 当前没有剩余代码项。用户准备好后分别推送后端、前端本地分支，再点 Jenkins 部署 test。
- 部署后补 test 网关接口与页面验收；如需新建/清理共享 test 数据，先征得用户同意。
