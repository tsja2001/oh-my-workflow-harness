01台账和02台账需求2是我做的，先这个集采日计划统计台账是别人做的，但是要我去改bug。
我先把这个块需求给你：
集采日计划统计台账
一、支持北分和机电手工录入；云采定时同步；
1、招采平台一级菜单”集采业务管理台账“；下增加二级菜单”集采日计划统计台账“；
2、”集采管理-执行单位集采管理岗”，此岗位有此菜单操作及查询权限；
3、集采执行单位需要每天24点前维护当月集采物资的计划执行情况；发布后纳入集采驾驶舱【集采计划分析】；
4、每日24：00系统按照“统计日期”自动取前一日的数据并自动更新“集采驾驶舱”；如7月2日24点，取“统计日期=7月1日”的数据；
5、台账支持数据查询、筛选、新增、修改、删除、导出；
a、新增：录入后直接发布；发布后，支持【修改、删除】，
b、修改：点击【修改】，可对“集采执行单位、集采类目、统计日期、当月计划总数量、当月响应数量、当月执行中数量	当月已完成数量、当月已滞后数量”进行修改；
c、删除：点击【删除】，可对本行进行删除；
d、修改后更新“发布时间和操作人”；

————————
二、云采计划数据定时同步：
1、每天早起10：00定时同步云采平台当月1日至前一日的计划数据；
2、统计范围：云采所有的还是只统计办公文具、办公设备？
3、当月计划统计情况：
①当月计划总数=全部-订单异常-已保存
②已响应=总数-供应商待确认;
③执行中=总数-供应商待确认-已完成=企业待审批+待发货+待收货
④已完成=总数-待审批-待发货-待收货；
⑤已滞后=0（默认0）
⑥待审批=供应商待确认+企业待审批
4、同步云采的，“集采执行单位、集采类目”均默认“云采平台”；
5、同步云采平台的不允许修改、删除；
6、同步云采平台的，发布时间为接口”同步时间“，统计日期为”前一日“，操作人为“系统自动”；
7、同步云采的执行单位，取订单的
-----------------
响应数量=执行中数量+已完成数量+已滞后数量；
三、增加统计行，
1、显示“截止当前日期的前一日累计数量（2026.1.1起）”；此数据不跟随查询条件变动；
2、每天晚上24:00系统按照统计行 中的 响应率、执行率、完成率、滞后率，自动更新【集采驾驶舱-集采计划分析】模块；
					
1. 计划总数量：	每个月最后一天的“当月计划总数量”+当前月当前日期前一日的“当月计划总数量”的累计值；
2. 响应数量/率：	1、响应数量=每个月最后一天的“当月响应数量”+当前月当前日期前一日的“当月响应数量”的累计值；
2、响应率=响应数量/计划总数量*100%
3. 执行中数量/率：	1、执行中数量：每个月最后一天的“当月执行中数量”+当前月当前日期前一日的“当月执行中数量”的累计值；
2、执行率=执行中数量/计划总数量*100%

同事说的就是：
“响应数量/率”这个数据他觉得有问题。
他的原话：
响应数量 应该是19025吧，是上月最后一条+前一天的数
然后在列表中圈中了“2026-8-13”的25
和“2026-7-31”的19000
和在一起是19025
但是现在页面上显示的是19,085.00 
帮我看下这块逻辑有没有问题。

测试环境是uat，按理说test逻辑一样，你可以先看uat的数据

对应接口（已脱敏）：
- 环境：UAT 招采平台
- 方法：POST
- 路径：`/gateway/d/business/order/centralPurchaseStatistics/calculateRates`
- 请求体：`{}`
- 调用方式：使用 `scripts/api.sh` 自动登录，禁止在文档中保存 Authorization、Cookie 或其他凭据。
