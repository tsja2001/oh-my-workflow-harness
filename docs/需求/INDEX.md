# docs/需求 索引

> **这是 `docs/需求/` 的 L0 层：只回答「什么时候该打开哪个主题」，不复述里面有什么。**
> 找历史方案、想知道某件事以前做过没有 → 先看这张表，再进目录。
> **新开主题必须在这里加一行**，否则对 AI 等于不存在。

---

## 业务线：台账 / 集采目录（2026-07-07 起；01/02/04 已交付，01b 收尾，15 开发中，01c/22 只有口头线索）

一条线拉通五个目录，**按下面顺序读才连得上**。

| 目录 | 什么时候开 |
|---|---|
| `01台账` | 台账需求**一期**开发。和同事协作，需求原件、方案、SQL 都在这 |
| `02台账需求2` | 台账需求**二期**，与一期并行推进（07-10 起，拖到 08-17） |
| `04和生产环境运维对接需求1-4` | **01/02 的 prod 部署阶段**——和运维对接、部署文档、bug 处理。**不是新需求** |
| `15集采目录维护台账` | **集采目录管控改造**（08-19 调研 → 09-03 开会 → 09-08 与产品当面定稿 → **开发 9/8~9/18、测试 9/21~9/24、国庆后上生产**）：台账加「集采目录层级/是否强管控/失效时间/状态」四个字段、类目管到末级、按 BU/区域生效、强管控改「否」走上一级审批。<br>**接手就看这三份**：`13`（开发基线，§1 已标注哪些被产品推翻）→ `12f`（09-08 产品答案回收，判重/期初/导入三条口径全变了）→ `13c`（两周任务与影响范围）。<br>不懂业务先看 `10d`（从头讲起，含模块关系图） |
| `01b-机电日金额初始化` | **本线新需求（08-31 起，进行中）**：机电公司集采日金额——轴承/润滑油/钢材/电线电缆类明细的**数据初始化 + 给机电公司的填写模板**。目录名带 01 主号=同一条线 |
| `01c-机电自动录入` | 🟡 **只有口头线索，未开发（09-08 产品顺口提的）**：机电公司嫌日金额天天手工录太累，想改成**系统自动取数**。难点是机电公司作为招标代理、招标企业都写它自己，**归不到真实采购企业**；产品设想从**收货地址截 `-` 前面的公司名**来区分。**产品明说排在 15 后面**。只有一份 `10-录音摘录与初步调研-cc.md` |
| `22云采协同交易数据bug` | 🔴 **线索极薄，别急着查（09-08 产品演示时顺口说的）**：某页面「当天的数据没更新」，**页面、平台、数据项、环境全没说清**。<br>`00-问题描述-录音摘录-cc.md` 里有原话和**要先补问的四句话**。**问清页面之前不要动手排查。** 目录名的"云采协同"是事后起的，录音里没出现过 |

## 业务线：取值 / 统计报表（2026-07-20 ~ 08-19，已交付）

| 目录 | 什么时候开 |
|---|---|
| `03采集管理业业务统计报表` | 采集管理业务统计报表的取值口径与开发 |
| `06取值需求` | **首页 `/hpc/index.html#/procurementPortal` 的统计数据取值**。本仓库流程最完整的样板，含 `问题解决/`、`复盘与学习/` |
| `06_2取值需求2-驾驶舱取值需求` | **驾驶舱统计数据取值**，是 06 的续集 |
| `14和生产环境对接报表取值` | **本线的 prod 对接阶段** |

## 安全 / 版本升级

| 目录 | 什么时候开 |
|---|---|
| `05fastjson升级` | fastjson 安全版本升级。**82 个文件，全是过程记录，含一次完整的"升级→出问题→回滚→重来"**。做类似升级前值得先看它怎么翻车的 |
| `16安全` | 安全整改、漏洞处置 |

## 开发环境 / 工具（都很小，出同类问题时才开）

| 目录 | 什么时候开 |
|---|---|
| `07开发本地运行java环境` | 本地 JDK / IDEA 跑起来。**注意与 `ai-docs/本地运行手册.md` 重叠，以手册为准** |
| `08jsonBUG解决` | 一次性 json bug，已完成 |
| `09自动登录获取Token` | 脚本自动登录拿 token。**已固化成 `scripts/auth.sh`，优先用脚本** |
| `10WSL-Clash网络间歇断网` | AI 工具（cc/codex/oc）走代理间歇断网。**本机开发环境问题，不是业务问题** |
| `11部分git仓库需要账号密码` | 部分公司仓库（如 `01zhaocai-end/scm-report-all`）pull/push 要账号密码 |
| `13数据库考试` | 一次数据库测试，与业务无关 |
| `21工作流` | **DPS 审批流全面研究（2026-09-04 完成）**：控制台登录配方、只读 API、引擎集成协议、模板四级降级路由。**产出已固化成 `ai-docs/工作流地图.md` + `scripts/wf.sh`，日常用那两样，本目录只存调查证据（`10-调查`）和用户扫盲（`51-扫盲`）** |

## 规划 / 元

| 目录 | 什么时候开 |
|---|---|
| `18集采平台下一步规划` | **未来一年 37 项计划、业务全景扫盲、企业系统全景图**。问「这系统整体是干嘛的」「邻居系统有哪些」先看这里（`50` 讲给改代码的人、`51` 讲给不看代码的人、`52` 是全景图、**`53` 讲"这套系统为什么长成这样"**——代码考古实证平台出身某 `scm-v3` 产品线、前一客户是中化能源；厂商=石化盈科仍标【推断】、**`54` 是全集团架构六视图**——板块×系统覆盖矩阵、12 条对外接口、9 个空格；**`55` 是业务架构**——价值流/能力地图（从菜单树实测）/业务对象/能力-系统覆盖矩阵）；子目录 `0827开会/` 是**美云智数 SRM 选型会**的录音解读与格局推演，含实测结论「现有 SRM 对接只覆盖 BU004 冀东发展一个板块」 |
| `19新需求` | 最新一批需求。**"新"会过期，下次收口时改成具体名字** |
| `20重构harness` | **本 harness 自身的诊断与重构**（索引缺口、记忆设计、目录生命周期）。改 `AGENTS.md` 规范前先看这里 |

---

## 已移出本仓库

| 原目录 | 去向 | 原因 |
|---|---|---|
| `17网络`（65 文件） | `/home/t/projects/personal/08网络/` | VPS / Cloudflare / SSH 域名迁移，个人网络运维，与招采无关 |
| `12deepseek-harness迁移` | `/home/t/projects/personal/09deepseek-harness迁移/` | 个人项目 |
| 顶层 `deepseek-harness/` | 同上 | 同上 |
| `scripts/windows-*` `macos-*`（11 个） | `/home/t/projects/personal/08网络/脚本/` | 同 17网络 |

> **`personal/` 不是 git 仓库**，这些文件已脱离版本控制。需要找回旧版本时从本仓库 `main` 分支历史取。

---

## 早期文件的命名例外

`01台账` `02台账需求2` `05fastjson升级` `06取值需求/复盘与学习` 等早期目录里，
有一批文件用的是**顺序编号**（`01xxx.md` `02问题.md` `03全套业务逻辑复盘.md`），**不是 `AGENTS.md` §5 的段号**。
读的时候按文件名判断，别当成段号。**新文件一律按 §5 的段号来。**
