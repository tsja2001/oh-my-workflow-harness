为了方便调用和开发，我把skill移动到了：skills/enterprise-dev-workflow。调用的先手动指定，然后再说放在环境的某个位置。

当前创建的为了工作项目的这个skill很好，我还有一些要求觉得可以继续提升skill
当前流程主要是 确定根据需求了解项目确定拆解需求-规划问题（让我知道有什么待确定的问题，方便和技术同事或者产品来沟通）- 执行开发-我参与点击部署等操作-我拿到浏览器CURL给AI进行测试-由AI做代码review（可选步骤）-前端工作未来也是我的。这种持续增加的记录公司和我的习惯、规定、操作方式等的文档，最好有个单独文档来记录，也方便我查看和修改。这个文档也要实时更新


像前期肯定有很多需要和同事确定的事情，你最好做一些设定，有些肯定不确定的就给我介绍一下情况背景，问题遇到了什么，怎么和同事问问题。有些事情可能有点点不把握，但是不问的话造成的影响不大，也推测一下产品/领导对这些功能呈现效果是这样还是那样应该感知不是很强，就不用问了。

整个流程比较长，我为了降低成本肯定会选择使用不同AI进行开发和测试。像前期系统性的统筹肯定要靠你Claude，执行开发可能继续用你出你Claude，也可能用其他AI，最后的测试和review肯定用最便宜的AI了。所以每个流程之间的流转肯定不能靠一个session的上下文记忆，来完成下一个任务。所以每个流程之间最好使用文档来记录，能够让下一阶段的AI知道当前任务走到什么地步，有什么需要注意的。每个流程之间要提前准备好公共的提示词，能够在每次不同的工作流里都用的上，无非就是对于测试和review要让ai独立思考通览全局。这里指的多个ai，是指可能用多个agent cli工具，也接入多个模型，协同工作。所以一定要再项目的跟层的agents.md 和 claude.md或者skill把规范说好，让ai能够读到对应的skill文档。（你也告诉我改吧skill文件放在什么位置合适）最后的接口文档，测试和review也要有文档。

我觉得这样skill和提示词文档的安排应该是（完全摒弃现在文件夹目录创建的结构，现在docs太乱了 ）
/work-wsl/这下面放个文件夹，用来放公用的给AI看的文档，这里面的文件一般不用我维护，全程让AI自己进化。
同级目录下还有个文件夹，用来放一些我文件夹，这里记录一些通用的经验，比如 docs/工作文档日常记录/记录.md 这个文件就记录一些通用的经验和笔记，还有同事发给我的一些文档。

然后就是按项目/需求的文件夹了，比如这次的的台账需求，我就可以把excel需求列表下载下来放在对应的文件夹里，然后前面说的AI在不同阶段用来交接的文档，也放在这里。这时候我就想到一个问题，一定要在明确的地方明确规定每个阶段每个AI要生成哪些文档，放在什么位置，文件名是什么。因为一个需求下的一个流程可能来来回回让不同ai做好几遍。如果没有规范每次生成的文件名都不一样，就会很乱。也要想好有可能到一个场景一个需求下，我就忽然觉得要让AI生成一份文档，我来看这份文档，这种在一开始规范里没有计划的文档，也要有规范命名、放置的位置。
我想文件命名的话，一定要用中文，然后后面跟着codex/cc（也就是简写claudecode）/oc（简写的opencode）配上agent的名称。然后文档也要用git管理。

对了我用的是wsl环境，我有时候直接在wsl的终端里起一个agent cli，有时候用桌面端app就像现在一样，在Windows的环境里去读取wsl里的文件夹，这时候经常命令执行有问题，你看看是让我配置个什么工具啥的，还是在skill里写好

像执行开发和拆解需求阶段，就可以直接让ai接入sql了。把sql账号密码，前端token（复制来的curl整个）等你觉得需要我提供方便AI执行的这些数据也准备一份文档，让我放进去，AI可以直接拿到这些账号密码token等数据。

就像上面说让ai直接进数据库查询这种通过我的配置，能让AI高效完成任务的流程，如果有可以配置的，或者有你想到的方面，一定要多跟我说，我可以和你尽可能提高开发效率。不要觉得会麻烦我，你可以天马行空的要一些。

同时在增效的同时，也要想一些降低token使用，提升执行速度的方式，比如一开始没有装mysql没法远程链接，我看你只能用内置python环境执行，这种情况一定要跟我说让我去装一个。再比如我有时候看到你的命令执行失败，也要复盘出来跟我说该怎么优化。你遇到或者想到任何能提升自己效率节省token的问题都跟我说。或者需要我配置一些能降低token使用量的工具也行。
也就是你一定要用各种方式尝试去降本增效的，可以自己优化skill流程，可以去让我帮你解决一些问题，可以让我和你一起开发一些工具，可以去找别人的工具仓库来安装等，可以安装别人的skill（要装太重的skill的话要经过的同意）

对于编译测试，我也想听听你的计划，我觉得如果小改动什么的可以不编译，大改动再编译测试一下，这是一个todo吧，我也想解决一下几乎不能本地编译这个问题。

总之就是要可进化，可以任何AI一同维护，每个阶段留痕，方便不同agent协同。（当前协同还是靠我手动，自动互相调用agent作为todo吧）
