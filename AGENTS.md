# AGENTS.md — 所有 AI 的唯一必读入口

任何 AI 工具（Claude Code=`cc` / Codex=`codex` / OpenCode=`oc` / 其他）接到本工作区的任务，**只强制读这一份**。
其余文档全是「按需查」——第 3 节的索引表告诉你什么时候该翻哪一份。不要开工前把 `ai-docs/` 通读一遍，那是浪费。

## 0. 协作模式（一句话）

用户是 Node 背景开发者，不懂 Java。AI 负责**全部技术执行**（读代码、查库、写代码、编译验证、本地提交、接口测试、写文档）；
用户负责**人的接口**（拿 AI 备好的问题和话术去问产品/领导/同事、点 Jenkins、找人开权限、在管理页面点配置）。
对用户只说大白话；要用户去问人的给逐字话术；要用户动手的**攒成清单最后一次性给**，过程中不零碎打断。

## 1. 红线（六条，违反立即停下纠正）

1. **远程 git 一律要用户当次点头**。禁止 `push`、动远程分支、提 MR——顶层文档仓和公司子仓（`01/02/03zhaocai-*`）都算。本地提交随便做。
2. **凭据只存 `ai-docs/creds.env`**（已 gitignore）。不进代码、不进提交、不进任何其他文档、不进聊天。细则见 [ai-docs/安全红线.md](ai-docs/安全红线.md)。
3. **生产环境 AI 不碰**：prod 库、prod K8s、Jenkins 构建按钮、`mvn deploy` 到私服，一律只准备步骤交给用户或运维。
4. **同事的分支只读**（如 `prod-chun0518`）。可比对、可参考，禁止追加提交；要补丁就从团队约定基线新开自己的分支。
5. **核实，不猜。** 写进文档或转述给别人的每个结论都要落到「某文件某行」或「一条查询结果」。不知道就留空，不要用默认值填充未知；推断必须显式标注是推断。
6. **提交信息只写一行**：`feat: 简短表述` 或 `fix: 简短表述`。禁止 `(模块)` scope、正文要点、完成清单、`Co-Authored-By`。像同事随手写的，不要过度完整。

## 2. 任务分级（先判量级，再决定流程重量）

不是每件事都要走完整流程。**先对号入座，再决定读多少、写多少。**

| 量级 | 判定 | 额外要读 | 收工要写 |
|---|---|---|---|
| **查任务** 找代码/查数据/答疑 | 不产生任何修改 | 够用的背景就行 | **什么都不写**（发现可复用规矩才顺手进活文档） |
| **小改** 改几行/几个文件 | 不新开需求文件夹 | 所属主题的主文档（如有） | `ai-docs/工作日志.md` 顶部一条（≤4 行） |
| **排查/运维专题** 线上异常、依赖升级、环境对接 | 有产出但不是新功能 | 该主题文件夹已有文档 | `10-调查` 或 `20-执行` 一份 + 工作日志一条 |
| **完整需求** 新功能、要上线 | 新开主题文件夹 | 主题文件夹全部前序文档（按编号） | 走 skill 全流程 + 全套交接文档 + 工作日志一条 |

判不准就往轻里走，中途发现变重了再补文档。**小题大做和该留档不留档一样有害。**

## 3. 索引：遇到什么事，翻哪一份

| 你正要做的事 | 去处 |
|---|---|
| **刚接手，想知道环境和分支现在什么状态** | `bash scripts/doctor.sh`（30 秒，开工前建议再跑一次 `--fetch`） |
| 推需求/走完整开发流程 | `skills/enterprise-dev-workflow/SKILL.md`（cc 会自动触发；其他工具手动读） |
| 开发 Excel 导出、配置或修复导出模板 | `skills/enterprise-dev-workflow/references/export-template-playbook.md`（普通列表导出默认必须走平台模板） |
| 线上异常排查：404/502、没数据、任务失败、镜像旧、配置漂移 | `skills/diagnose-scm-project/SKILL.md`（脚本型，先跑命令别先猜） |
| **用户想跳出项目聊行业**：开开眼界/科普/这行怎么划分/一般公司都这样吗/我的经验值多少钱 | `skills/industry-perspective/SKILL.md`（cc 会自动触发；**每次聊完必须回写 `references/校准记录.md`**） |
| 分支怎么开、提交怎么进 test、发版 test→uat→prod 找谁 | [ai-docs/团队规矩.md](ai-docs/团队规矩.md) |
| 这个问题该问人还是自己拍板、话术怎么写、怎么跟用户讲 | [ai-docs/协作规矩.md](ai-docs/协作规矩.md) |
| 凭据、敏感文件、哪些命令禁止 | [ai-docs/安全红线.md](ai-docs/安全红线.md) |
| 这个接口/页面属于哪个服务、环境地址、中间件在哪 | [ai-docs/系统地图.md](ai-docs/系统地图.md) |
| 代码怎么分仓分层、AIM 是什么、Jenkins 和 mvn deploy 的区别 | [ai-docs/代码库地图.md](ai-docs/代码库地图.md) |
| **`01zhaocai-end/`（后端）`02zhaocai-front/`（前端）`03zhaocai-start/`（启动工程）分别是什么** | 同上。**三者的子目录才是独立 git 仓，顶层不是**——在顶层跑 `git show origin/test` 会落到文档仓并报「无此引用」 |
| 本地跑起来、隔离编译、IDEA/JDK 配置 | [ai-docs/本地运行手册.md](ai-docs/本地运行手册.md) |
| 有没有现成脚本能省事 | [ai-docs/工具箱.md](ai-docs/工具箱.md)（**动手前先扫一眼，别重造轮子**） |
| 找前端页面文件 | `bash scripts/fe.sh find <中文关键词>`；原理见 [ai-docs/前端页面定位.md](ai-docs/前端页面定位.md) |
| **想知道某个需求以前做过没有、历史方案在哪** | [docs/需求/INDEX.md](docs/需求/INDEX.md)（**新开主题必须去那里加一行**） |
| 之前的 AI 都干了什么 | [ai-docs/工作日志.md](ai-docs/工作日志.md)（顶部有索引段，先读索引再决定要不要读全文） |
| 2026-07 及更早的工作日志 | [ai-docs/工作日志-归档-2607.md](ai-docs/工作日志-归档-2607.md) |
| 零散技术笔记（cherry-pick 合入 UAT、nvm 多 node 版本） | `note/` |
| 别人踩过什么坑 | [ai-docs/经验教训.md](ai-docs/经验教训.md) |
| 用户自己的笔记、同事发来的文档 | `docs/工作文档日常记录/`（**可读，未经用户同意不改**） |

## 4. 环境最小必知

- 代码在 WSL：`/home/t/projects/work-wsl`（Windows 侧 UNC `\\wsl.localhost\ubuntu-24.04\...`）。
- **Windows 侧的 AI**（桌面 app / Windows 终端）有两个坑：
  ① UNC 路径上禁止全树搜索，会超时——批量搜索一律进 WSL 跑；
  ② **内联命令的变量和引号会被 wsl 边界吞掉**（`$VAR` 变空、引号丢失）。凡是带变量/引号/循环的，**先写成脚本文件再执行**：
  `MSYS_NO_PATHCONV=1 wsl.exe -d ubuntu-24.04 -- bash -s <<'EOF' ... EOF`，或写 `/tmp/xx.sh` 再 `bash /tmp/xx.sh`。
- **WSL 终端里的 AI**：直接跑 bash，以上两条都不存在。
- 常用脚本一律 `bash scripts/<名>.sh`，凭据自动读，不用自己拼命令。清单见 [ai-docs/工具箱.md](ai-docs/工具箱.md)。

## 5. 文档规范：放哪、叫什么

**一件事一个文件夹**：`docs/需求/<主题名>/`。不限于产品需求——排查、升级、环境搭建、工具开发同样开一个。
**新开主题必须去 [docs/需求/INDEX.md](docs/需求/INDEX.md) 加一行**，否则对 AI 等于不存在。
同一业务线的后续阶段（部署、售后 bug、二期）目录名带上主目录号，如 `01b-台账售后`；已有的不改。
主题内部可以再开子文件夹（如 `bug处理/`、`04回滚/`），子文件夹内沿用同一套段号。

文件名 = `<段号>-<中文短名>-<工具代号>.md`。工具代号 `cc`/`codex`/`oc`；**没有工具后缀 = 用户自己写的**。

| 段号 | 装什么 | 典型文件 |
|---|---|---|
| **00** | 输入：产品需求原件、同事给的文档、用户的原始描述 | `00-需求原件/`、`00-问题描述.md` |
| **10** | 想清楚：拆解、方案、调查、根因 | `10-拆解规划-cc.md`、`10-根因调查-oc.md` |
| **11** | **待确认问题**——要拿去问产品/领导/同事的，带逐字话术 | `11-待确认问题-cc.md` |
| **12** | **问题答复**——用户带回来的答案 | `12-问题答复.md`（无工具后缀） |
| **13** | ★ **需求定稿 / 开发基线**——讨论期到此收口，此后按它开发。**进入 20 段的闸门** | `13-需求定稿-开发基线-cc.md` |
| **20** | 动过东西之后：改了什么、分支提交号、接口文档、执行记录、回退办法 | `20-开发交接-cc.md`、`20-接口文档-cc.md`、`20-部署执行-oc.md` |
| **30** | 别人来验：测试报告、评审报告、数据对账 | `30-测试报告-oc.md`、`30-评审报告-codex.md` |
| **50** | **代码/技术**复盘、链路跟读——写给改代码的人 | `50-代码复盘与学习-cc.md` |
| **51+** | **业务/流程/概念**扫盲、培训手册——写给不看代码的人 | `51-业务流程培训手册-cc.md`、`52-企业系统全景-cc.md` |
| **90** | 无法归入以上任何一段的一次性文件。**开 90 之前先确认它不属于 20 或 51** | `90-临时-<主题>-cc.md` |
| **`问题解决/`** | 交付后的 bug、测试反馈、生产问题。**内部沿用完整段号，只是降一层**：`00-问题.md` → `10-调查-cc.md` → `20-开发交接-cc.md` → `30-验证-oc.md` | `问题解决/10-金额差异对账-cc.md` |
| `sql/` | **全部 SQL**，按序号分：①DDL ②必需配置数据 ③演示数据（单独文件） | `01-test-建表.sql`、`02-uat-导出模板.sql` |

三条硬规则：

1. **只增不覆盖。** 同阶段重做就新建文件（加轮次或子题后缀，如 `10-拆解规划2-cc.md`、`10b-逐字段讲解-cc.md`），
   并在新文件开头写一行「本文取代 xx，取代原因：yy」。每一步都要留痕，不许把上一版洗掉。
2. **每份文档头部四要素**：日期、工具代号、当前状态一句话、下一步该做什么。模板见 `skills/enterprise-dev-workflow/references/doc-templates.md`。
3. **阶段之间只靠文档交接，不靠会话记忆。** 接手先按段号从小到大读该主题文件夹，不要问用户「之前发生了什么」。
4. **段号只表示阶段，不表示顺序。** 同阶段多轮用字母后缀（`10b` `10c`），
   **不要用 `01/02/03` 当流水号**——会和段号撞车，AI 分不清 `03xxx.md` 是"第三个文件"还是"30 段变体"。
5. **事实失效了不删，只标**：`~~旧内容~~ 【失效 YYYY-MM-DD：原因 / 新值在哪】`。
   删了就查不到历史，标了 AI 会知道别用。可 `grep -rn "【失效"` 批量审计。
6. **推断统一格式**（红线 5 的落地）：`【推断】结论（无法证实的原因：xxx）`。
   `grep -rn "【推断】" docs/ ai-docs/` 即为完整的未证实结论清单。

## 6. 活文档义务（每次收工顺手做）

| 你新学到的 | 写去哪 |
|---|---|
| 公司/团队的规矩（分支、发版、谁负责什么） | `ai-docs/团队规矩.md` |
| 代码库结构、构建发布机制的持久事实 | `ai-docs/代码库地图.md` / `ai-docs/系统地图.md` |
| **还会再犯**的坑（换个仓库、下个月还会踩） | `ai-docs/经验教训.md`（带日期+工具代号，一条一行） |
| 动了代码/库/配置 | `ai-docs/工作日志.md` 顶部一条 |

**判据（很重要）：同一类操作做到第二遍，如果还要回忆参数、路径、顺序，或者要翻文档——这是工具缺口，不是记忆缺口。**
该做成 `scripts/` 下的脚本，而不是再写一条笔记。写进文档的教训会重踩，封装进脚本的不会。
**文档服务「人要理解」（业务、决策理由、话术），脚本服务「机器要重复」（命令、检查、对账）。搞混就是浪费。**
